# MPPI 채널 제어기

채널 MPC 를 **선형화 + QP** 에서 **MPPI**(Model Predictive Path Integral, 샘플링 기반)로
바꾼 버전에 대한 설명이다. 구현은 `include/Mppi.hpp` + `src/Mppi.cpp` (약 750 줄),
연결은 `src/Controller.cpp` 의 `AcadosMpc::solve()`.

시스템 전체 구조는 `README.md`, 실기 피팅 절차는 `RUNBOOK.md` 를 볼 것.

---

## 0. 한눈에

| | 값 |
|---|---|
| 대체한 것 | `update_linearization → build_mpc_qp → solve_qp_first_step` (보정 Δu 산출) |
| 유지한 것 | 역모델 피드포워드 + 적분항(`uref`), 명령 테이퍼, 크래킹 클램프, macro 게이트 |
| 제어 주기 | **500 Hz 유지** (낮출 필요가 없었다) |
| 계산 시간 | 채널당 **303~309 µs 평균 / 651~665 µs 최대** (틱 2000 µs = 33%, 32코어 12채널 병렬) |
| 켜고 끄기 | `MPC_parameters.solver: mppi | qp` — 같은 빌드로 A/B 가능 |
| 성능 | 위치 IAE **−62%**, 정착 **−54%**, 압력 RMSE **−40%**, 정상상태 압력오차 0.99 → 0.013 kPa (전부 범위 비중첩) |

`solver: qp` 도 그대로 남아 있다. 이 하네스는 비결정론적이라(README 0절) 빌드를 바꿔
비교하면 원인 분리가 안 되기 때문이다.

---

## 1. 왜 선형화를 버렸나

기존 경로는 13-parameter 밸브 모델을 수치 야코비안으로 선형화해 `(A_scalar, B_row)` 를 만들고
Δu 에 대한 응축 QP 를 qpOASES 로 풀었다. **두 가지가 구조적으로 깨져 있었다.**

1. **초킹 구간에서 `A_scalar` 가 정확히 0 이다.** 초킹 유량은 하류압과 무관하므로
   `dQ/dPout = 0` 이고, 양압 채널의 `A_scalar = mi[2] + ma[2] − at[1] − lk[1]` 이 통째로 0 이
   된다. 그러면 `T_bar = 0` 이 되어 목적함수가 `(P_ref − P_now)` 가 아니라 **절대압 `Xref`** 를
   추종하려 한다. 동작점 대부분이 초킹이라 상시 조건이다.
2. `A_scalar` 는 연속시간 야코비안 `∂Ṗ/∂P [1/s]` 인데 `build_mpc_qp` 가 이산 전이행렬로 써서
   `A¹..A^NP` 를 만들었다 (`1 + A·Ts` 여야 한다).

그 결과 **Δu 가 사실상 0 이었다** — 즉 실제로 동작하던 것은 피드포워드 + 적분항이고 MPC 계층은
무력했다. QP 가 무해했던 것도 그래서다.

그리고 이 플랜트의 지배적 비선형성은 **선형화로 표현될 수 있는 종류가 아니다.**

| 성질 | 왜 선형화가 안 되나 |
|---|---|
| **크래킹 임계** | `A_eff = A_max·sigmoid(k·F)^alpha` 에 `alpha≈3884` → 사실상 계단. ≈52%(상류압 의존) 아래에서 유량이 **정확히 0** |
| **초킹** | 하류압 민감도가 구간에 따라 0 이거나 유한 — 야코비안이 동작점에서 정보를 잃는다 |
| **명령 테이퍼** | 목표 근처에서 "크래킹 임계 위쪽 여유분" 만 줄인다 (조각별 정의) |
| **밸브 2차 동특성** | `wn≈40 rad/s → τ≈25 ms` 로 **지평(40 ms)과 같은 스케일**인데 선형화 경로는 아예 무시했다 |

MPPI 는 기울기가 필요 없다. 위 모델을 **그대로** K 개 롤아웃하고 `exp(−J/λ)` 가중 평균한다.
계단·불연속·포화·상태 의존 지연이 전부 자연스럽게 들어간다.

---

## 2. 이 플랜트가 왜 어려운가 — 숫자 하나

챔버 50 mL 에 밸브를 완전히 열면 **4 ms 에 99 kPa** 가 움직인다
(레일 351 kPa abs, 초킹, `A_max = 0.2845`):

```
Q      = A_max · P_in · Φ_choked = 0.2845 × 351 × 0.685 ≈ 68 LPM
dP/dt  = R·T·ṁ / V = 287 × 293.15 × (68 × 2.155e-4) / 1000 / 50e-6 ≈ 24 800 kPa/s
```

그래서 지평 10 스텝 롤아웃 중 일부는 **1000 kPa 를 스윙**한다. 비용 분포의 꼬리가 극단적으로
두껍고, 이것이 아래 설계 선택 대부분의 이유다. 크래킹 임계와 명령 테이퍼가 없으면 이 플랜트는
사실상 온-오프다.

---

## 3. 알고리즘

### 3.1 위치 — MPPI 는 피드포워드를 대체하지 않는다

```
solve(dt, out3, t):
  err   = ref − P_now
  uref  = compute_input_reference(...)      # 역모델 피드포워드 + 밸브별 Ki·∫e
                                            #   + Bouc-Wen z 갱신 + 크래킹 임계 갱신
  Δu    = mppi_->solve(x0, ex, uref)        # ← QP 가 있던 자리
  u     = taper_above_crack(clamp(uref + Δu, 0, 100), u_crack)
  out3  = u × 40.95                          # PWM
  advance_valve_estimate(...)                # 밸브 내부 상태(q, qd) 추정 전진
```

MPPI 는 `uref` **주변의 보정** Δu 를 낸다. 전권을 주지 않는 이유는 밸브 13-parameter 가
아직 전부 추정값이어서(README 0절) 모델 오차를 흡수할 적분항이 필요하기 때문이다.
파라미터 피팅(README 4·5절)이 끝나면 전권 MPPI 로 가는 것이 다음 단계다.

### 3.2 롤아웃 모델 — 시뮬과 식이 동일하다

`VirtualPowerpack::step_valve` / `pressure_derivative` 와 **같은 식·같은 상수·같은 오일러 순서**를
쓴다 (`R=287`, `T=293.15 K`, `LPM→kg/s = 2.155e-4`, `κ=1.4`). 그래야 가상 하드웨어에서의 비교가
"모델이 달라서 생긴 차이" 가 아니게 된다. 바꿀 때는 반드시 양쪽을 함께 바꿀 것.

밸브 1개, 한 스텝:

```
I      = u/100 · I_MAX
z      ← z + A_bw·dI − beta_bw·|dI|·z − gamma_bw·dI·|z|          (Bouc-Wen)
F_net  = clamp(I + C_z·z + C_p·P_in − C_k, −500, 500)
A_eff  = A_max · sigmoid(k_shape·F_net)^alpha_shape
Q_stat = A_eff · P_in · Φ(P_in, P_out)                            [LPM]
q̈      = wn²·(Q_stat − q) − 2ζ·wn·q̇        (wn/ζ 는 dI 방향별)
q      = max(0, q)
```

챔버 (등온 이상기체):

```
dP/dt = ( R·T·ṁ/1000 − P·V̇ ) / V ,   ṁ = Q_net · LPM_TO_KGPS      [kPa/s]
Q_net = (양압) q_mi + q_ma − q_at − q_leak
      = (음압) q_at + q_leak − q_mi − q_ma
```

상태: `P` + 밸브 3개 × `(q, q̇, z, prevI, dir)`. 외생 입력(레일압, `V̇`)은 지평 동안 상수다.

**밸브 상태 추정(`q`, `q̇`)을 틱 사이에 이어 간다.** `advance_valve_estimate` 가 매 틱 실제 인가
명령 + 측정 압력으로 이 선형 필터를 전진시킨다. 이유: `τ≈25 ms` 가 지평 40 ms 와 같은 스케일이라
"지금 유량이 정상상태" 로 시작하면 지평 안에서 가장 큰 항을 틀리게 잡는다. 명령이 2 ms 마다
바뀌므로 실제 `q` 는 상당히 지연돼 있다. 안정한 2차계라 발산하지 않고 오차원은 모델 불일치뿐이다.

### 3.3 두 가지 수치 최적화

`alpha_shape ≈ 3884` 때문에 순진한 구현은 느리고 불안정하다.

- **`A_eff`**: `pow(sigma, 3884)` 대신 `A_max·exp(−alpha·log1p(exp(−k·F)))`. exp 1회 + log1p 1회.
  `F_net ≤ F_open` 이면 float 언더플로가 확정이라 즉시 0 을 반환한다 (닫힌 밸브는 대부분 여기서 끝난다).
- **`u_crack`**: `A_eff = frac·A_max` 가 되는 `F_net` 은 `frac` 만으로 정해지므로
  **`P_in`·`z` 와 무관한 상수**다. 그래서 `u_crack(P_in, z) = (F_crack − C_z·z − C_p·P_in + C_k)/I_MAX·100`
  으로 런타임에 log 없이 구한다 (`P_in`·`z` 에 affine). 351 kPa abs 에서 52.3% — 문서값과 일치.

### 3.4 비용

```
J = (1/NP) · Σ_k [ w_track·log(1 + e_k²)  +  Σ_j ( w_effort·d_kj² + w_du·Δd_kj² ) ]
    + w_track · terminal_mult · log(1 + e_T²)

e_k  = (P_k − P_ref(k)) / track_scale_kpa
d_kj = (u_kj − uref_j)/100          이탈
Δd   = (u_kj − u_{k−1,j})/100       변화율
```

**`e²` 가 아니라 `log(1+e²)` 다.** 2절의 이득 때문에 일부 롤아웃이 1000 kPa 를 스윙하고,
`e²` 로 재면 그 하나가 `(Jmax−Jmin)/(median−Jmin)` = **2285** 까지 비용 분포를 지배한다.
그러면 어떤 산포 척도로 정규화해도 가중치가 균일해지고 선택이 사라진다. `log(1+e²)` 는
**단조**라 순서를 보존하면서 크기만 압축한다 (`e=100 → 9.2`, `e=1 → 0.69`). 배율 2285 → 29.

**`P_ref(k)` 는 상수가 아니라 1차 접근 궤적이다** — 3.6 참조. 이것이 성능을 가장 크게 바꿨다.

### 3.5 가중과 갱신

```
J_min, J_med = min, median(J)                    ← 중앙값. 평균·최댓값은 꼬리에 무너진다
λ_eff = λ · max(J_med − J_min, ε)                ← 비용 스케일에 불변
w_i   = exp( −(J_i − J_min) / λ_eff )
nom  ← clamp( nom + Σ (w_i/Σw) · ε_i , du_min, du_max )     ← **원 노이즈** 가중 평균
Δu    = clamp( nom[0], max(−uref, du_min), min(100−uref, du_max) )
nom  ← shift(nom)                                 ← 워밍 스타트 (한 스텝 밀고 마지막 복제)
```

세 가지가 의도적이다.

- **λ 를 절대값이 아니라 비용 산포에 대한 비율로 쓴다.** 비용 스케일이 오차 크기·챔버 부피·
  레일압에 따라 통째로 변하므로 절대 λ 는 동작점마다 재튜닝해야 한다.
- **산포는 중앙값 기준.** `(Jmax−Jmin)` 은 이상치 하나에 무너지고, 평균도 함께 끌려 올라간다.
  중앙값은 K/2 개가 바뀌어야 움직인다.
- **갱신은 원 노이즈의 가중 평균**(MPPI 표준형). 인가값(클램프된 Δu)의 평균을 쓰면
  `uref=0` 에서 박스가 `[0, du_max]` 라 대칭 노이즈의 음수 절반이 전부 잘려 **평균이 구조적으로
  양수**가 된다 — "닫아라" 라는 피드포워드를 매 틱 밀어 올린다.

### 3.6 데드밴드 플랜트를 위한 세 장치

크래킹 임계가 있는 플랜트에서 순진한 MPPI 는 작동하지 않는다. 세 가지를 넣었다.

**(a) 혼합 분산.** 정상상태에서 `uref ≈ 0` 인데 표준편차가 작으면 **모든 샘플이 임계 아래**라
궤적이 전부 같고 비용도 같아진다. 그래서 대부분은 `sigma_pct` 로 동작점을 다듬고,
`explore_frac` 만큼은 `sigma_explore_pct` 로 임계를 건너뛴다. 단일 분산으로는 정밀도와
도달성을 동시에 얻을 수 없다. 같은 이유로 MPPI 의 Δu 한계는 QP 의 ±30 이 아니라 **±100** 이다
(±30 이면 `uref≈0` 에서 52% 에 도달조차 못 한다).

**(b) 평평 가드.** 그래도 비용이 평평한 틱은 많다 — 실측 **약 70%** 다. 대부분의 시간 밸브는
임계 아래에 있으니 정상이다. 그런 틱에 갱신하면 `Σŵε` 가 순수 잡음이고, 500 Hz 로 누적되면
명목이 무작위 보행으로 박스 끝까지 표류해 거기서 잠긴다 (계측: 첫스텝 포화 **99.9%**).
그래서 `median − min ≤ flat_spread` 인 틱에는 갱신 대신 명목을 `flat_decay` 로 0(= 순수
피드포워드)에 감쇠시킨다. 정보가 없을 때의 안전한 기본값이다.

**(c) 테이퍼를 롤아웃에 넣는다.** 플랜트가 실제로 받는 값은 테이퍼된 명령이다. 그 불연속을
롤아웃에 넣어야 MPPI 가 경계를 안다 — 선형화가 표현하지 못하는 종류의 비선형성이 바로 이것이다.

**(d) 스테이지 레퍼런스를 1차 접근 궤적으로.** 지평 전체를 `P_ref` 상수로 두면 MPPI 에게는
"40 ms 안에 전 오차를 닫아라" 가 된다. 그런데 `P_ref` 는 생성기가 **슬루 제한해 만든 궤적의
현재 점**이고 챔버는 2절의 이득으로 실제로 닫을 수 있다 — 그래서 최대 공격성으로 밀고 위치가
오버슈트한다. 대신

```
P_ref(k) = P_ref − (P_ref − P_now)·exp(−k·Ts / mppi_ref_tau_s)
```

를 기준으로 주면 MPPI 가 "경쟁하는 공격적 제어기" 가 아니라 "그 궤적에서 모델 오차를 메우는
보정기" 가 된다. **`mppi_ref_tau_s` 가 가장 영향이 큰 노브다** (6절).

---

## 4. 파라미터

`config/powerpack_config.yaml` → `MPC_parameters`. 각 항목의 근거는 그 파일의 주석에 있다.

| 파라미터 | 기본값 | 뜻 / 튜닝 |
|---|---|---|
| `solver` | `mppi` | `mppi` \| `qp` |
| `mppi_samples` (K) | 128 | 64~192 에서 품질 차이가 측정 분산 안이었다. 예산이 빠듯하면 64 로 |
| `mppi_np`, `mppi_ts_s` | −1 (= `NP`, `Ts`) | 계획 지평. **늘리지 말 것** — 6절 |
| `mppi_substeps` | **1** | 스텝당 오일러 서브스텝. 2 는 비용 2배에 이득이 없었다 |
| `mppi_ref_tau_s` | **0.12** | 스테이지 레퍼런스 접근 시상수 [s]. **가장 영향이 크다.** ≤0 이면 `target_time_constant` |
| `mppi_lambda` | 0.15 | `λ_eff = λ·(median−min)`. 0.60 은 나빴고 0.08~0.15 가 최선 |
| `mppi_sigma_pct` | 8.0 | 정밀 탐색 표준편차 [명령 %] |
| `mppi_sigma_explore_pct` | 30.0 | 임계 돌파용 표준편차 |
| `mppi_explore_frac` | 0.30 | 큰 sigma 를 쓰는 샘플 비율 |
| `mppi_noise_beta` | 0.70 | 노이즈 1차 상관. 독립 노이즈는 250 Hz 성분이라 τ≈25 ms 밸브가 통과시키지 못해 낭비된다 |
| `mppi_w_track` | 1.0 | 추종. 음수면 `Q_values` |
| `mppi_w_effort` | 0.10 | `uref` 이탈. 음수면 `R_values` |
| `mppi_w_du` | 0.05 | 스텝 간 변화율 (채터 억제) |
| `mppi_track_scale_kpa` | 10.0 | 오차 정규화 기준 |
| `mppi_terminal_mult` | **0.0** | 말단 가중 배수. 5 → 0 이 큰 개선이었다 (6절) |
| `mppi_du_limit_pct` | 100.0 | MPPI 의 Δu 한계. QP 의 ±30 을 쓰면 임계에 도달 못 한다 |
| `mppi_taper_in_rollout` | true | 3.6(c) |

**튜닝 순서**: `mppi_ref_tau_s` → `mppi_terminal_mult` → `mppi_lambda` → 나머지.
앞의 둘이 나머지 전부를 합친 것보다 영향이 크다.

---

## 5. 진단 로그 읽는 법

10 초마다 채널별로 한 줄 나온다.

```
gid=3 MPPI: 311 us 평균 / 658 us 최대 (틱 예산 대비), 유효샘플 18.2/128,
       Jmin 0.0040, 초과 0.0020, 이상치배율 108.7, 첫스텝 포화 22.8%, 평평 77.1%
```

(기본 설정에서 실제로 나오는 값이다.)

| 항목 | 건강한 값 | 벗어나면 |
|---|---|---|
| `us 최대` | < 틱 예산(2000)의 60% | `mppi_substeps=1`, 그다음 `mppi_samples` 를 내린다 |
| `유효샘플` | K 의 **10~50%** | 1 에 붙으면 가중 붕괴(λ↑), K 에 붙으면 균일 평균(λ↓) |
| `이상치배율` | 30~150 | `(Jmax−Jmin)/(median−Jmin)`. **단독으로는 문제가 아니다** — 기본 설정에서 `초과` 가 0.002 수준으로 작아 배율이 100 을 넘는 것이 정상이다. `유효샘플` 이 K 에 붙는 것과 **함께** 나타날 때만 꼬리가 가중치를 지배한다는 신호다 |
| `첫스텝 포화` | < 30% | 99% 면 명목이 박스에 잠긴 것 — 3.6(b) 의 실패 모드 |
| `평평` | 60~80% | **정상이다.** 대부분의 시간 밸브는 크래킹 임계 아래에 있다 |

`유효샘플`·`이상치배율`은 **활성 틱(갱신을 실제로 한 틱)** 평균이다. 전체 틱으로 나누면
평평 비율만큼 축소돼 보인다.

---

## 6. 성능

### 6.1 측정 방법

`scripts/ctrl_eval.py` (가상 하드웨어, 실기 불필요). **단일 스텝으로 비교하면 안 된다** —
이 하네스는 비결정론적이어서(README 0절) 유사 설정의 IAE 가 19.5~29.2 로 흩어졌다.
그래서 한 번 실행에 스텝 4개(45→15→40→25°)를 넣고 전이별 지표를 평균한다. 그렇게 하면
재실행 재현성이 IAE 기준 ±10% 안으로 들어온다.

```bash
python3 scripts/ctrl_eval.py qp   --tag qp
python3 scripts/ctrl_eval.py mppi --tag mppi
python3 scripts/ctrl_eval.py mppi --tag t10 --overrides MPC_parameters.mppi_ref_tau_s=0.10
```

지표: 위치 **IAE**(`∫|각도−목표|`, 6축 평균) · 오버슈트 · 정착(±0.5°) · 최종오차,
그리고 MPPI 가 직접 최적화하는 **압력 추종**(`|P − P_ref|` 의 RMS, 12채널) · 정상상태 압력오차.
위치 지표만 보면 "MPPI 가 나아졌는데 상위 계층이 지배해서 위치가 안 변한 것" 과
"MPPI 가 나빠진 것" 을 구별할 수 없다.

### 6.2 결과

설정은 `config/powerpack_config.yaml` 기본값 그대로. 6축, 스텝 4개(45→15→40→25°), 각 5 s.

| | IAE [deg·s] | 오버슈트 [°] | 정착 [s] | 최종오차 [°] | 압력 RMSE [kPa] | 정상상태 압력오차 [kPa] |
|---|---|---|---|---|---|---|
| `qp` (n=6) | 14.36 (12.81~16.59) | 2.24 (1.65~2.78) | 2.46 (2.13~2.66) | 0.174 | 9.79 (9.37~10.26) | 0.99 (0.76~1.15) |
| `mppi` (n=3) | **5.44 (5.29~5.51)** | 2.24 (2.02~2.36) | **1.13 (1.07~1.21)** | 0.162 | **5.87 (5.50~6.10)** | **0.013 (0.00~0.04)** |
| 변화 | **−62%** | ±0% | **−54%** | −7% | **−40%** | **−99%** |

**IAE · 정착 · 압력 RMSE · 정상상태 압력오차는 범위가 서로 겹치지 않는다** — 이 하네스의
분산을 감안해도 확실한 개선이다. 오버슈트와 위치 최종오차는 동등하다.

읽는 법: MPPI 는 **같은 오버슈트로 절반 시간에 도달**한다(정착 2.46 → 1.13 s, IAE −62%).
그리고 정상상태 압력오차가 0.99 → 0.013 kPa 로 사실상 사라진다 — 선형화 경로에서는 Δu 가
0 이라 피드포워드의 모델 오차가 적분항으로만 지워졌고, 그 잔차가 1 kPa 수준으로 남아 있었다.

MPPI 가 nan 없이 3회 모두 IAE 5.29~5.51 로 들어온 것도 의미가 있다 — 초기 구현에서는
같은 설정 재실행이 4.71 ↔ 6.63 으로 갈렸다(8.10-1~5 의 결함들 때문). 지금은 재현된다.

### 6.3 노브별 스윕 (전부 다단 프로파일, 전이 4개 평균)

**`mppi_ref_tau_s`** — 가장 영향이 크다. `terminal_mult=0`, `substeps=1` 기준:

| τ [s] | IAE | 오버슈트 [°] | 정착 [s] | pRMSE [kPa] |
|---|---|---|---|---|
| 0.02 | 27.98 | 7.71 | 3.39 | 30.20 |
| 0.04 | 17.67 | 8.91 | 2.21 | 23.08 |
| 0.06 | 6.14 / **12.34** | 2.71 / **7.93** | 1.05 / 1.86 | 8.47 / 14.64 |
| 0.09 | 5.61 | 2.33 | 1.22 | 7.90 |
| **0.12** | **6.01** | **1.86** | **1.02** | **5.70** |
| 0.16 | 6.93 | 2.11 | 1.45 | 5.53 |
| 0.30 | 19.49 | 1.68 | 2.73 | 12.50 |

τ = 0.06 은 **같은 설정 재실행에서 결과가 완전히 달라진다**(IAE 6.14 vs 12.34) — 안정 한계다.
0.09 도 재현성이 0.12 보다 나빴다(IAE 6.97~10.70 vs 8.56~9.38). 그래서 **0.12** 를 택했다.

**`mppi_terminal_mult`** (τ=0.12, substeps=1):

| 값 | IAE | 오버슈트 | 정착 | pRMSE |
|---|---|---|---|---|
| 12 | 12.60 | 5.16 | 1.74 | 10.47 |
| 5 (초기값) | 8.96 | 2.44 | 1.57 | 7.92 |
| 2 | 6.91 | 2.43 | 1.38 | 6.28 |
| **0** | **6.32** | **2.22** | **1.16** | **6.40** |

말단 가중이 클수록 나쁘다. 조밀한 스테이지 비용이 이미 있는 receding-horizon 제어기에서
말단 항은 "지평 끝 한 점" 에 과도한 권한을 준다.

**`mppi_lambda`** (τ=0.12, terminal=0): 0.08 → IAE 5.37 · 0.15 → 5.70 · 0.30 → 6.01~6.32 ·
0.60 → 8.39. 낮을수록 좋지만 λ→0 은 최선 샘플 1개만 쓰는 탐욕적 선택이라 분산이 커진다.
**0.15** 를 택했다.

**`mppi_samples`** (τ=0.12, terminal=0): 64 → 5.63 · 128 → 6.01~6.32 · 192 → 5.64.
**차이가 측정 분산 안이다.** 샘플을 늘려 얻을 것이 없으므로 예산이 빠듯하면 64 로 내리면 된다.
512 는 **최대 2299 µs 로 틱 예산(2000)을 초과**해 제어가 붕괴했다(오버슈트 23°).

**계획 지평** (`mppi_np × mppi_ts_s`, τ=0.20 시절 측정):

| 지평 | IAE | pRMSE |
|---|---|---|
| **40 ms** (10×4 ms) | **19.53** | **5.76** |
| 80 ms (20×4 ms) | 33.28 | 10.45 |
| 120 ms (15×8 ms) | 42.71 | 12.31 |
| 160 ms (20×8 ms) | 33.58 | 10.41 |

**지평을 늘리면 단조적으로 나빠진다.** 롤아웃이 레일압과 `V̇` 를 지평 내내 상수로 두므로
40 ms 를 넘으면 그 가정이 깨져 모델 오차가 결정을 지배한다. 즉 **모델 유효 창이 약 40 ms** 다.
지평을 늘리려면 먼저 레일압·부피 예측을 롤아웃에 넣어야 한다 (8절).

---

## 7. 실시간

제어 주기는 **500 Hz 를 유지했다** — 낮출 필요가 없었다.

| 설정 | 평균 [µs] | 최대 [µs] | 틱 예산 대비 |
|---|---|---|---|
| K=128, substeps=1 (기본) | 310 | 649 | 32% |
| K=128, substeps=2 | 523~560 | 1065 | 53% |
| K=256, substeps=1 | 605 | 1173 | 59% |
| K=512, substeps=1 | 1181 | **2299** | **초과 — 제어 붕괴** |

32 코어에서 `ThreadPool` 이 12 채널을 완전 병렬로 돌린다(`min(12, hardware_concurrency)`).
비용은 `K × NP × substeps` 에 선형이다. 예산이 빠듯하면 순서대로: `mppi_substeps` → 1,
`mppi_samples` → 64. 두 항목 모두 측정된 품질 손실이 없었다.

주기를 낮춰야 한다면 `period_ms` 를 올린다(2 → 4 = 250 Hz). 그때 `mppi_ts_s` 를 손대지 말 것 —
계획 지평은 제어 주기와 독립이고, 6절대로 40 ms 가 최적이다.

---

## 8. 한계와 다음 단계

- **MPPI 는 아직 보정기다.** 피드포워드 + 적분항이 그대로 있다. 밸브 13-parameter 가 전부
  추정값인 동안은 정상상태 오차를 밀어낼 적분항이 필요하기 때문이다. 파라미터 피팅
  (README 4·5절, `RUNBOOK.md`)이 끝나면 피드포워드를 빼고 전권 MPPI 로 가는 것이 다음
  단계이고, 그때 `mppi_w_effort` 의 의미가 달라진다.
- **모델 유효 창이 40 ms 다.** 레일압과 `V̇` 를 지평 동안 상수로 둔 결과다. 더 긴 지평을
  쓰려면 레일 동역학(라인 PID + 펌프 능력경계)과 액추에이터 운동을 롤아웃에 넣어야 한다.
- **적분 안티와인드업이 피드포워드 기준이다.** `compute_input_reference` 의 안티와인드업은
  피드포워드 명령의 포화로 판정하고 MPPI 보정 후의 실제 인가값을 보지 않는다. 지금 설정에서는
  문제가 관측되지 않았지만, 전권 MPPI 로 갈 때 반드시 함께 고쳐야 한다.
- **`Ts`(4 ms) 와 제어 주기(2 ms)가 다르다.** 계획 격자가 제어 격자보다 거친 것은 MPC 에서
  정상이지만(매 2 ms 재계획), 모델은 첫 명령이 4 ms 유지된다고 본다.
- **위치 성능의 상한은 상위 계층이 정한다.** MPPI 가 압력을 완벽히 추종하면 그다음은
  TorquePID + PressureRefGen 이 결정한다. 6.2 의 압력 지표가 그 여유를 보여준다.

---

## 9. 파일

```
include/Mppi.hpp        모델 커널 선언 + PlantParams/Params/Stats + Solver (249 줄)
src/Mppi.cpp            Φ·A_eff·Bouc-Wen·2차 동특성·챔버 ODE, RNG, 롤아웃·가중·갱신 (497 줄)
src/Controller.cpp      AcadosMpc::solve() 의 솔버 분기, 파라미터 로더, 진단 로그
config/powerpack_config.yaml   MPC_parameters.mppi_* (근거 주석 포함)
scripts/ctrl_eval.py    성능 평가 하네스 (6.1)
launch/virtual.launch.py       solver:= / overrides:= 인자
```

구현 과정에서 막힌 것들의 상세 기록은 `README.md` 8.10 절에 있다.

---

## 10. 더 올리려는 시도와 그 결과 (2026-08-21)

MPPI 로 바꾼 뒤 "여기서 더 좋아지려면 무엇을 고쳐야 하나" 를 조사해 세 가지 모델 오차를
정량화하고 전부 구현·측정했다. **결론부터: 세 가지 다 실제로 존재하지만 제어 성능에는
거의 기여하지 않았고, 남은 병목은 압력 계층이 아니라 그 위에 있다.**

측정은 전부 `scripts/ctrl_eval.py`(스텝 4개 평균, 웜업 10 s)로, 같은 빌드에서
`overrides:=` 로 A/B 했다. 기준선(배포 기본값): **IAE 5.32~5.73, 정착 1.05~1.10 s,
압력 RMSE 5.58~5.63 kPa**.

### 10.1 출발점 — 한계는 최적화기가 아니다

표본 수 K 를 64 → 128 → 192 로 늘려도 성능이 **전혀** 변하지 않았다. 제어기는 자기
시뮬레이션 안에서 이미 최선을 찾고 있고, 그 시뮬레이션이 실제와 다르다는 뜻이다.
그래서 "더 잘 푸는 방법" 이 아니라 "무엇을 틀리게 알고 있나" 를 조사했다.

### 10.2 측정 지연 ≈18 ms (지평의 45%)

LPF 가 직렬 2단이다 (브리지/시뮬 α=0.2, 컨트롤러 α=0.2, 각 τ≈9 ms @500 Hz).
dP/dt 500 kPa/s 에서 초기상태 오차 9 kPa 이고 측정 pRMSE 가 5.87 이었다 — 같은 자릿수다.

| | IAE | 압력 RMSE | 정상상태 압력오차 |
|---|---|---|---|
| 필터값 (기준) | 5.30 | 5.51 | 0.25 |
| 생값 (`mppi_raw_state`) | 6.43 | 5.09 | 0.00 |
| 관측기, 초기상태에만 적용 | 6.71 | **4.91** | 0.00 |
| 관측기, **일관 적용** (`mppi_estimator`) | 5.78 | 5.54 | 0.00 |

읽는 법:
- 지연을 없애면 **MPPI 가 직접 최적화하는 압력 추종은 개선된다** (5.51 → 4.91).
- 그런데 생값은 양자화 잡음(양압 보드 0.25 kPa/LSB)이 그대로라 위치가 나빠진다.
  → 그래서 **관측기**가 답이다: 모델로 앞서 예측하고 그 예측에 **같은 LPF 2단을 복제**해
  측정과 같은 조건으로 비교한 뒤 잔차로 보정한다. 필터가 안 걸린 추정값을 MPPI 에 준다.
- **일관성이 중요했다.** 추정값을 초기 상태에만 쓰고 오차·피드포워드·적분항은 필터값을
  쓰면 과도 구간에 둘이 최대 9 kPa 어긋나 MPPI 보정이 피드포워드와 싸운다
  (IAE 6.71). 일관 적용으로 바꾸자 5.78, 정착 1.22 → **1.08**, 정상상태 압력오차 → **0.00**.

**기본 OFF.** 이유: 위치 IAE 가 소폭 나쁘고, 더 중요하게는 **평가 지표가 개선을 다 볼 수
없다** — pRMSE 를 필터된 측정값으로 재므로 진짜 압력이 좋아져도 반영이 덜 된다.
실기에서는 잡음 특성이 다르므로 그때 다시 판단할 것. 적분항을 없애는(offset-free) 다음
단계의 전제이기도 하다.

### 10.3 공유 레일을 무한 소스로 가정 — 물리적으로 실재하나 제어에는 무의미했다

조사로 나온 숫자들:
- 레일 부피 500 mL vs 그 레일이 먹여 살리는 챔버 6개 합 300 mL → 비 **1.67**.
  "무한 강성 소스" 가 아니다.
- 채널 밸브 **하나**의 완전 개방 유량(4.6 g/s)이 펌프 전체 토출(≲1 g/s)의 **5배**.
- 40 ms 지평 동안 무시된 강하: 채널 1개 ≈5 kPa, 양압 6채널 동시 ≈34 kPa
  (추종 정규화 기준 10 kPa 대비 0.5~3.4σ).
- 양압 라인 PID 는 **대기 방출 밸브밖에 없어** 레일압을 올릴 수단이 없고 정상상태에 이미
  포화(u=100)해 있다. 채널이 레일을 끌어내릴 때 남은 권한이 0 이다.
- 그리고 **"채널 수요 합 vs 레일 공급" 을 비교하는 코드가 저장소 전체에 없었다.**

가장 싼 형태로 구현했다 (`mppi_rail_rate`): 12채널의 **명목 명령**으로 레일 질량수지를
한 번 계산해 `dP_rail/dt` 를 내고, 롤아웃이 레일을 그 기울기로 이동시킨다.

| | IAE | 오버슈트 | 정착 | 압력 RMSE |
|---|---|---|---|---|
| OFF (기준) | **5.32** | 1.84 | 1.05 | **5.63** |
| ON | 5.62 | 2.46 | 1.11 | 6.01 |

**중립~약간 나쁘다.** 그리고 이유가 명확하다: `PressureRefGen::build_slew_box` 가 이미
**측정된 레일압으로 20 ms 안에 도달 가능한 ΔP** 를 계산해 레퍼런스를 제한한다. 즉
레퍼런스가 애초에 레일이 못 주는 것을 요구하지 않으므로, 예측 정확도를 높여도 살 것이 없다.
**슬루 박스가 존재하는 이유가 바로 이것이었다.** 기본 OFF, 스위치는 남긴다.

### 10.4 전체 시스템 중앙집중 MPPI — 구현했고, 모델은 맞았고, 제어는 나빴다

`solver: mppi_system` (`include/MppiSystem.hpp`, `src/MppiSystem.cpp`).
12채널 + 라인 밸브 2개를 하나의 최적화로 푼다.

**속도는 문제가 아니었다.** 플랜트의 밸브 개수는 어느 쪽이든 같으므로 같은 K 에서 총
계산량이 1.24배다. 병렬화 축을 채널 12개 → 표본 K개 로 바꾸면 된다.

**모델은 정확했다.** 1스텝 예측 레일압 vs 측정 레일압 잔차가 **양 0.26~0.40 / 음
0.57~0.90 kPa** 다. 즉 `VirtualPowerpack::integrate` 와 sub-kPa 로 일치한다.
(이 연속 교차검증은 코드에 남아 있어 두 파일의 식이 갈리면 로그에 드러난다.)

**그런데 제어 성능이 나빴다.** 그리고 그 과정에서 찾은 것들이 이 절의 실질적 소득이다.

| 단계 | IAE | 무엇을 고쳤나 |
|---|---|---|
| 초기 | 30.9 | — |
| 펌프 테이블 배선 순서 | 26.3 | `build_system_mppi` 를 `refgen_` 생성 **뒤로**. 그전에는 펌프 유량이 0 이라 모델이 "양압 레일은 절대 회복 못 한다" 고 믿었다 (레일 예측오차 24.6 kPa) |
| 핫패스 힙 할당 제거 | 29.9 | 롤아웃이 표본마다 vector 4개를 만들어 틱당 1024회 할당을 12스레드가 동시에 → malloc 경합. 평균 1138 → 725 µs |
| **factored 공로 배분** | **21.1** | 표본 하나가 26개 밸브를 동시에 흔들면 채널 3 에 좋고 채널 7 에 나쁜 표본이 중간 점수를 받아 가중 평균이 전부를 뭉갠다. → 롤아웃은 중앙집중, **비용·가중은 그룹(채널 12 + 라인 1)별로** |
| 캐시 친화 갱신 | — | 제어 인덱스를 바깥 루프에 두면 260 KB 배열을 인덱스마다 다시 훑어 틱당 ~69 MB. 표본을 바깥으로 |
| 레일은 예측만, 제어는 PID | **17.8** | MPPI 가 라인 밸브를 잡으면 **불안정**했다 (양압 74↔800, 음압이 대기압 넘는 107. PID 는 222/100, 28/74). 레일 동역학이 채널 하나에 2479 kPa/s 인데 2 ms 마다 재계획하니 뱅뱅 동작 |

그래도 **17.8 vs 채널별 5.3** 이다. 남은 원인을 특정했다:

> **공유 롤아웃 샘플링의 공로 배분 한계.** factored 배분으로 채널 g 의 *비용* 은 분리했지만,
> 채널 g 의 표본들은 자기 제어만 다른 게 아니라 **다른 11채널의 무작위 탐색이 만든 레일
> 궤적까지 다르다.** 공유 상태를 모두가 흔드는데 한 채널의 효과만 분리할 수는 없다.

그 통찰이 10.3 의 설계(롤아웃은 채널별, 레일 궤적은 명목으로 한 번 계산해 공유)를 낳았고,
그것마저 중립이었다.

**남은 문제** (기본 OFF 인 이유): 평균 0.9~1.4 ms 는 예산 안이지만 **최대 15~20 ms 스파이크**가
남아 있다 (틱 2 ms). 힙 할당과 캐시는 고쳤으므로 풀 디스패치/스핀대기 경합이 남은 후보다.

### 10.5 그래서 다음은 압력 계층이 아니다

세 가설을 전부 구현·측정한 결과가 일관되게 "압력 계층에는 남은 여유가 거의 없다" 고 말한다.
조사에서 함께 드러난 구조가 그 이유를 설명한다.

```
angle_ref (정형·램프·저크 제한 없는 생 스텝)
   ↓  TorquePID: PID + 중력FF + 마찰보상. 모델 없음, 프리뷰 없음,
   ↓             축 관성(0.05)·감쇠(0.30)를 모른다 (시뮬은 갖고 있다)
τ_ref → F_ref
   ↓  PressureRefGen (20 ms): **시간축이 아예 없다.** N = 축 수이고
   ↓                          목적함수가 축에 대한 합, 동역학 등식제약 없음.
   ↓                          F_preview 는 실기 경로에서 한 번도 전달되지 않고
   ↓                          Hpreview 는 죽은 필드, x_prev_ 는 대입만 되고 안 읽힌다
P_ref (계단, ZOH 10틱)
   ↓  MPPI (2 ms, 지평 40 ms): 스테이지 레퍼런스를 지수로 **자가 합성**
밸브 듀티
```

**계획하는 계층이 없고, 그 아래 모든 계층이 각자 국소 대용품을 발명했다.** 정착 1.1 s 를
지배하는 시간 스케일에서 모델 기반 최적화를 하는 계층이 하나도 없다.

다음 단계는 그래서 **계획기**다 (`AxisDynamics` placeholder 를 `MppiSystem.hpp` 에 만들어 뒀다):
1. `angle_ref` 궤적 정형 — 축 모델이 **필요 없다** (속도·가속 한계만). 이 액추에이터는
   밀 수만 있고 당길 수 없으므로 상승/하강 한계를 비대칭으로 둔다. 이것만으로도 죽어 있는
   `F_preview`/`Hpreview` 배선이 살아나 MPPI 가 자가 합성 대신 진짜 계획을 받는다.
2. 위치 계층 MPC (지평 300~500 ms) — **축 동역학 실측이 전제**다. 관성·점성감쇠·쿨롱마찰·
   단방향 토크 한계를 재야 한다. 시뮬 값은 실측값이 아니다.

### 10.6 부수적으로 고친 것

- **벽시계 5초 게이트를 틱 기준으로.** `elapsed_time_sec_` 를 `tick_ × period_ms` 로 만들었다.
  전에는 5초 안에 몇 틱이 들어가는지가 실행마다 달라 밸브 잠금 해제와 적분 리셋이 서로
  다른 시점에 풀렸다 (README 0절의 산포 원인 중 하나).
- **틱 간격 진단 신설.** 컨트롤러는 rclcpp 타이머 없이 센서 이벤트로 돌면서 dt 를 항상
  `period_ms` 로 가정한다. 실측은 **2.09 ms**(가정 2.0, +4.3%) 였다 — 원래 보이지 않던
  모델 오차다. 3% 넘게 벌어지면 경고한다. 실기에서 먼저 확인할 것.
- `AcadosMpc::solve` 를 `prepare` / `finish` 로 분리. 중앙집중 경로가 기존의 튜닝된
  피드포워드·적분·크래킹 처리·macro 게이트를 **한 줄도 복제하지 않고** 재사용한다.
- 진단 리포팅 버그: 유효샘플·이상치배율이 활성 틱에서만 누적되는데 전체 틱으로 나눠
  평평 74% 상황에서 실제값의 26% 로 표시됐다.
- `scripts/ctrl_eval.py` 에 레일 압력(평균/최악) 보고 추가. 중앙집중이 라인 밸브를 잡을 때
  불안정하다는 것을 이 지표가 잡아냈다.
