# HANDOFF — 실기 피팅·제어 실험 경과 (2026-08-23 ~ 08-27)

> **이 문서는 실기(6축 파워팩 하드웨어)에서 실제로 돌려 본 결과와 아직 못 고친 것을 적은
> 인수인계 기록이다.** 절차는 [`RUNBOOK.md`](RUNBOOK.md), 배경·이론은 [`README.md`](README.md),
> 채널 MPC 알고리즘은 [`MPPI.md`](MPPI.md).
>
> 브랜치 `feat/mppi-controller`. 이 세션 이전 상태는 "피팅 도구는 다 만들었지만 실기 데이터는
> 하나도 없다" 였고, 이제 **밸브·펌프 실측 피팅 결과가 config 에 들어갔다.** 그런데
> **08-27 갱신: 실기 1축 압력 제어가 돌아간다.** 폭주는 잡혔고 세이프티 래치가 106회 → 14회로
> 줄었다. 다만 추종 오차 RMS 17.9 kPa 로 품질은 아직 목표 미달이다 — 바로 아래 ★ 절이 최신 상태다.

---

# ★ 2026-08-27 세션 — 1축 압력 제어 실기 구동까지

> 아래 2~6절은 **08-23~26 세션의 기록**이고, 이 절이 **가장 최신 상태**다.
> 이 세션에서 폭주(550~766 kPa)를 잡고 실기에서 압력 제어가 실제로 돌기 시작했다.
> 다만 **아직 추종 품질은 목표 미달**이다 — 남은 원인까지 아래에 적었다.

## S-1. 실기 결과 (1축, control_mode 2, MPPI, actuator_connected:=false, 45° 지령)

| 실행 | 양압 레일 | 세이프티 래치 | ch0 압력 범위 | 상태 |
|---|---|---|---|---|
| 17:10 | **100~110 kPa (죽음)** | **106회** | 99.6 ~ 317.0 | 폭주 |
| 17:16 | 100~230 kPa (살아남) | **14회** | 104.0 ~ 189.7 (t=15~27s) | **제어 성립** |

`~/result/20260827_171652/20260827_171652.csv` 가 원본이다.

**t=15~27초 구간 (ch0, 레퍼런스 132.7~185.0 kPa):**
```
|오차| 평균 13.45 kPa,  RMS 17.94,  최대 58.9
```
폭주는 사라졌고 세이프티도 거의 안 걸리지만 **추종 오차는 아직 크다.**
(중간에 몇 개 시점만 보면 ±2 kPa 로 붙는 구간이 있으나 구간 통계는 위와 같다 —
순간값으로 판단하지 말 것.)

## S-2. 폭주를 만든 원인 — 넷을 고쳤다

1. **macro 게이트 임계값** (`macro_gate_frac 0.02`, `macro_micro_sat_pct 100`)
   MATLAB 원본에는 macro 를 "열지 말지" 정하는 임계값이 **없다.** `update_sources` 는
   `mfill = min(요구, 용량); mboost = max(0, 요구 − 용량)` 의 **나눗셈**이고 그 결과를
   실시간 API 는 버린다(`[rail_next, ~] = ...`). 포팅이 이를 비율 게이트로 바꿨는데,
   슬루 박스가 이미 부스트 능력을 포함해 레퍼런스를 만들므로 상승 구간에서는
   `starve > 0` 이 구조적으로 보장돼 2% 를 항상 넘었다. 게다가 게이트가 열리면
   micro·macro **양쪽에 전량**을 요구해 유량이 두 배가 됐고 macro 상류는 800 kPa 탱크다.
   → 임계값 3개를 전부 지우고 MATLAB 과 같은 나눗셈으로 되돌렸다.

2. **밸브 모델이 닫히지 않았다** — 피팅된 `C_p` 가 탐색 상한 `2.0e-3` 에 정확히 걸려
   있었다. 상류압이 한 점뿐인 데이터에서는 `C_p·Pin` 과 `C_k` 가 축퇴해 차이만
   식별되기 때문이다. 그 값을 mode 2 의 레일 SP(351 kPa abs)로 외삽하면 **u=0 에서도
   유효면적이 10.6% 열려 있다.** 챔버가 명령과 무관하게 레일·탱크 압력까지 끌려 올라갔다.
   → 정상폐쇄 불변식(u=0 받침 제거)을 모델에 넣고, 상류압을 바꿔 가며 재피팅했다.

3. **Bouc-Wen 적분 발산** — dI 에 대한 미분방정식인데 오일러 1스텝으로 적분했다.
   실기 피팅값에서 `beta_bw·|dI| = 444.6 × 0.3 = 133 ≫ 1` 이라 z 가 매 틱 ±1e6 로 튀고
   `C_z·z` 가 나머지를 압도해 밸브가 릴레이가 됐다 (u=100 에서도 유효면적 0).
   → 모델은 그대로 두고 수축조건까지 dI 를 쪼개 적분한다.

4. **양압 라인이 열려 있었다 (하드웨어)** — 펌프는 돌아 음압을 21 kPa 까지 뽑는데
   양압 레일이 100~110 kPa(대기압)에 머물렀다. 토출이 그대로 대기로 빠지고 있었다.
   레일이 죽으면 micro 가 무력해져 **잔여가 전부 macro(583 kPa 탱크)로 몰린다** —
   이것이 17:10 실행의 직접 원인이다. 라인을 닫자 17:16 실행에서 제어가 성립했다.
   (08-26 세션의 "900 ms 데드타임" 오진과 **같은 함정**이다 — S-5 참조.)

## S-3. 통신 계층에서 고친 것

| 결함 | 증상 | 조치 |
|---|---|---|
| **CAN 송신 과다** | 우리 TX 750/s 가 보드 수신을 **55% 잡아먹었다** (보드가 500 → 226 Hz). 손실이 ID 순서대로 커져 board 20+ 가 먼저 굶는다 | `can_tx_min_interval_ms`/`can_tx_fallback_ms` = 10 (100 Hz). 실기 확인: 보드 **~400 Hz** 회복 |
| **센서 LPF 정수 절단** | LPF 상태를 uint16 에 되먹여 5 mV 미만 변화가 사라졌다. **상승에서만 −1.0 kPa 영구 편향** (과압 세이프티도 1 kPa 늦게 걸린다) | 상태를 double 로 분리 |
| **CAN 수신 워치독 부재** | 수신이 끊기면 센서가 얼어붙는데 컨트롤러는 살아 있어 PWM 워치독도 안 걸린다 — 얼어붙은 압력을 보며 밸브를 계속 연다 | `can_rx_watchdog_ms` 200. 안전 상태 + 명령 무시 |
| **브리지에 launch 인자 미전달** | `num_actuators:=1` 이 컨트롤러에만 가서 브리지는 엔코더 6개를 기대했다 | 브리지가 선언한 키만 골라 전달 |

## S-4. 남은 문제 (다음 세션이 이어서 할 일)

### S-4-1. ★ 제어 루프가 500 Hz 가 아니라 323 Hz 다
```
틱 간격이 가정과 다르다: 가정 2.0 ms, 실측 평균 3.09 ms (비 1.547)
```
컨트롤러는 **모든 계산에서 dt = period_ms = 2 ms** 를 쓴다. 특히
`advance_valve_estimate` 가 밸브 상태를 실시간 대비 1.55배 느리게 전진시켜 모델이
밸브를 굼뜨다고 보고 과도하게 명령한다. 그 결과 `첫스텝 포화 98.5~99.4%`.
`period_ms=3` 으로 맞추면 실기에서 눈에 띄게 안정적이라는 것이 확인됐다.

### S-4-2. ★ 압력 레퍼런스 자체가 5.7 Hz 로 진동한다
엔코더가 0° 로 고정돼 있고 목표가 45° 고정이므로 **레퍼런스는 상수여야 한다.**
그런데 생성기가 매 20 ms 최적화를 다시 풀면서 계속 바뀐다:
```
ch0 레퍼런스 틱간 변화 평균 2.78 kPa, 최대 43.2 kPa  (≈ 139 kPa/s)
평균선 교차 137회 / 12초 → 약 5.71 Hz
```
원인 후보: 목적함수 J0 가 (P⁺,P⁻) 직선 위에서 축퇴라 매 스텝 다른 점을 고르고,
슬루 박스가 **측정 압력**에 매달려 있어 챔버가 흔들리면 박스도 같이 흔들린다.
**이것이 지금 추종 오차의 주된 원인으로 보인다.**

### S-4-3. MPPI 실행시간 스파이크
평균 700~770 us 인데 **최대 12,780 / 14,288 us** 다 (틱 2 ms). 스레드 우선순위·핀닝
문제로 보인다. 이 스파이크가 틱을 밀어 S-4-1 을 악화시킨다.

### S-4-4. 레일 셋포인트가 펌프 능력을 넘는다
`pos_sp_max_kpa: 250`(gauge) = 351 kPa abs 인데 부하 중 실측 최대가 229.9 다.
도달하지 못하는 목표를 계속 요구하면 LinePID 가 상시 포화한다.

### S-4-5. board 20·21 무응답
버스가 완전히 비어 있을 때도(우리 송신 0) 두 보드만 프레임이 **0** 이다.
나머지 18개는 정확히 500.0 Hz, 에러 프레임 0개. board 22 는 정상이므로 우선순위
문제가 아니다. 전체 ID 스캔에서 예상 밖 ID 도 없었다 → 배선·전원·펌웨어 ID 문제다.
**1축 압력 시험에는 필요 없다** (board 1~4, 5, 11 만 쓴다).

## S-5. 되풀이하지 말 것

- **양압 레일에 공급압이 있는지 먼저 확인할 것.** 이 함정에 두 번 빠졌다
  (08-26 "900 ms 데드타임" 오진, 08-27 17:10 폭주). `board 1` 을 직접 떠서 본다.
- **순간값으로 성능을 판단하지 말 것.** 몇 개 시점만 보면 ±2 kPa 로 보이는데
  구간 통계는 13.45 kPa 였다.
- **`kill` 로 `ros2 run` 을 죽였다고 믿지 말 것.** 래퍼만 죽고 노드는 산다.
  브리지가 중복 실행된 채 측정해 결론이 두 번 뒤집혔다. `pgrep -f` 로 확인할 것.
- **`pkill -f <패턴>` 이 자기 자신을 죽인다** — 명령줄에 그 패턴이 들어 있으면.

## S-6. 새로 생긴 도구

| 도구 | 용도 |
|---|---|
| `scripts/valve_params_install.py` | 피팅 산출 yaml 을 운영 config 로 **안전하게** 병합 (alias·빈 리스트 제거, 밸브 닫힘 검사, `--copy-from/--copy-to` 로 채널 복사) |
| `scripts/valve_params_from_orifice.py` | 하드코딩 기본값(1.6 mm 피팅)을 밸브별 오리피스 면적비로 스케일 |
| `scripts/pack_for_fit.sh` | 피팅 데이터만 골라 묶어 빠른 기계로 이식 |
| CAN 수신 진단 (`can_diag_period_s`) | 보드별 실제 수신 Hz. **0 이면 배선, 낮으면 버스 경합**을 가른다 |
| 제어 보강 3종 (`aug.*`) | 실행 중 켜고 끄는 이득 적응 / 오프셋 프리 / 자동 튜닝. 전부 기본 off |

---

## 1. 한눈에 — 지금 상태

| 영역 | 상태 |
|---|---|
| 밸브 13-parameter (12채널 × 3밸브) | **실측 피팅 완료** → `config/valve_params.yaml`. 기동 로그 `12/12 채널` 확인됨 |
| 펌프 레일 부피·누설 | **실측 완료** → `config/pump_params.yaml` (pos 138.6 mL / neg 29.0 mL) |
| 펌프 실측 능력경계 | **실측 완료 + C++ 반영 경로 신설.** 기동 로그 `실측 능력경계 4 점으로 cap_ppos 덮어씀` |
| 채널 밸브 구동 | **동작한다** (아래 2-1 크래킹 게이트 버그 수정 후) |
| 압력 추종 제어 | **실기에서 돌아간다** (08-27). 오차 RMS 17.9 kPa — 품질은 아직 목표 미달, S-4 참조 |
| 하드웨어 응답 속도 | **빠르다.** 전류 20 ms, 압력 상승 8 ms / 정착 150 ms 실측 (3-4 참조) |
| 엔코더 board 20·21·22 | 캘리브레이션 없음, 기본 gain 부호가 실측 3개와 **반대** (위치 제어 전 필수) |
| gid11 (board16) | **하드웨어 무응답** — 소프트웨어로 못 고침, 물리 점검 필요 |

---

## 2. 이 세션에서 찾아 고친 결함

### 2-1. 크래킹 게이트가 채널 밸브를 영구히 닫아 놨다 ★ 가장 중요

`AcadosMpc::finish()` 에 이 브랜치에서 추가돼 있던 코드:

```cpp
auto taper_above_crack = [tp](float u, float u_c) {
  return (u <= u_c) ? 0.0f : u_c + (u - u_c) * tp;   // ← (u <= u_c) 분기가 문제
};
```

`taper_` 는 오차가 크면 1.0 이라 **아무것도 깎지 않아야** 하는데, 그 앞의 조건분기가
`u ≤ u_crack` 이면 무조건 0 으로 떨어뜨린다. 결과: **레퍼런스는 정상 수신되고 레일 PID 도 정상
동작하는데 채널 밸브 PWM 만 계속 0**. `board/pwm_cmd` 를 직접 떠서 확인했다 —
`board5 v1/v2/v3 = [0,0,0]` 인데 `board1 v1 = 39`(레일은 살아 있음).

`git show master:src/can_powerpack/src/Controller.cpp` 로 확인한 결과 **master(위치 제어가
됐던 버전)에는 이 게이트가 아예 없다** — `u0 = clamp(uref+du, 0, 100)` 을 그대로 낸다.
master 방식으로 되돌렸고, 그 뒤 밸브가 정상 반응한다.

> `u_crack_` 자체는 계속 계산된다(진단·향후용). 게이트만 없앴다.

### 2-2. `valve_params.yaml` 이 두 노드를 죽였다

생성 스크립트가 "가장 잘된 채널 결과를 전체에 복사"할 때 파이썬에서 **같은 dict 객체를 참조로
재사용**해서, PyYAML 이 anchor/alias(`&id001` / `*id001`)로 압축해 저장했다.
ROS 2 의 `rcl_yaml_param_parser` 는 alias 를 지원하지 않는다:

```
Couldn't parse params file: '.../valve_params.yaml'. Error: Will not support aliasing at line 139
→ can_bridge_node, pp_controller 둘 다 exit code -6 (SIGABRT)
```

alias 를 없애자 다음 문제가 나왔다 — `_fit.weak_params: []`(빈 리스트)는 타입을 추론할 수 없어
**노드 생성 중**(사용자 코드 실행 전) `InvalidParameterValueException: No parameter value set`.

`_fit` 은 사람이 보는 진단 메타데이터(mode / r2 / sse / weak_params)일 뿐 C++ 이 읽지 않으므로
운영 config 에서는 통째로 제거했다. 피팅 리포트(`results_fit/*/report.md`)에는 그대로 남아 있다.

> **교훈: config 로 나가는 yaml 에 빈 리스트·anchor/alias 를 절대 넣지 말 것.**

### 2-3. 실측 능력경계가 컨트롤러에 도달하지 않았다

`pump_fit_solve.py` 는 `pump_frontier_measured`(Phase F 직접 측정)를 yaml 에 쓰는데,
**C++ 이 그 키를 읽는 코드가 없었다.** `PressureRefGen` 은 기동 시 `pump.*` 기하값으로
슬라이더-크랭크를 시뮬레이션해 cap 테이블을 스스로 만든다 — 즉 신뢰도 낮은 기하 피팅만
반영되고, 신뢰도 높은 실측은 무시되고 있었다.

신설한 경로: `PumpTable::override_cap_measured()` → `PressureRefGen::apply_measured_frontier()`
→ `Controller.cpp` 가 `build_pump_table()` **직후** 파라미터를 읽어 적용.
측정 구간 안은 실측으로 덮어쓰고 **밖은 기하 외삽을 그대로 둔다.** 헤더 단독 테스트로 검증:

```
-65.4 kPa → 334.7 (실측 2점 평균)   -63.8 → 359.5   -36.0 → 385.8   (정확)
-50.0 kPa → 372.6 (측정 두 점 사이 선형보간)
-90 / -80 / -20 / 0 kPa → 오버라이드 전과 동일 (측정범위 밖)
빈 벡터 / 길이불일치 → 무변화
```

기존 하드코딩은 **1200 kPa** 였다 — 실측(335~386)의 3~4배 과대평가.

### 2-4. 피팅 스크립트 결함들

| 파일 | 결함 | 수정 |
|---|---|---|
| `pump_fit_solve.py` | neg 레일 "맨몸" 감쇠로 `leak_none0.csv` 만 봐서, 그 회차 R²=0.71 로 기각되면 부피를 못 구하고 죽었다. `leak_pos100.csv`(양압에만 ΔV) 회차의 neg 감쇠는 R²=0.97 로 정상이고 **neg 부피는 안 건드렸으니 물리적으로 맨몸과 동등**한데 태그만 보고 무시 | "이 레일에 ΔV 가 안 들어간 모든 회차"를 맨몸 후보로 |
| `valve_fit_record.py` | 종료 시 SIGABRT (`destroy_node()` 가 spinner 의 `flush()` 와 경합) | `_shutting_down` Event + `shutdown()→join()→destroy_node()` 순서 |
| `valve_fit_record.py` | 트립 후 `to_neutral()`/`charge_full()` 이 안전벤트를 **다시 닫아** 압력이 계속 상승 (실기 재현: 둘 다 닫힌 채 190→197 kPa) | `wait_hold()` 반환값이 `'tripped'` 면 즉시 return |
| `valve_fit_record.py` | 200 Hz 재발행이 CAN 버스를 점유해 ID 높은 보드(14+)로 가는 새 명령이 우선순위 경합에서 밀렸다 | `keepalive_flush()` + `--flush-hz`(기본 20) 로 재발행만 감속. 실제 상태 변화는 여전히 즉시 발행 |
| `valve_fit_solve.py` | 단일 `BASE_INITIAL` 에서만 시작해 나쁜 국소최적에 갇힘 | `--warm-start-yaml` + `random_search(base=[...])` 다중 base |
| `valve_fit_solve.py` | dP/dt 순간 글리치가 피팅을 오염 | `reject_glitches()` (MAD 기반). **단, 글리치 마스크를 `full['mask']` 에 섞으면 이중부피법이 깨진다**(부피오차 2.9%→18.4% 회귀 확인) → 13-parameter 피팅용 `fit_mask` 에만 적용 |
| `pump_fit_record.py` | 같은 SIGABRT | 동일 수정 |

### 2-5. 센서 재영점

board7(gid2 양압 ch2) offset `1089.0 → 1054.0` (사용자 재측정). `powerpack_config.yaml` 과
`scripts/can_monitor.py` 양쪽에 반영.

---

## 3. 미해결 — 다음 세션이 이어서 할 일

### 3-1. ★ 압력 추종이 안 된다 (뱅뱅/진동)

`solver:=mppi num_actuators:=1 actuator_connected:=false`, 45° 지령, 레일·매크로 공급 정상.
ch0 실측 압력이 이렇게 왕복한다:

```
t=+1s  100.6      t=+4s  553.6      t=+7s  101.1
t=+2s  101.1      t=+5s  555.8      t=+8s  101.1
t=+3s  100.3      t=+6s  524.8
```

과압 세이프티(190 kPa, 해제 175)가 반복 래치돼 벤트를 전개하고, 내려가면 다시 채운다.
목표는 178 kPa 인데 550 kPa 까지 올라가는 게 문제의 핵심 — **채워 넣는 경로가 너무 세다.**

### 3-2. macro(탱크 부스트)가 1축 저부하에서도 계속 열린다

사용자 지적: *"1축만 하고 있으니 매크로 사용이 필요할 리가 없다."* 맞다. 그런데
**macro 를 여는 독립 게이트가 두 개** 있고, 둘 중 하나만 막아도 다른 쪽이 연다:

| 게이트 | 기본값 | 위치 | 근거 |
|---|---|---|---|
| `PressureRefGen.macro_gate_frac` | **0.02** (2%!) | `Controller.cpp:2685` | 생성기의 축별 유량 부족률 `starve_pos > frac` |
| `MPC_parameters.macro_micro_sat_pct` | **100** | `Controller.cpp:496` | `u_micro_req >= sat_pct` (micro 포화) |

```cpp
auto macro_open = [this](float u_micro_req) {
  return macro_allow_.load(...) || u_micro_req >= cfg_.macro_micro_sat_pct;
};
```

`macro_gate_frac=1.1` 만 준 실험에서는 `starve 0/0% [--]` 로 게이트가 닫혔고
`board5 v3 = 0` 도 확인됐지만, 그때 탱크가 `LOW`(106~240 kPa)로 빠져 있어서
"게이트가 닫혀서 조용했던 것"과 "탱크가 비어서 힘을 못 썼던 것"이 구분되지 않았다.
탱크를 교체하고 다시 하니 다시 550 kPa 로 폭주했다.

**다음 실험 (미완 — 사용자가 중단시킴):** 두 게이트를 동시에 끄고 micro 단독으로 추종되는지 본다.

```bash
ros2 launch can_powerpack control.launch.py \
  solver:=mppi num_actuators:=1 actuator_connected:=false \
  overrides:=PressureRefGen.macro_gate_frac=1.1,MPC_parameters.macro_micro_sat_pct=1000
```

여기서 조용해지면 원인은 macro 게이트 정책이고, 여전히 폭주하면 micro 피드포워드
역모델(`compute_input_reference`)이나 13-parameter 자체를 의심해야 한다.

`macro_gate_frac` 기본값 0.02 는 재검토 대상이다 — 생성기가 이미 슬루박스 제약 안에서 최적해를
내는데, 별도 진단식(`mboost = dup − mfill_cap`)이 계산 방식이 달라 2% 를 너무 쉽게 넘긴다.

### 3-3. QP 솔버가 `control_mode=0` 에서 거의 100% 실패한다

```
[AcadosMpc] gid=0 QP: hot-start 실패 0.0% (cold 로 복구), 완전 실패 100.00% / 5000 호출
```

`control_mode=2` 에서는 같은 QP 가 ~4% 만 실패한다. `control_mode=0` 경로 전용 문제로 보인다.
아직 원인 미확인 (사용자 지시로 MPPI 우선 진행).

### 3-4. `control_mode=0`(압력 직접 지정)은 지금 구조상 제어가 안 된다

`RefTcpServer` 는 `control_mode != 1,2` 일 때 `[pos_kpa, neg_kpa]` 를 받아 `mpc_ref_kpa_` 에
직접 넣는다(→ 새로 만든 `scripts/pressure_ref_client.py`). 그런데 **레일 셋포인트를
갱신하는 코드가 `control_mode 2` 전용**이다:

```cpp
// Controller.cpp:2692 — run_optimized_pressure_ref() 안, 즉 mode 2 에서만 실행된다
pid_pos_.ref = gen_rail_pos_sp_kpa_;
pid_neg_.ref = gen_rail_neg_sp_kpa_;
```

mode 0 에서는 `LinePID.pos.ref` / `neg.ref` 가 yaml 고정값(**150 / 20 kPa**)에 박힌 채
채널 목표와 무관하게 돌아 진동한다. mode 0 을 쓰려면 목표 압력에서 레일 SP 를 유도해
`LinePID` 에 넘기는 코드가 필요하다.

### 3-5. "PID 모드 900 ms 데드타임"은 오진이었다 — 하드웨어는 빠르다

세션 초반에 `current_mode/control_type` 을 `0,0`(Normal/PID) → `1,1`(Debug/PWM) 로 바꾸고
**그 모드에서 밸브 13-parameter 전체를 피팅했다.** 이유는 "PID 모드에서 v1 이 ~900 ms
데드타임을 보인다" 였다.

사용자 요청으로 `can_control.py` 방식(ROS 없이 CAN 직접)으로 재측정한 결과 — Normal(PID) 모드,
board5 v3 에 4095 스텝:

```
 t[s]      I3[mA]     P[kPa]
0.536        0.0     102.35     ← 스텝 직전
0.538       32.6     101.86     ← 전류 상승 시작 (2 ms)
0.546      182.0     117.98     ← 압력 상승 시작 (8 ms)
0.558      241.7     255.46
0.600      249.8     514.56
0.688      250.7     590.51     ← 정착 (~150 ms)
```

**전류 20 ms 정착, 압력 8 ms 반응 / 150 ms 정착. 900 ms 데드타임은 없다.**
당시 "데드타임"으로 보였던 것은 **양압 레일에 공급압이 없었기 때문**일 가능성이 높다 —
이 세션에서 board1 을 직접 재보니 101.2 kPa(완전 대기압)였고, v1(micro)에 250 mA 를 1.2 초
넣어도 압력이 전혀 안 움직였다. v3(macro, 669 kPa 탱크)로 옮겨 붙이자 위처럼 정상 응답했다.

그래서 `current_mode/control_type` 을 **master 기본값 `0,0`(Normal/PID)로 되돌렸다.**
다만 **`valve_params.yaml` 은 Debug 모드에서 피팅한 값이다** — 두 모드의 크래킹·응답 특성이
다르면 Normal 모드에서 재피팅이 필요할 수 있다. 이게 3-1 폭주의 원인 후보이기도 하다.

### 3-6. 양압만 쓰고 음압은 대기압에 머문다 (설계 의도 — 유지 결정)

`PressureRefGen::objective()` 의 `J0`(힘 추종)은 `F = P⁺·A − P⁻·A` 만 보므로, 목표 F 를
만족하는 (P⁺,P⁻) 조합이 **직선 하나 전체**다 — J0 만으로는 어느 쪽을 쓸지 정해지지 않는다.
실제로 한쪽을 고르는 건 `J2`(가용유량 최대화)이고, 지금 레일 조건에서는 그게 항상 양압이다.

τ ≤ 4.11 N·m(= P⁺ 단독 최대) 구간에서는 P⁻ 가 아예 안 움직이고, 그 위(110~140° 지령,
τ 4.7~6.9)에서는 P⁻ 가 47~89 kPa 까지 실제로 내려간다. **버그가 아니라 원본 MATLAB 포팅
그대로의 자기균형 인수인계 설계다.** 사용자 확인 후 원설계 유지로 결정했다
(균형 사용을 원하면 목적함수에 분배 항을 추가해야 한다).

### 3-7. 그 외 남은 것

- **엔코더 board 20·21·22 캘리브레이션 없음.** 기본 gain 이 실측 3개(모두 음수)와 부호가
  반대라 그 축은 각도가 거꾸로 읽힌다. 위치 제어(mode 2) 전 필수. `scripts/encoder_calib.py`, RUNBOOK 0.5절
- **gid11(board16) 하드웨어 무응답.** `--flush-hz` 로 board14·15 는 개선됐지만 board16 은
  버스가 한가할 때도 무반응 — 배선/탱크 물리 점검 필요
- **`ORIFICE_MM[('pos_micro','v2')]` 가 4.0 으로 하드코딩.** 실제 2.3 mm (사용자 확인).
  `valve_fit_solve.py` 에서 수정해야 하나 아직 안 했다
- **음압 피팅 R² 가 낮다** (최선 0.378). 사용자가 제안한 대안(라인에 100 mL 고정탱크 +
  채널에 170 kPa 레귤레이터)은 `--fixed-volume-side line` 로 **구현·회귀검증까지 됐지만**
  양압 센서 문제로 실측은 보류 상태
- **`results_fit/` · `results_pump/` 는 `.gitignore` 대상** — 원본 CSV 와 피팅 리포트는
  이 기계 로컬에만 있다. 클론한 쪽에서는 재현할 수 없으니 필요하면 따로 받을 것

---

## 4. 실측 피팅 결과 요약

### 밸브 (`config/valve_params.yaml`, 12채널 × micro/atm/macro)

| 채널 | 밸브 구성 | 출처 |
|---|---|---|
| ch0–5 (양압) | v1 fill 2.3 mm, v2 vent 2.3 mm, v3 boost 1.6 mm | pos_micro 채널별 개선 피팅 + pos_macro 는 gid1(board6) 결과 R²=0.7343 을 전체 적용 |
| ch6–11 (음압) | v1/v2/v3 전부 **같은 4.0 mm 밸브** | gid9 v1 결과 R²=0.378 을 micro/atm/macro 에 동일 적용 |

13-parameter 는 과매개변수(flat manifold)라 **개별 파라미터는 신뢰하지 말 것.**
믿을 수 있는 건 R² / 크래킹 임계 / 챔버 부피뿐이다 (RUNBOOK 자체 판정 기준).

오리피스 지름과 파라미터 사이에 뚜렷한 경향은 **없었다** — 밸브의 역할(fill/vent/boost)이
지름보다 지배적이었다.

### 펌프 (`config/pump_params.yaml`)

| 항목 | 값 | 신뢰도 |
|---|---|---|
| 레일 부피 pos / neg | 138.6 / 29.0 mL | 이중부피법 실측. 단 neg 의 "맨몸"은 ΔV→pos 회차 대체값(2-4 참조) |
| 레일 누설 pos / neg | 0.00108 / 0.00005 LPM/kPa | 위와 동일 |
| 실측 능력경계 | P⁻ −65.4→P⁺ 334.5 / −63.8→359.5 / −36.0→385.8 | **직접 측정. 컨트롤러가 쓰는 유일한 출력** |
| 기하 (`pump.*`) | 소기량 456.65 mL, 압축비 22.0 | **낮음.** 소기량×Cb_in 축퇴, 데드헤드는 외삽 |

Phase M 은 40점 확보했지만 **양 레일 교차검증(질량보존) 가능한 점이 0개** — pulse 구간에서
admit 이 완전히 안 닫혔을 가능성. 진행에는 지장 없지만 교차검증은 못 했다.

---

## 5. 지금 상태로 다시 시작하는 방법

```bash
colcon build --packages-select can_powerpack --cmake-args -DCMAKE_BUILD_TYPE=Release
source install/setup.bash

# 무액추에이터 1축, MPPI (RUNBOOK 4.5.2 의 가장 안전한 시작점)
ros2 launch can_powerpack control.launch.py \
     solver:=mppi num_actuators:=1 actuator_connected:=false

# 각도 지령 (control_mode 2 = 각도 → 토크 → 압력. 압력을 직접 주는 게 아니다)
python3 src/can_powerpack/scripts/position_ref_client.py 127.0.0.1 2293 --once 45
```

기동 로그에서 반드시 확인할 두 줄:

```
밸브별 13-parameter: 2/2 채널이 chN.{micro,atm,macro}.* 를 로드했다
PressureRefGen: 실측 능력경계 4 점으로 cap_ppos 덮어씀 (Phase F)
```

**실측 압력은 `[RefGen]` 로그로 보지 말 것** — 그건 생성기가 계산한 *레퍼런스*다.
실측은 토픽을 직접 떠야 한다:

```bash
ros2 topic echo --once --flow-style /pack2/board/sensors std_msgs/msg/UInt16MultiArray
# index 0=board1(레일 P+), 2=board3(탱크), 4=board5(ch0), 10=board11(ch6)
# P = (orig_mV − offset) × gain + 101.325,  orig_mV = (4125 − adc_mV)/0.825
```

밸브 지령이 실제로 나가는지는 `/pack2/board/pwm_cmd`(index `(board−1)*3 + {0,1,2}` = v1/v2/v3).

### 실험 끝나면 반드시 프로세스를 내릴 것

이 세션에서 여러 번 실수했다 — launch 를 백그라운드로 띄워 놓고 안 죽여서, 사용자가
"코드를 다 껐는데도 파워팩이 디버그 신호를 받는다 / `can_control.py` 가 안 먹는다" 상황이
반복됐다. CAN 버스를 물고 있으면 수동 도구가 경합에서 밀린다.

```bash
pgrep -af "control.launch.py|lib/can_powerpack/"   # 남은 게 없어야 한다
```

---

## 6. 안전 관련 — 하드코딩된 값들

| 항목 | 값 | 위치 |
|---|---|---|
| 과압 트립 / 해제 | 190 / 175 kPa | `pressure_safety_limit_kpa_`, 래치되면 v2 전개 |
| 채널 정격 | P⁺ ≤ 185 kPa, P⁻ ≥ 27 kPa | `PositionController.axis*.p_pos_max_kpa` 등 |
| PWM 워치독 | 200 ms (실기만, 가상 없음) | 타임아웃 시 채널 폐쇄 + 라인 밸브(idx 0, 3) 전개 |

**펌프 실험(RUNBOOK 2절)과 밸브 실험의 안전 상태가 정반대다** — 펌프가 도는 동안은
양 라인 밸브 **전개**가 안전 상태다. "전 밸브 0" 은 릴리프를 닫는 것이라 양압이 무한정 오른다.

---

## S-7. 8.3 Hz 진동의 근본 원인과 해결 (2026-08-27, 실기 20260827_195422)

### 계측으로 확정한 것

`pp_logger` 를 schema 2 로 올려 **밸브 지령(pwm)과 실측 전류(current)** 를 처음으로
같이 기록했다. 그 전 68건의 결과에는 이 두 열이 없어서 원인을 좁힐 수 없었다.

실기 56.5 초 (양압 ch0, board5):

| 항목 | 값 |
|---|---|
| 챔버 진동 | **8.27 Hz**, peak-to-peak **96.5 kPa** |
| 추종 오차 | 절대평균 19.5 / RMS 28.2 kPa |
| 밸브 개폐 전환 | v1 8.69 Hz, v2 8.47 Hz |
| 지령 변화율 | 평균 1198 %/s, **최대 10000 %/s** (한 틱에 0↔100) |
| 지령이 0.3 초 이상 유지된 구간 | **0건** |
| 한 틱(10 ms) 압력 변화 | 중앙 5.81, 95분위 19.01, 최대 82.3 kPa |

전류가 흐를 때의 챔버 응답 (배기 닫힘·라인압>챔버+20 kPa):

| v1 전류 | dP/dt 중앙값 |
|---|---|
| 0~20 mA | +0.4 kPa/s |
| 120~140 mA | +942 kPa/s |
| 140~160 mA | **+1180 kPa/s** |

### 원인 — alpha_shape 폭주

`alpha_shape = 3884.2` 는 시그모이드에 3884 승을 붙여 밸브를 **계단**으로 만들었다.
역모델의 비례대역이 이렇게 눌렸다:

| 요구 dP/dt | 옛 모델이 내는 지령 |
|---|---|
| 10 kPa/s | 64.5% |
| 100 kPa/s | 68.0% |
| 1000 kPa/s | 73.3% |

즉 쓸 수 있는 전 구간이 **3.5%p**. `mppi_sigma_pct = 8.0` 은 그 창의 2.3 배라
MPPI 가 창 안에 착지할 수 없었다. 명령은 계단 양쪽을 왕복했다.

같은 로그에서 유효면적을 역산하면 (배기 닫힘 구간, 챔버 50 mL):

```
v1 micro : 42.5% → 0.0248   47.5% → 0.0319   52.5% → 0.0325
v2 atm   :  2.5% → 0.0      42.5% → 0.0303   47.5% → 0.0436   57.5% → 0.0416
```

같은 지점에서 모델이 예측한 면적은 **0.0000 (언더플로)**. 실기와 모델이 무관했다.

> 면적 절대값은 챔버 부피 가정(50 mL)에 비례한다. 그러나 컨트롤러도 요구 유량을
> **같은 부피**로 계산하므로 (`Controller.cpp:541`) 부피 오차는 폐루프에서 상쇄된다.
> 절대 LPM 이 아니라 비율이 결정한다.

### 고친 것

1. **`cmd_lpf_hz` 가 죽은 코드였다.** 설정에서 읽어 구조체에 넣고 MPPI 로 넘기지만
   `u_lpf_` 사용처가 `src/` 안에 0곳이었다. `AcadosMpc::finish()` 에 실제로 구현했다.
   1차 저역통과는 DC 이득이 1 이라 정상상태 오차를 만들지 않는다 — 느려질 뿐이다.

2. **밸브 곡선 재보정** (`scripts/valve_params_calibrate.py`).
   `alpha_shape` 를 1 로 되돌리고 두 점으로 비례대역을 다시 세웠다:
   20 mA(6.7%) 에서 σ=0.002, 55% 에서 σ=0.95 → `k_shape = 63.1956`.
   비례대역 **3.5%p → 23.2%p**. `A_max` 는 실측 포화값 (pos micro 0.033, atm 0.044,
   macro 는 기존 비율 유지, neg 는 같은 배율).

3. **`C_k` 를 밸브별 상류압 기준으로 분리.** `C_p·Pin` 항이 상류압에 비례하는데
   macro 는 상류가 탱크 800 kPa 라 `C_p·Pin = 0.096` 으로 `C_k` 의 68% 를 먹었다.
   전 밸브 공통 `C_k` 를 쓰면 macro 가 지령 15% 에서 이미 반개가 되고, 시뮬에서
   MPPI 가 macro 를 4~8% 만 열어도 챔버가 199 kPa 로 치솟아 과압 세이프티가
   배기를 100% 로 래치했다 (3 Hz 리밋사이클). 각 밸브가 **자기 동작점에서**
   39.5% 에 반개가 되도록 따로 잡았다.

4. **MPPI 노이즈를 새 비례대역에 맞췄다.**
   `mppi_sigma_pct` 8.0→3.0, `mppi_sigma_explore_pct` 30→10, `mppi_du_limit_pct` 100→12.

5. **시뮬 플랜트가 피팅값을 한 번도 안 쓰고 있었다.** `VirtualPowerpack` 은 밸브
   파라미터를 **채널당 하나**(`channel_config.chN.<키>`) 로 읽는데 `valve_params.yaml`
   은 밸브별(`chN.micro/atm/macro.<키>`) 이고, 게다가 `/pack2/pp_controller` 아래라
   런치의 `can_bridge` 섹션 로더에 잡히지 않았다. 플랜트는 코드 기본값
   (A_max 0.2845, alpha_shape 3884.2) 으로 돌았다 — **컨트롤러가 보는 밸브와 시뮬
   안의 밸브가 서로 다른 물건이었다.** 플랜트를 `[gid][micro/atm/macro]` 로 분리하고
   런치가 같은 파일을 넘기게 했다.

6. **밸브 권한 검증이 alpha=1 에서 오탐**했다. `F_open`(언더플로 지점) 기준은
   alpha 가 클 때만 뜻이 있어서 12채널×3밸브 전부 ERROR 를 뱉었다. "최대 상류압에서
   u=0 받침이 A_max 의 몇 %인가" 로 바꿨다 (경고 임계 1%).

### 결과 (시뮬, 4단 스텝 101→109→136→164 kPa)

| | 고치기 전 (실기) | 고친 후 (시뮬) |
|---|---|---|
| 진동 | 8.27 Hz, p-p 96.5 kPa | 0.34 Hz(스텝 응답), 정상상태 p-p 0.25~0.50 kPa |
| 밸브 개폐 전환 | 8.69 Hz | **0.05 Hz** |
| 지령 변화율 최대 | 10000 %/s | **549 %/s** |
| 오버슛 | — | **+0.4 ~ +0.5 kPa** |
| 정착시간(±1 kPa) | 정착 안 함 | **0.34 ~ 0.43 s** |
| 정상상태 오차 | RMS 28.2 kPa | **+0.00 ~ +0.02 kPa** |

### 남은 것 / 주의

- **실기 검증이 아직이다.** 위 수치는 전부 시뮬이고, 시뮬 플랜트는 이번에 보정한
  바로 그 파라미터를 쓰므로 모델–플랜트가 일치한다. 실기에서 다시 재야 한다.
- **비례대역 가운데를 실측한 적이 없다.** 20~120 mA 구간은 컨트롤러가 한 번도
  머문 적이 없어 표본이 없다. 새 곡선의 중간부는 두 점 사이의 **설계된 보간**이지
  피팅이 아니다. 일부러 완만하게 잡았다 — 틀려도 느려질 뿐이라서다.
- 적분기는 **오차 부호가 바뀔 때마다 0 으로 리셋**된다 (`Controller.cpp:547`).
  8 Hz 진동에서는 초당 16번 지워져 무력했다. 진동이 잡히면 살아난다.
- 음압 채널은 직접 계측이 없어 양압 micro 의 보정 배율을 그대로 적용했다.
- 되돌리려면 `config/valve_params.yaml.pre_cal_20260827` 이 있다.

---

## S-8. 밸브가 안 열려 추종 실패 → 실측 재적합 + 적분항 (2026-08-28, 실기 20260828_113700)

### 증상

S-7 보정 후 진동은 사라졌지만 챔버가 레퍼런스를 20~33 kPa 아래로 놓쳤다.

```
t=6~26s  지령 v1 = 21~28%, 전류 56~75 mA, 라인압 250~325 kPa
         → 챔버 100.1 kPa 고정 (레퍼런스 121 / 133)
오차 절대평균 16.51 kPa, 최대 33.7
```

### 원인 1 — 보간 구간이 틀렸다

S-7 에서는 20~120 mA 에 표본이 아예 없어 "20 mA 닫힘 / 55% 전개" 두 점으로
**완만하게 보간**했다 (k_shape 63.2, 비례대역 23.2%p). 이번 실행이 그 공백을
메웠고, 보간이 틀렸음이 드러났다:

| 전류 (지령) | n | dP/dt 중앙 | 유효면적 |
|---|---|---|---|
| 20~30 mA (8.3%) | 770 | +0.2 | 0.000006 |
| 50~60 mA (18.3%) | **1407** | −0.4 | 0.000000 |
| 70~80 mA (25.0%) | 151 | −0.4 | 0.000000 |
| 80~90 mA (28.3%) | 120 | −0.3 | 0.000000 |

**90 mA(30%)까지 완전히 닫혀 있다.** S-7 곡선은 25% 에서 면적 0.0037(≈140 kPa/s)
을 예측했다. 그래서 컨트롤러가 25% 면 충분하다고 믿고 멈춰 섰다.

이전 실행의 개방 구간(42.5%→0.0248, 47.5%→0.0319, 52.5%→0.0325)과 합쳐 12점
최소자승으로 다시 적합했다 (전 점 ±0.8% 재현):

```
A_max = 0.032259   k_shape = 283.6416   C_k = 0.14604655   alpha_shape = 1.0
@190 kPa  반개 41.1%   비례대역 38.5~43.7% = 5.2%p
```

**비례대역은 5.2%p 다.** 넓히고 싶어도 실기가 그렇지 않다 — 이 밸브는 사실상
온/오프고, S-7 의 23.2%p 는 데이터가 아니라 내 보간이었다.

### 원인 2 — 적분 이득이 전부 0 이었다

`pos_ki_micro/macro/atm`, `neg_ki_*` 가 모두 `0.0`. 모델이 조금이라도 빗나가면
정상상태 오차가 영원히 남는 구조였다. micro/atm 0.15, macro 0.05 로 켰다.

### 고친 것

1. **곡선 재적합** (위). `valve_params_calibrate.py` 에 12점 적합 결과를 넣었다.
2. **적분항 활성화** + **기여분 상한** `ki_u_limit_pct = 10`.
   크래킹 아래에서 유량이 정확히 0 이라 적분기는 반응 없이 쌓이다가 밸브가
   열리는 순간 700 kPa/s 로 덤핑한다. 적분기의 역할은 모델 잔차(≈수 %p)를
   메우는 것뿐이므로 그만큼만 허용한다. (u≥100 에서만 멈추던 기존 anti-windup 은
   크래킹 아래에서 절대 걸리지 않는다.)
3. **명령 LPF 를 비대칭으로.** 대칭 필터는 닫는 것도 늦춘다. 밸브가 열리면
   챔버가 700 kPa/s 로 차오르는데 2 Hz 대칭 필터는 닫는 데 τ=80 ms 를 걸어
   그동안 ≈57 kPa 를 더 밀어 넣는다 — 진동을 막으려던 필터가 오버슛을 만든다.
   **여는 쪽만 완만히, 닫는 쪽은 즉시** 로 바꿨다.
4. **충전 밸브에 뻑뻑함 여유폭 +1.5%p** (`STIFF_MARGIN_PCT`). 아래 강건성 참조.
5. MPPI 탐색을 5.2%p 대역에 맞췄다: sigma 3.0→1.5, explore 10→5, du_limit 12→8.

### 강건성 — 실측한 비대칭

플랜트만 모델 대비 옮겨 가며 45° 스텝을 준 결과:

| 실제 밸브가 모델 대비 | 최종 오차 | p-p | 판정 |
|---|---|---|---|
| −2.9%p (헐거움) | +0.12 | 2.5 | 정상 |
| 정합 | −0.02 | 0.2 | 정상 |
| +1.5%p (뻑뻑) | +0.02 | 0.5 | 정상 |
| **+3.0%p (뻑뻑)** | +1.99 | **65.6** | **붕괴** |

**위험한 방향은 하나뿐이다: 모델은 유량이 있다고 믿는데 실제로는 0 일 때.**
그러면 피드백이 아예 없어 지령이 대역을 한참 지나칠 때까지 램프업하고,
크래킹을 넘는 순간 한꺼번에 터진다. 2026-08-28 실기의 "25%/75 mA 로 20 초"가
정확히 이 상태였다. 그래서 모델을 일부러 조금 뻑뻑하게 잡는다.

여유폭을 더 키우면 이번엔 배기 밸브가 과배기해 정상상태가 내려앉는다
(+6.8%p 시험에서 −14 kPa). 그래서 **상류가 고압인 충전 밸브(pos micro/macro)
에만** 걸고 배기에는 걸지 않는다.

### 물리적 한계 (튜닝으로 못 넘는 것)

밸브가 열리면 챔버가 ~700 kPa/s 로 찬다. 밸브 폐쇄에 ≈60 ms (wn 40 rad/s) 가
걸리므로, **밸브가 크게 열린 뒤에는 지령을 0 으로 내려도 40 kPa 가 더 들어간다.**
비례대역이 5.2%p 뿐이라 "조금만 열기" 의 여유가 거의 없다. 제어의 요체는
**크래킹 근처를 벗어나지 않는 것**이고, 그래서 모델 오차가 대역폭의 절반을
넘으면 리밋사이클이 된다. 실기가 그 안에 드는지는 실기로만 확인된다.

### 결과 (시뮬 4단 스텝 101→108→136→163 kPa, 모델=플랜트)

| | S-7 실기 | 지금 |
|---|---|---|
| 오버슛 | 추종 실패 | **+0.0 ~ +0.4 kPa** |
| 정착시간(±1 kPa) | — | **0.47 ~ 0.54 s** |
| 정상상태 오차 | 절대평균 16.51 kPa | **−0.01 ~ +0.03 kPa** |
| 최대압 vs 최대 ref | — | 163.3 vs 163.0 |

---

## S-9. 내려오지 못하던 문제 — MPPI 가 적분항을 되돌리고 있었다 (2026-08-28, 실기 20260828_151518)

### 증상

올라가는 쪽은 잘 됐다 (117.7 → 오차 −0.1, 143.5 → +0.2). 내려오지를 못했다:

```
t=39.7  ref 127.2  act 143.5  err +16.4   v2atm 37.6%  I2 93.9 mA
t=50.5  ref 127.1  act 143.2  err +16.1   v2atm 38.1%  I2 96.4 mA
```

배기 지령이 **38.1% / 96 mA 에 11 초간 고정**된 채 유량이 0 이었다.

### 실측한 배기 밸브 특성 (두 실행 합산)

| 지령 | 전류 | n | 유효면적 |
|---|---|---|---|
| 0% | 0~15 mA | 551 | ≈0 |
| 10.9~22.1% | 15~60 mA | 262 | ≤ 0.00024 |
| **38.1%** | **90~105 mA** | **1199** | **0.000000** |
| ~45% | 135~150 mA | 67 | 0.044428 |
| 47.1% | 150~165 mA | 215 | 0.042813 |

배기 크래킹은 **38.1~45% 사이**다. 모델의 10% 개방점은 (Pin=143.5 에서) 37.9%
라 대략 맞다 — 즉 곡선 자체가 원인이 아니었다.

### 원인 — MPPI 가 적분항을 되돌린다

피드포워드가 36.1% 를 요구하고 적분항이 +10 을 얹으면 46.1% 라 열려야 했다.
실제 지령은 38.1% 였다. **MPPI 가 −8 을 빼고 있었다.**

적분항이 `uref` 안에 있으면 MPPI 는 그것을 포함한 값을 시작점으로 잡고 **같은
밸브 모델로 재최적화**한다. 적분항이 모델이 요구하는 것보다 밸브를 더 여는
순간 MPPI 의 예측은 "그러면 넘친다" 가 되어 du 로 그만큼 빼 버린다. 적분항이
메우려는 것이 바로 그 모델의 오차인데, 같은 모델에게 심판을 맡기면 영원히
못 메운다.

### 고친 것

적분 보정을 `u_trim_[3]` 으로 분리해 **MPPI 뒤, 명령 LPF 앞**에서 더한다
(`AcadosMpc::finish()`). anti-windup 판정에는 트림을 포함시킨다.

### 결과 — 두 방향 모두 강건해졌다

충전 밸브가 적합값보다 뻑뻑할 때 (45° 스텝):

| 실제가 모델보다 | 적분항이 uref 안 (S-8) | **MPPI 뒤 (지금)** |
|---|---|---|
| −3.0%p | — | 오차 −0.06, p-p 0.8 |
| 0 | 오차 −0.02, p-p 0.2 | 오차 −0.02, p-p 0.2 |
| +3.0%p | 오차 +1.99, **p-p 65.6 붕괴** | **오차 −0.04, p-p 0.2** |
| +6.0%p | — | 오차 +4.68, p-p 123.3 붕괴 |

**허용 오차가 ±2.5%p → ±4%p 로 넓어졌다.** MPPI 가 적분항을 되돌리던 것이
양방향 실패의 공통 원인이었다.

상승·하강 4단 스텝 (139.5 ↑ / 133.2 ↓ / 157.9 ↑ / 126.7 ↓):

| 구간 | 오버슛 | 정착(±1 kPa) | 정상상태 오차 |
|---|---|---|---|
| ↑ 139.5 | +2.3 | 1.23 s | −0.02 |
| ↓ 133.2 | +0.1 | 0.51 s | +0.00 |
| ↑ 157.9 | +1.5 | 0.84 s | +0.05 |
| ↓ 126.7 | −0.1 | 0.45 s | +0.36 |

배기가 모델보다 +2.0%p 뻑뻑해도 하강한다 (136→127, 정착 0.91 s, 오차 +0.42).
음압 채널 오차 절대평균 0.21 kPa.

---

## S-10. 보정이 통째로 17% 밀려 있었다 — I_MAX 실측 (2026-08-28, 실기 20260828_152809)

### 증상

시작부터 못 따라갔다. 챔버가 이전 실행 잔압으로 141 kPa 에 있는데 레퍼런스는
대기압(101.4)이다. 컨트롤러는 배기를 **49.4% 로 19 초간 유지**했고 챔버는
140.6 → 141.3 으로 꿈쩍도 안 했다.

### 원인 — 지령 → 전류 이득을 잘못 알고 있었다

정상상태 구간만 골라 (0.5 초 이상 ±1% 유지) 3개 실행 80 구간을 모으면:

```
전류[mA] = 2.505 · 지령[%] − 0.6      R = 0.999
```

**실효 I_MAX = 0.2505 A. 모델은 0.3 A 를 가정하고 있었다.**

더 나쁜 것은 S-7·S-8 의 보정이 그 잘못된 값으로 **환산**됐다는 점이다.
2026-08-27 실기는 8 Hz 로 스위칭 중이라 로그의 지령 열이 전류와 대응하지
않았고, 그래서 전류로 구간을 나눈 뒤 `지령 = 전류/3.0` 으로 되돌렸다.
실제는 `/2.505` 이므로 **곡선 전체가 약 17% 아래로 밀렸다.**

| | 모델이 본 크래킹 | 실측 |
|---|---|---|
| 충전 micro | 41.1% | **49.2%** |
| 배기 atm | 41.1% | **52.0%** |

배기를 49.4% 로 밀어도 안 열리는 게 당연했다 — 실제 크래킹 아래였다.

### 고친 것

`valve_params_calibrate.py` 를 **전류(A) 기준 적합**으로 다시 썼다. 지령 환산을
거치지 않으므로 I_MAX 를 잘못 알아도 곡선이 밀리지 않는다.

```
I_MAX  = 0.2505 (실측)      k_shape = 284.5405      alpha_shape = 1.0
pos/micro  A_max 0.032258  C_k 0.14981698  반개 50.7%  대역 47.6~53.8%
pos/atm    A_max 0.042874  C_k 0.15422550  반개 54.8%  대역 51.7~57.9%
pos/macro  A_max 0.015611  C_k 0.22301698  반개 50.7%  대역 47.6~53.8%
```

배기의 원 적합은 `k_shape ≈ 1059` (비례대역 1.7%p) 로 사실상 계단이 나왔다.
같은 하드웨어이므로 충전과 같은 k_shape 를 쓰고 C_k 로 크래킹만 맞췄다
(면적 −8.7% 오차를 감수). 계단보다 완만한 쪽이 제어 가능하다.

여유폭(`STIFF_ROLES`)에 배기(pos/atm)를 추가했다 — 이번 실패가 배기 쪽이었고,
"멈춰 서는" 실패가 "조금 넘치는" 실패보다 나쁘다.
`ki_u_limit_pct` 10 → 15 (크래킹이 47~58% 로 올라가 여유가 더 필요하다).

### 교훈

**로그의 지령 열은 스위칭 중에 믿을 수 없다.** 전류가 유일하게 신뢰할 수 있는
신호다. 앞으로 밸브 특성은 전류 기준으로만 뽑을 것. 지령↔전류 환산이
필요하면 그 이득 자체를 정상상태 구간에서 먼저 실측할 것.

### 결과 (시뮬, 4단 각도 60→25→50→20°)

양압 오차 절대평균 0.60 kPa, 음압 0.46 kPa (스텝 과도 포함).
정착 후: 양압 ref 127.4 → 실측 127.3, 음압 ref 36.6/70.1/101.3 → 전부 일치.
밸브 개폐 0.04~0.05 Hz. 회귀 테스트 통과.

---

## S-11. 하강 추종과 적분항 (2026-08-28, 실기 20260828_153540)

### 질문: I텀은 작동하는가 → 작동한다. 다만 매번 지워지고 있었다

증거는 로그에 있다. t=52.4~66.4 구간에서 오차가 **+5.6 → +0.4 로 14 초에 걸쳐
감소**한다. 이것이 적분항이다. 문제는 두 가지였다.

**(1) 부호가 바뀔 때마다 통째로 0 으로 리셋됐다.**
```
u 48.4 → 54.3  (trim 4.3 → 8.9, 크래킹 도달까지 6 초)  → 압력 하강 시작
오차 부호 반전 → trim 8.9 → 0.0, u 54.3 → 44.9        → 다시 6 초 재학습
```
이 밸브는 크래킹 아래에서 유량이 정확히 0 이라 적분기가 크래킹까지 기어오르는
데 수 초가 걸린다. 목표를 지나치는 순간 그 수 초치를 전부 버리고 0 에서 다시
시작하므로 평균이 늘 목표 위에 앉는다. 리셋을 없앴다. 대신 각 밸브는
**자기 방향의 부호만** 쓴다 (`std::abs()` 는 리셋이 있을 때만 성립했다).

**(2) 이득이 너무 낮았다.** `ki_atm` 0.15 → 0.60, `ki_micro` 0.15 → 0.35.
0.15 면 +3 kPa 오차에서 0.45 %/s 라 스텝 간격(6~12 초) 안에 크래킹에 못 닿는다.

### 배기 밸브 재적합 — 이전 적합이 오염돼 있었다

S-10 의 배기 A_max = 0.0429 는 틀렸다. 2026-08-27 실행은 과압 세이프티가 배기를
**100%(250 mA)** 로 래치했고, 전류의 50 ms LPF 가 그 순간을 142~165 mA 구간에
번지게 해 면적이 20 배로 잡혔다. 이번 정상상태 데이터가 옳다:

| 전류 | 지령 | n | dP/dt |
|---|---|---|---|
| 122~130 mA | 49.7% | **1512** | −0.1 kPa/s |
| 130~138 mA | 52.9% | 772 | −1.9 |
| 138~146 mA | 57.2% | 50 | **−33.7** |
| 146~154 mA | 59.6% | 71 | −43.9 |

재적합: `A_max 0.006, k_shape 413.1948, C_k 0.15860395`, 반개 **56.6%(142 mA)**.
최대 배기율은 ≈145 kPa/s 로 충전(1000+ kPa/s)보다 한참 약하다.

### 크래킹 하한 — `u_crack_` 도 죽은 코드였다

역모델은 요구 유량이 작으면 작은 지령을 낸다. 그런데 크래킹 아래에서는 유량이
정확히 0 이므로 그 지령은 "조금 흐른다" 가 아니라 "아무것도 안 흐른다" 가 되고,
피드백이 없어 오차가 얼어붙는다. **유량을 요구했으면 최소한 크래킹까지는 연다.**

`u_crack_` 은 계산만 되고 아무도 쓰지 않았다 (접근자 `u_crack()` 도 호출처 0곳).
`valve_crack_area_frac` 은 1e-6 → **0.01**. 1e-6 은 시그모이드 바닥이라 실제
유량이 시작되는 지점보다 한참 아래를 가리켰다 (배기 기준 112 mA, 실측 138 mA).
0.05 는 반대로 충전 최소 유량이 72 kPa/s 나 되어 한 번 열 때마다 6 kPa 를 밀어
올려 ±3 kPa 리밋사이클이 됐다. 0.01 이면 충전 ≈14.5, 배기 ≈1.9 kPa/s 다.
**최소 스텝이 곧 정착 정밀도다.**

### 순서가 전부다 — MPPI 뒤, 그리고 LPF 뒤

같은 함정을 세 번 밟았다.

1. **적분 보정을 uref 에 넣으면 MPPI 가 되돌린다** (S-9). 같은 모델로
   재최적화하니 "그만큼 열면 넘친다" 며 du 로 뺀다.
2. **크래킹 하한도 마찬가지다.** 피드포워드에 걸었더니 하한 54.3% 인데 최종
   지령이 45.9% 였고 오차가 +1.05 kPa 에 정확히 얼어붙었다.
3. **하한을 LPF 앞에 걸어도 무효다.** 명령 LPF 는 비대칭이라 "닫을 땐 즉시"
   경로가 있고, 지령이 한 틱이라도 내려가면 필터가 그 값으로 곧장 떨어진다.
   그래서 출력은 평균이 아니라 최근 **최솟값**에 눌러앉는다.

지금 `finish()` 의 순서는 **MPPI → 적분 보정 → LPF → 크래킹 하한** 이다.
모델을 이기려는 보정은 전부 모델 뒤에, 물리적 하한은 맨 마지막에.

### 남은 것

시뮬에서 정상상태 오차 **+0.48 ~ +0.70 kPa**, p-p 0.75~1.25 로 작게 순환한다
(음압은 −0.27 ~ −0.41). 얼어붙지는 않는다.

이 +0.6 정도의 편향은 **최소 스텝의 비대칭** 때문이다. 충전 최소가 14.5 kPa/s
인데 배기 최소는 1.9 kPa/s 라, 순환의 평균이 목표보다 위에 앉는다. 더 줄이려면
`valve_crack_area_frac` 을 밸브별로 나눠 최소 유량을 맞춰야 한다 (지금은 전역
하나다). 정착 시간이 10~19 초로 긴 것도 같은 이유다 — 최소 스텝이 작을수록
정밀하지만 느리다.

### 교훈

시험 하네스가 `channel_config.chN.atm.C_k` 를 **옛 값으로 하드코딩 오버라이드**
하고 있어서, 새 보정을 적용하고도 한동안 옛 곡선으로 시뮬을 돌렸다. 오버라이드로
모델 오차를 흉내 낼 때는 **현재 yaml 값을 읽어 상대량으로** 걸 것.

---

## S-12. 3 Hz 리밋사이클 — 크래킹 하한이 "확 열기"가 되어 있었다 (2026-08-28, 실기 20260828_160825)

### 증상

추종은 거의 맞는데 **3.04 Hz 로 p-p 14~30 kPa** 진동. v2 가 100%(과압 세이프티)
까지 갔다. 한 주기:

```
27.60  act 128.2  v1 41.7%          ← 압력이 살짝 내려가자 크래킹 하한이 강제 개방
27.69  act 128.5  I1 104.5 mA
27.73  act 131.3  dP/dt +116        ← 40 ms 만에 +3 kPa
27.77  act 134.1  v1 0  v2 53.9     ← +5.4 kPa 오버슛, 배기로 전환
28.37  act 128.1  v1 41.1           ← 다시 아래로 지나쳐 v1 발사
```

### 원인 1 — 하한이 무조건 걸렸다

S-11 에서 넣은 크래킹 하한은 "유량을 요구했는데 아무 일도 안 일어난다" 를
구제하려는 것이었는데, 조건 없이 걸리니 압력이 조금만 내려가도 밸브를 열었다.
**압력이 이미 움직이고 있으면 피드백이 살아 있으므로 하한이 필요 없다.**
`|dP/dt| < crack_floor_rate_kpas(5.0)` 일 때만 걸도록 게이트를 달았다
(측정 변화율은 0.25 kPa 양자화 때문에 τ=100 ms 로 걸러 쓴다).

### 원인 2 — 충전 곡선의 저단부가 33 배 틀렸다

같은 로그에서 지령 41%/104 mA, 라인압 245 kPa 일 때 dP/dt 가 +285~+495 kPa/s
였다. 유효면적으로 5.66e-3 = A_max 의 **17.5%**. 모델은 같은 점에서 1.7e-4
(0.5%) 를 예측했다. 그래서 "최소 유량" 이라고 연 지점이 실제로는 285 kPa/s 를
쏟는 지점이었다.

그 점(102 mA @245 kPa 에서 σ=0.175)에 맞춰 충전 `C_k` 를 0.14605948 → **0.136851**
(@245 kPa) 로 내렸다. 반개 지령이 50.7% → 44.4% 가 된다.

고단부(127.5 mA 에서 면적 0.0248)는 27% 과대평가가 되지만, 미세 제어를 지배하는
것은 저단부다. **하나의 시그모이드로 양쪽을 다 맞출 수는 없다** — 두 실측
구간의 기울기가 서로 다르다 (저단 k≈490, 고단 k≈146).

### 결과 (시뮬, 각도 4단)

| | 실기 (고치기 전) | 시뮬 (지금) |
|---|---|---|
| 진동 주파수 | **3.04 Hz** | **0.20 Hz** |
| 정상상태 오차 (양압) | +3.86 / +3.22 | **+0.09 / −0.04 / −0.06** |
| p-p | 14~30 kPa | 1.75~3.75 kPa |
| v2 최대 | 100% (세이프티 래치) | 60.7% |

음압도 −0.29 ~ +0.03 kPa. 회귀 테스트 통과, 기동 ERROR 0건.

### 남은 것

p-p 1.75~3.75 kPa 의 느린(0.2 Hz) 맥동이 남는다. 충전 밸브의 최소 스텝이
그만큼이라서다 — 이 밸브는 비례대역이 좁아 "아주 조금만 열기" 가 안 된다.
더 줄이려면 밸브별 `valve_crack_area_frac` 이 필요하고(지금은 전역 하나),
근본적으로는 저단·고단 기울기가 다른 것을 시그모이드 하나로 쓰는 한계가 있다.

---

## S-13. 도달했다가 무너지는 진동 — 게이트 부호 오류 + 레일이 너무 높다 (2026-08-28, 실기 20260828_163335)

### 결정적 장면

t=62.0~63.5 에 컨트롤러가 **완벽하게 잡고 있었다**. 배기를 크래킹(54.9% / 137 mA)
에 두고 챔버 114.3 kPa 를 ±0.4 로 1.5 초간 유지했다. 그러다 무너졌다:

```
63.54  act 114.3  v1 0.0  I1 1.8   v2 54.9  I2 136.6   ← 안정
63.64  act 115.1  v1 0.0  I1 45.1  v2 11.6  I2  70.0   ← 배기 지령이 11.6% 로 붕괴
63.74  act 121.8                                        ← 아무것도 못 하는 사이 +6.7
64.34  act 125.0                                        ← 2 Hz · p-p 40 kPa 진동 시작
```

### 원인 1 — 크래킹 하한 게이트를 절댓값으로 걸었다

S-12 에서 넣은 게이트는 `|dP/dt| < 5 kPa/s` 였다. 위 장면에서 압력이 +8 kPa/s 로
**올라가기 시작하자** 이 조건이 깨져 하한이 풀렸다. 배기가 필요한 순간에,
정확히 배기가 가장 필요한 이유(압력이 반대로 흐른다) 때문에 하한을 놓은 것이다.

조건은 "압력이 **원하는 방향으로** 충분히 움직이는가" 여야 한다:
`want_sign_ * dpdt_f_ < crack_floor_rate_kpas`.
`want_sign_` 은 이번 틱의 요구가 압력을 올리려는 것(+1)인지 내리려는 것(−1)인지다
(양압 micro·macro = +1, atm = −1; 음압은 반대).

네 경우 모두 맞는다:
| 상황 | 판정 |
|---|---|
| 배기 필요 + 압력 상승 중 (+8) | (−1)(+8) = −8 < 5 → **하한 적용** ✓ |
| 배기 필요 + 이미 −30 으로 하강 | (−1)(−30) = +30 > 5 → 하한 해제 ✓ |
| 충전 필요 + 이미 +300 으로 상승 | (+1)(+300) > 5 → 하한 해제 ✓ (S-12 의 3 Hz 방지) |
| 충전 필요 + 압력 정체 | (+1)(0) < 5 → **하한 적용** ✓ |

### 원인 2 — 레일 셋포인트가 챔버 레퍼런스와 무관했다

로그: `rail SP 283.9` 인데 챔버 목표는 `P⁺=114.3` 이었다. **게이지로 182.6 vs 13,
14 배다.** 셋포인트가 힘 수요로만 정해져 있었기 때문이다
(`ppos_sp = Ppos_sp_min + demand_norm·(Ppos_sp_max − Ppos_sp_min)`).

그 차압이 밸브를 초킹 영역(Pr = 114/285 = 0.4 < 0.528)에 두어 한 번 열 때마다
300 kPa/s 가 쏟아졌고, 레일 자체도 138~286 kPa 를 오갔다.

`PressureRefGen.rail.pos_headroom_kpa`(기본 60) 를 추가해 **직전 해의 최대 챔버
레퍼런스 + 여유폭**으로 묶는다. 0 이하면 끈다. 한 틱 지연이 있지만 레일은 챔버보다
훨씬 느리므로 문제되지 않는다.

시뮬 확인: `rail SP 283.9 → 131.3`, 라인–챔버 차압 중앙 **132 → 30 kPa**.

### 결과 (시뮬, 각도 4단)

| | 실기 (고치기 전) | 시뮬 (지금) |
|---|---|---|
| 정상상태 오차 (양압) | +2.68 / −0.12 / +1.78 | **+0.03 / +0.12 / +0.16** |
| p-p (양압) | 38~45 kPa | **1.25~2.75 kPa** |
| 정상상태 오차 (음압) | +3.63 / −3.29 / +5.06 | **−0.07 ~ −0.32** |
| 라인–챔버 차압 | 132 kPa | 30 kPa |

회귀 테스트 통과, 기동 ERROR 0건.

### 교훈

게이트·하한처럼 "언제 개입할지" 를 정하는 조건은 **부호까지 따져야 한다.**
절댓값으로 쓰면 개입이 가장 필요한 순간(상태가 반대로 가는 순간)에 정확히
개입을 멈춘다. S-12 에서 3 Hz 를 잡으려고 넣은 게이트가 이번엔 2 Hz 를 만들었다.

---

## S-14. 음압이 도달 못 하고 양압이 노는 문제 — 챔버 경계가 레일을 몰랐다 (2026-08-28, 실기 20260828_164637)

### 증상

양압은 잘 됐다(오차 +0.00 ~ +0.06). 음압은 오차 +15 ~ +40 kPa 로 ±25 kPa 진동.
그리고 레퍼런스 배분이 한쪽으로 쏠렸다 — 양압은 대기압 위 +21 kPa 밖에 안 쓰는데
음압은 −53 ~ −74 kPa 까지 내려갔다.

### 원인 — 챔버 경계가 레일 능력과 무관했다

슬루 박스는 **한 스텝 유량**만 본다. dt 가 크면 사실상 전 구간이 열린다 —
실기 로그에서 `P⁻` 경계가 늘 `[27.0~101.3]` (최대폭) 이었다. 그래서 최적화가
음압레일이 40 kPa 일 때도 챔버 목표를 27 kPa 로 냈다. **챔버는 레일보다 깊어질
수 없으므로** 도달 불가능한 목표를 쫓으며 진동했다.

```
t=20.1  ref- 39.6  act- 57.2   음압레일 33.2   ← 목표와 레일 차이 6 kPa
t=54~60 ref- 27.0  act- 28.5   레일 28.1       ← 레일 바닥, 더 못 감
```

그리고 이것이 배분 쏠림의 원인이기도 했다. **음압이 공짜처럼 보였기 때문**이다 —
최적화 입장에서 음압은 −74 kPa 까지 마음대로 쓸 수 있는 자원이었고, 그래서 힘을
전부 음압으로 만들고 양압을 놀렸다. 실제로는 낼 수 없는 압력이었다.

`PressureRefGen.rail.chamber_neg_headroom_kpa` / `chamber_pos_headroom_kpa`
(기본 15) 를 추가해 챔버 레퍼런스를 **측정 레일에서 그만큼 떨어진 곳까지만**
허용한다. 0 이하면 끈다.

### 결과 — 배분이 저절로 균형을 찾는다

| | 고치기 전 (실기) | 고친 후 (시뮬) |
|---|---|---|
| 양압 레퍼런스 | 대기압 +21 kPa | **+55.6 kPa** |
| 음압 레퍼런스 | 대기압 −74 kPa | **−35.4 kPa** |
| 음압 경계 | 늘 `[27.0~101.3]` 고정 | `[52.8~101.3]` 레일 따라 이동 |
| 양압 오차 | +0.00 ~ +3.96 | −0.05 ~ +0.94 |
| 음압 오차 | +1.69 ~ +18.63 | −0.39 ~ +0.11 |

**질문에 대한 답: 그렇다, 비슷하게 쓸 수 있다.** 별도의 배분 가중치를 넣을
필요가 없었다 — 각 쪽이 실제로 낼 수 있는 만큼만 쓰게 하면 최적화가 알아서
양쪽을 고르게 쓴다. 한쪽에 불가능한 자유를 주고 있던 것이 문제였다.

### 곁가지로 잡은 것 — macro 상류압을 800 으로 잘못 알고 있었다

같은 실행 끝에서 양압이 236 kPa 까지 폭주해 과압 세이프티가 래치했다.
양압레일이 99~110 kPa 로 무너져 챔버 목표(122)보다 낮아지자 `split_demand` 가
수요를 전부 macro 로 보냈는데, macro 는 탱크(570 kPa)를 물고 있다.

그런데 macro 모델이 틀렸다. `PIN_ROLE[('pos','macro')]` 를 800 kPa 로 뒀지만
실측 탱크는 **575 kPa** (3개 실행 중앙값) 다. `C_p·Pin` 항이 0.0276 어긋나
지령으로 11%p 차이가 났다 — 모델은 macro 가 139 mA 에서 반개라고 봤지만 실제는
107 mA 였다. 컨트롤러가 96~123 mA 를 자유롭게 내는 동안 탱크 압력이 챔버로
쏟아졌다. `PIN_ROLE` 을 575 로 고쳤다 (반개 139 → 107 mA).

**교훈: `PIN_ROLE` 이 틀리면 `C_p·Pin` 항이 통째로 어긋난다.** 각 밸브의 상류압은
가정하지 말고 로그에서 확인할 것.

---

## S-15. "목표 압력이 높으면 안 된다" — 레일 셋포인트 단위 버그 (2026-08-28, 실기 20260828_172124)

### 증상 — 사용자 관찰이 정확했다

| 챔버 목표 | 결과 |
|---|---|
| 101.3 / 106.7 / 114.6 / 123.9 | 성공 (오차 −0.6 ~ +1.9, p-p 1.4~2.0) |
| **136.6** | 실패 (p-p 175) |
| **145.1** | 실패 (p-p 203, 103~208 kPa 진동) |

### 원인 — S-13 에서 내가 넣은 상한의 단위 버그

로그를 보면 양압 레일 셋포인트가 **전 구간 131.3 kPa 고정**이었다. 챔버 목표
145.1 보다 낮다 — micro 로는 채울 방법이 없다. 그래서 `split_demand` 가 수요를
전부 macro(탱크 590 kPa)로 보냈고, 그 경로는 난폭해서 과압까지 갔다.

S-13 에서 넣은 코드:
```cpp
const double cap = (max_ref - P_ATM) + p_.rail_pos_headroom;   // ← 틀렸다
```
`x_prev_` 는 **이미 gauge** 다 (`build_slew_box` 의 `Pp0` 와 같은 단위). 여기서
P_ATM 을 또 빼면 언제나 큰 음수가 되어 `max(Ppos_sp_min, cap)` 이 항상
`Ppos_sp_min` = 30 kPa gauge = **131.3 kPa abs** 로 클램프된다. 정확히 관측값이다.

```cpp
const double cap = max_ref + p_.rail_pos_headroom;             // 고침
```

같은 τ=2.59 N·m 에서 셋포인트 비교:
| 실행 | 커밋 | rail SP |
|---|---|---|
| 20260828_163335 | 0a08379 (상한 없음) | 283.9 (너무 높음) |
| 20260828_172124 | 2252ce5 (상한 + 단위 버그) | **131.3 (너무 낮음)** |
| 지금 | 고침 | 챔버 목표 + 60 을 추종 |

### 검증

시뮬(각도 30→60→90→45°, 챔버 목표 최대 151 kPa):

```
P⁺=133.8 → rail SP 193.8      (정확히 +60)
양압 오차  +0.33 / +0.14 / −0.03 kPa   p-p 0.75~3.00
음압 오차  −0.40 / −0.30 / −0.27 / −0.03 kPa   p-p 0.97~3.17
```
높은 목표(151 kPa)에서도 오차 +0.88 로 정착한다. 회귀 테스트 통과(종료코드 0).

### 교훈

**gauge / absolute 를 코드 안에서 섞지 말 것.** `PressureRefGen` 내부는 전부
gauge 이고 (`x_prev_`, `lb/ub`, `Ppos_sp_*`, `sup.P_rail_*`), 로그 출력만
`to_abs_kpa()` 로 절대압으로 바꾼다. 로그가 절대압이라 내부도 절대압일 거라고
넘겨짚은 것이 원인이다.

또 하나: **A/B 시험 전에 빌드가 실제로 링크됐는지 확인할 것.** 처음 A/B 는
링크 전 바이너리를 써서 "수정이 안 먹는다" 는 잘못된 결론을 냈고, 진단 출력을
넣고서야 이미 고쳐져 있었다는 것을 알았다.

---

## S-16. 음압 진동 · 깊은 목표 미도달 · 안 사라지는 정상상태 오차 (2026-08-28, 실기 20260828_173347)

### (1) 음압 채널 밸브를 처음 실측했다 — 추정치가 두 방향으로 틀렸다

그동안 음압 밸브 파라미터는 양압 값에 `1.778125/0.58789258 = 3.02` 를 곱한
**추정치**였다. 실측하니 두 밸브가 서로 반대 방향으로 틀려 있었다.

```
n1 micro (챔버→진공, 압력 내림)   105mA→0.000074  115→0.001994  125→0.011906
                                  135→0.015008    145→0.018026
n2 atm   (대기→챔버, 압력 올림)   105mA→0.010488  115→0.013286  125→0.014177
                                  135→0.014704
```

| | 실측 | 예전 모델 |
|---|---|---|
| n1 @125 mA | 0.011906 | **0.096216** (8 배 과대) |
| n2 @105 mA | 0.010488 | **0.000000** (닫혔다고 봄) |

- n1 을 8 배 크게 보니 **너무 조금 열었다** → 깊은 목표에서 도달 실패
- n2 를 닫혔다고 보니 **너무 많이 열었다** → +20 kPa 오버슛

이 조합이 "목표가 깊을수록 커지는 진동" 의 정체다. 실제로 오차가 목표 깊이에
정확히 비례했다:

| ref⁻ | 101.3 | 93.9 | 83.1 | 76.5 | 69.4 | 67.6 | 52.0 | 49.8 |
|---|---|---|---|---|---|---|---|---|
| 오차 | −0.19 | −0.79 | +2.21 | +4.95 | +6.31 | +9.54 | **+15.15** | **+15.96** |

실측값으로 다시 적합했다 (새 모델은 실측을 −11.7% ~ +19.4% 로 재현):
```
neg/micro  A_max 0.018500  C_k 0.13251600 @80 kPa   반개 49.1% (123 mA, 실측 119)
neg/atm    A_max 0.015000  C_k 0.11419700 @101.3    반개 40.7% (102 mA, 실측 102)
```
`PIN_ROLE` 도 고쳤다 — neg micro·macro 의 상류는 대기(101.3)가 아니라 **챔버**다
(진공 동작 중 대표값 80 kPa).

### (2) 안 사라지는 정상상태 오차 — 트림이 크래킹 하한에 눌려 있었다

로그에서 "얼어붙은" 구간을 찾으면:
```
양압 93.8~97.4s (3.6초)  ref 101.5  오차 +1.62  p-p 1.61  배기 지령 57.4% 고정
음압 93.3~97.4s (4.1초)  ref 101.3  오차 −1.66  p-p 1.64
```
**57.4% 는 정확히 그 순간의 크래킹 하한값이다.**

적분 트림은 MPPI 뒤·하한 앞에 있었다. MPPI 출력이 0 이면 최종 지령은
`max(0 + 트림, 하한)` 이 되고, 트림은 `ki_u_limit_pct = 15%p` 로 묶여 있어
하한(57.4%)을 넘을 수가 없다. 하한이 트림을 통째로 덮어써서 지령이 하한값에
그대로 얼어붙는다 — 적분기가 아무리 쌓여도 출력이 안 변한다.

트림을 **하한 뒤**로 옮겼다. 최종 순서:

```
MPPI → 명령 LPF → 크래킹 하한 → 적분 트림
```

같은 함정을 이번이 네 번째다 (S-9 uref 안, S-13 하한을 피드포워드에,
S-13 하한을 LPF 앞에, 이번 트림을 하한 앞에). **모델·필터·하한을 이기려는
보정은 언제나 그것들 뒤에 와야 한다.**

### 결과 (시뮬, 각도 40→75→100→55°)

| | 고치기 전 (실기) | 고친 후 (시뮬) |
|---|---|---|
| 양압 오차 | −0.47 ~ +5.09 | **+0.00 ~ +0.70** |
| 양압 p-p | 1.4 ~ 40.2 | **0.75 ~ 2.00** |
| 음압 오차 | −6.41 ~ **+15.96** | **−0.09 ~ +0.00** |
| 음압 p-p | 0.14 ~ 46.7 | **0.00 ~ 0.43** |

회귀 테스트 통과(종료코드 0).

### 남은 관찰 — 양압 챔버에 미세 누설이 있다

배기를 크래킹에 물려 놓아도 압력이 안 내려가는 구간이 있었다. 유량 균형상
챔버로 들어오는 것이 있다는 뜻이다 (레일 130~150 kPa 에서 v1 이 완전히 안 막는
쪽이 유력). 지금은 적분기가 흡수하지만, 크면 `leakage_u_pos`(현재 0.0) 로
모델에 넣는 편이 낫다. 실기에서 모든 채널 밸브를 닫고 챔버 압력 변화를 재면
바로 확인된다.

---

## S-17. 6채널 — 레퍼런스 흔들림 재발 + 공급 부족 감지 (2026-08-28, 실기 20260828_181748)

### (1) 레퍼런스 흔들림은 내가 만든 순환이었다

목표 각도가 고정인데 레퍼런스가 폭 4.3~27.5 kPa 로 흔들렸다. 1축에서 없앴던
증상이 6축에서 재발했다.

데이터가 답을 준다:
```
t=44.4  ref⁺ 124.3  ub⁺ 124.3  라인압 138
t=45.5  ref⁺ 104.6  ub⁺ 104.6  라인압 120
상관: ref⁺ vs ub⁺ = +0.998,  ub⁺ vs 라인압 = +0.971,  ref⁻ vs lb⁻ = +0.995
```
**레퍼런스가 경계에 소수점까지 붙어 있다.** S-14 에서 넣은
`chamber_pos/neg_headroom` 이 경계를 **측정 레일**에 직결시켰고, 수요가 커서
레퍼런스가 경계에 붙으면 레일 리플이 그대로 레퍼런스 리플이 된다.

1축에서는 경계에 닿지 않아 안 보였다. 6축이 같은 레일을 나눠 쓰면서 항상
닿게 됐다. 공급압에 저역통과를 걸었다 (`supply_filter_tau_s`, 기본 0.5 s) —
레일이 실제로 내려앉는 것은 따라가되 리플만 없앤다.

### (2) 공급 부족을 터미널에서 보이게 했다

그 실행은 **탱크가 98 kPa** (정상 ~600) 로 비어 있었다. `tank_low` 는 계산되고
있었지만 INFO 줄 끝에 `" LOW"` 로만 붙어 있어 알아채지 못했다. 세 가지를
2 초마다 ERROR 로 올린다:

- 탱크가 운전 하한(`tank_stop_kpa`) 미만 → macro 부스트 불가
- 양압레일이 챔버 목표 최대보다 낮음 → micro 로 채울 수 없고 수요가 macro 로 몰림
- 음압레일이 챔버 목표 최저보다 깊지 않음 → 더 내려갈 수 없음

### (3) 그 로그로는 밸브를 피팅하면 안 된다

측정된 유효면적이 전부 1e-5~5.8e-4 로, 1축 정상 측정값(0.032)의 **1/50** 이었다.
밸브가 작아서가 아니라 **공급이 모자라서**다:

```
탱크 98~99 kPa           (정상 ~600)
충전 중 라인−챔버 차압    21~34 kPa   (1축 때는 100+)
6채널 동시 충전 요구      10.7% 시간, 그때 라인압 146 → 131 로 주저앉음
ch1 챔버가 150 kPa 인데 라인 중앙이 141 — 레일로는 채울 수가 없다
```
이 값으로 피팅했다면 밸브를 50 배 작게 잡아 컨트롤러가 전 채널을 100% 로
냈을 것이다.

`scripts/valve_fit_from_log.py` 를 만들었다. 채널별·밸브별 특성을 **전류 기준**
으로 뽑되, 먼저 공급 상태를 검사해 미달이면 **거부한다** (탱크 < 450 kPa 이거나
충전 구동 차압 중앙 < `--min-dp`, 기본 60 kPa). `--force` 로 무시할 수 있지만
그 결과를 설정에 넣으면 안 된다.

### 채널별 피팅 절차

1. 탱크를 채운다 (≥ 550 kPa abs). 시작 시 `[공급 부족]` ERROR 가 없어야 한다.
2. **채널을 하나씩** 돌린다 — 동시에 돌리면 레일을 나눠 쓰느라 차압이 떨어져
   또 공급 제한에 걸린다. `num_actuators:=1` 로 축을 바꿔 가며 각 60 초 이상.
3. `valve_fit_from_log.py <run.csv> --axes 1` 로 채널별 표를 뽑는다.
4. 채널 간 차이가 유의하면 `valve_params_calibrate.py` 의 `FIT` 표를 채널별로
   나눈다 (지금은 pos/neg 역할별 공통).

---

## S-18. 6채널 실기 (2026-08-28, 20260828_184156) — 하드웨어 정상화 후

앞선 두 실행은 하드웨어 연결 문제로 버렸다 (`20260828_181748` 탱크 공백,
`20260828_183044` ch5 양압 채널 물리적 개방 — 손으로 열어 보니 공기가 새고 있었다).
ch5 가 충전 밸브를 82.8% 시간 동안 164 mA 로 열어 둔 채 레일을 빨아들여
**모든 채널**을 굶겼다: ch5 열림 구간 라인압 중앙 119.2 vs 닫힘 구간 141.8 kPa.

정상화 후 결과:

| | 값 |
|---|---|
| 탱크 | 519~639 (중앙 629) ✓ |
| 양압 라인 | 113~251 (중앙 166, 이전 120) |
| 공급 부족 경고 | 7건 (이전 27건) |
| **음압** | 오차 −1.35~+1.85 kPa, p-p 0.09~2.92 — **거의 완벽** |
| 양압 정상상태 | 대체로 −0.24~+3.4 kPa |
| 양압 진동 | 간헐적으로 **p-p 15~40 kPa**, 채널을 옮겨 다님 |

### 양압 진동의 정체 — 지령 트림이 밸브를 활짝 연다

macro 는 거의 안 쓰였다 (통전 0.01~0.54% 시간). v1↔v2 릴레이가 3 Hz 로 돈다:
```
39.97  act 140.2  v1 58.6%/148mA
40.13  act 148.6  v2 54.4%/133mA   ← +8.4 오버슛
40.61  act 160.4  v2 52.7%         ← +19.5
40.93  act 126.0  v1 55.9%         ← −14.9
```
모델이 요구하는 지령은 **38.5%** (면적 2% 개방) 인데 실제 지령이 **53%** 다.
차이 15%p 는 지령 트림이 `ki_u_limit_pct` 상한에 포화한 것이고, 이 밸브는
비례대역이 6.2%p 라 53% 는 **사실상 완전 개방** — 의도의 48 배 유량이다.

### 시도한 것과 그 결과 — 유량 트림은 지령 트림을 대체하지 못한다

적분기를 지령 %p 가 아니라 **요구 유량의 배율**로 쓰도록 바꿔 봤다
(`q_gain = 1 + clamp(ki_flow·적분, ∓q_trim_limit)`). 보정이 수요에 비례하므로
오차가 작으면 지령이 크래킹 근처에 머문다 — 원리는 옳다.

그러나 **크래킹 위치 오차는 못 고친다.** 곡선이 가팔라 유량을 3 배 요구해도
지령은 1.6%p 밖에 안 오른다. 시뮬에서 플랜트를 모델보다 8%p 뻑뻑하게 두면:

| 설정 | 정합 | +8%p 뻑뻑 |
|---|---|---|
| 지령 트림 15%p (기존) | 오차 0.02, p-p 0.00 | 오차 7.78, p-p 54.4 |
| 유량 트림 (지령 트림 3%p) | 오차 0.08, p-p 0.35 | 오차 8.89, p-p 44.9 |

둘 다 8%p 오차에서는 무너진다. 유량 트림이 더 낫지도 않다.
**구현은 남기되 기본은 꺼 둔다** (`ki_flow: 0.0`). 실기 검증 전에는 켜지 말 것.

### 남은 것

양압 진동을 실제로 잡으려면 크래킹 위치를 더 정확히 알아야 한다. 이번 실행에서
잰 잔차는 −13%p 였지만 **진동 중 과도 구간에서 잰 값**이라 밸브 동특성(60 ms)이
섞여 있어 정적 오차인지 구분되지 않는다. 채널을 하나씩, 공급이 넉넉한 상태에서,
정상상태를 유지하며 재야 한다 (`valve_fit_from_log.py` 가 공급을 검사한다).

채널별 이득 차이도 최대 2 배로 나왔지만 (상대 부피 1.00 / 1.96 / 1.95 / 1.60 /
1.31 / 1.89) 같은 이유로 확정할 수 없다.

---

## S-20. 6채널 실기 — 느린 채널 / 진동 채널의 공통 원인, 그리고 라인 MPPI 가 나쁜 이유
(2026-08-29, `20260829_142216` = 채널별 MPPI, `20260829_142442` = 라인 MPPI)

두 실행 모두 공급은 정상이었다 (탱크 중앙 620).

### (1) 느린 채널과 진동 채널은 같은 원인이다 — 챔버 부피

부피는 요구 유량에 그대로 곱해진다 (`m_dot = P_dot·V/(R·T)`). 그런데 `vol_ml_` 은
채널별 배열이면서 **전 채널이 같은 값(50 mL)으로 초기화**된다.

같은 전류·같은 차압에서 dP/dt 를 비교하면 (A 실행, 공급 정상):

| ch | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 상대 유효 부피 | 1.00 | 2.82 | 3.06 | 0.76 | 0.42 | 1.61 |

증상과 정확히 맞는다:
- **ch1·ch2 (부피 큼)** → 필요한 유량의 1/3 만 요구 → 밸브 통전 8~12%,
  정착 8~22 초, 정상상태 오차가 남는다
- **ch3·ch4 (부피 작음)** → 과요구 → ch4 는 p-p 33~45 kPa 로 반복 진동

**고친 것**: `channel_config.chN.volume_ml` 오버라이드를 추가했다 (0 이하면 공통값).
기동 시 `[채널 부피 mL] ch0=50 ch1=... ` 로 찍는다.

**주의 — 크기는 아직 확정 못 했다.** 전류 창을 좁히면 같은 채널이 0.15~2.45 배로
흩어진다. 6축을 같이 돌리면 레일을 나눠 쓰느라 차압이 흔들리기 때문이다. 순서는
일관되지만 값은 **채널을 하나씩 돌려** 재야 한다.
`scripts/chamber_volume_from_log.py` 가 그 계산을 하고, 흩어짐이 크면 스스로
경고한다 (이번 6축 로그로 돌리면 "흩어짐 18.7 배" 라고 거부한다).

### (2) 라인까지 MPPI 가 나쁜 이유 — 배기를 아예 안 연다

| | 배기 지령 평균 | 전류 | 통전>100 mA | 레일 중앙 | 레일 셋포인트 |
|---|---|---|---|---|---|
| A 라인 PID | **57.5%** | 143.5 mA | **90.5%** | 181 | 207.9 (−28 미달) |
| B 라인 MPPI | **3.1%** (최대 8.8) | 7.7 mA | **0.0%** | **266** | 198.3 (**+66 초과**) |

MPPI 입장에서는 레일이 높을수록 채널을 채우기 쉬우니 배기를 닫는 것이 합리적이다.
**레일이 높으면 채널 밸브가 난폭해진다는 것을 모른다.** 그 결과 B 는 ΔP 가 67~117
kPa (A 는 34~73) 로 커져 밸브가 잠깐만 열려도 왈칵 쏟아지고, 음압까지 나빠졌다
(A: p-p 0.1~0.3 → B: 7~30).

원인은 레일 가중치 `sys_w_rail = 0.5` 가 너무 낮은 것이다. 시뮬 스윕:

| sys_w_rail | 0.5 | 1.5 | 2.0 | **3.0** | 5.0 | 10.0 |
|---|---|---|---|---|---|---|
| 양압 오차 \|평균\| | 0.25 | 0.08 | 0.08 | **0.01** | 0.05 | 0.04 |
| p-p 중앙 | 30.74 | 29.46 | 22.42 | **0.26** | 1.25 | 1.51 |

2 와 3 사이에 문턱이 있다. `feat/mppi-rail` 브랜치의 기본값을 **3.0** 으로 올렸다.

> 시뮬은 펌프가 약해 레일이 셋포인트 아래에 머물러 **배기 동작 자체는 재현하지
> 못한다.** 실기에서 레일이 셋포인트를 넘는지, 그때 배기가 열리는지 확인할 것.

### (3) 덤 — 무수요 공급 경고 오탐

기동 직후 챔버·레일이 모두 대기압일 때 "여유 +1 kPa" 라고 경고가 떴다. 목표가
대기압 ±5 kPa 안이면(=사실상 수요 없음) 알리지 않게 했다.

---

## S-21. 축 선택 — 채널을 하나씩 돌린다 (2026-08-29)

채널별 부피·밸브는 **채널을 하나씩** 돌려야 잴 수 있다. 여럿을 같이 돌리면 같은
레일을 나눠 쓰느라 차압이 흔들려 추정이 흩어진다 (S-20: 같은 채널이 창마다
0.15~2.45 배로 튀었다).

### 무엇이 막고 있었나

`PositionController.axisN.pos_gid` / `neg_gid` / `actuator_idx` 는 이미 파라미터화
돼 있었다. 그런데 `build_mpcs()` 가 활성 채널을 **`0..num_actuators-1` 로 하드코딩**
해서, gid 를 바꿔도 그 채널의 MPC 가 만들어지지 않아 무시됐다.

활성 채널을 축별 gid 설정에서 도출하도록 고쳤다. 기동 시 확인용 한 줄을 찍는다:
```
[활성 채널 gid] 2 8  (보드 = gid + 5)
```
(`pos_ctrl_cfg_` 는 `build_mpcs()` 보다 뒤에 로드되므로 파라미터를 직접 읽는다.)

### 쓰는 법

```bash
ros2 launch can_powerpack control.launch.py axis:=2 actuator_connected:=false overrides:=period_ms=3
```

`axis:=N` 하나면 된다 — `num_actuators=1`, `axis0.pos_gid=N`,
`axis0.neg_gid=6+N`, `axis0.actuator_idx=N` 을 대신 넣어 준다. 명시적으로 준
`num_actuators` 나 `overrides` 가 있으면 그쪽을 존중한다 (`setdefault`).

각도는 하나만 보낸다:
```bash
python3 src/can_powerpack/scripts/position_ref_client.py 127.0.0.1 2293
# "컨트롤러 축 수 = 1" 확인 후 "70" 처럼 한 개
```

검증 (시뮬 `axis:=2`, 목표 70°): ch2 만 159.3 kPa 로 올라가고 오차 +0.27,
나머지 다섯 채널은 대기압 그대로에 밸브 전류 0.0 mA.

### 부피 측정 절차

채널마다 60 초 이상, 압력을 오르내리게 한 뒤:
```bash
python3 src/can_powerpack/scripts/chamber_volume_from_log.py \
    ~/result/<ch0>/<ch0>.csv ~/result/<ch1>/<ch1>.csv ... --axes 6
```
여러 로그를 함께 주면 각 로그에서 자기 채널만 뽑아 합친다. 창 간 흩어짐이 3 배를
넘으면 스스로 경고한다. 결과를 `channel_config.chN.volume_ml` 에 넣는다.

---

## S-22. 양압 지연의 정체 — 크래킹 위치가 11%p 낮았다 (2026-08-29, `20260829_152528`)

### 증상

레퍼런스가 바뀌고 압력이 오르기까지 **최대 3.2 초** 걸린다. 그 뒤 한 번에 튄다.

ch1 스텝 직후 궤적:
```
12.07  ref 121.5  v1지령 40.6%              ← 피드포워드가 즉시 40.6%
12.15~13.52  지령 43~45% 로 1.4 초 정체      ← 압력 104.5 에서 미동 없음
13.60~15.28  지령 45 → 62.9% 로 1.7 초 상승  ← 적분 트림이 기어오른다
15.36  압력 109 → 147.7                     ← 0.08 초에 +38.6 kPa (483 kPa/s)
```

지령은 0.02 초 만에 40% 에 닿는데 전류가 120 mA 에 닿기까지 최대 1.9 초다.
**적분기가 데드존에서 기어오르는 것이 맞다** — 다만 그건 증상이고, 원인은
피드포워드가 크래킹 한참 아래를 내는 것이다.

### 원인 — C_k 가 낮았다. A_max 는 맞다

압력이 실제로 움직이기 시작한 순간의 전류를 채널마다 재면:

| ch | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 실측 크래킹 [mA] | 133 | 156 | 150 | 132 | 132 | 135 |
| 실측 크래킹 [지령%] | 53.2 | 62.6 | 60.2 | 54.1 | 53.0 | 55.4 |

모델의 10% 개방점은 **105 mA (42%)** — 27~50 mA 낮다.

**A_max 는 맞다.** 크래킹 순간의 유량이 +122~173 kPa/s 이고, 이는 유효면적
0.0034 에 해당한다. 모델의 10% 개방 면적은 0.0032 다 — 일치한다.

> 앞서 "모델이 285 배 과대" 라고 본 것은 **크래킹 아래 구간에서 쟀기 때문**이다.
> 그 구간에서는 모델이 유량을 예측하는데 실제로는 0 이라 비율이 무한대로 간다.
> 크래킹 **위**에서 재야 A_max 를 볼 수 있다.

### 고친 것

`chN.micro.C_k` 를 채널별로 올렸다 (`C_k = I_crack + C_p·Pin − F(10%)`):

```
ch0 0.16462554   ch1 0.18167037   ch2 0.17670887
ch3 0.16307004   ch4 0.16222298   ch5 0.16612577      (기존 0.13400850)
```

검산 — ch1 상황(ref 121.5, 챔버 104, 라인 154, 오차 17.5 kPa)에서 피드포워드가 내는 지령:

| ch | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 옛 C_k | 41.2% | 41.2% | 41.2% | 41.2% | 41.2% | 41.2% |
| **새 C_k** | **53.5%** | **60.3%** | **58.3%** | **52.8%** | **52.5%** | **54.1%** |
| 실측 크래킹 | 53.2% | 62.6% | 60.2% | 54.1% | 53.0% | 55.4% |

**피드포워드만으로 크래킹에 닿는다** (±2%p). 적분기가 3 초간 기어오를 필요가 없다.

### 이걸로 함께 풀리는 것

- **지연** — 피드포워드가 바로 크래킹에 닿으니 데드존 체류가 사라진다
- **오버슛** — 63% 까지 지나쳐서 여는 일이 없으니 +38 kPa 폭주가 줄어든다
- **적분 트림 15%p** — 메울 오차가 작아지니 트림도 작게 쓸 수 있다

### 부피 문제는 아직 열려 있다

챔버 부피는 여전히 확정 못 했다 (세 방법이 10 배 이상 어긋난다 — S-21 참조).
다만 이번 C_k 보정은 **부피와 무관하다** — 크래킹은 "압력이 움직이기 시작하는
전류" 라는 부피와 상관없는 관측이기 때문이다. 부피는 정착 속도와 채널 간 편차에
남아 있으므로 다음에 따로 잡아야 한다.

---

## S-23. 지연은 줄었고, 남은 채널별 편차는 크래킹 하한을 실측으로 올려 잡았다 (2026-08-29)

### C_k 보정의 효과 (`20260829_153330`)

레퍼런스 상승 → 압력 상승 지연:

| ch | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 지연 중앙 [s] | 0.21 | 0.37 | 0.55 | 0.65 | 0.45 | 0.76 |

직전 실행의 최대 3.2 초에서 크게 줄었다. 다만 채널별 편차가 남았다:

| ch | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| p-p [kPa] | 1.3~2.4 | **31~47** | 1.3~3.0 | 1.2~7.1 | 1.4~28.3 | 1.5~13.8 |
| 정착 [s] | 1.2~6.8 | **안 됨** | 0.3~9.0 | 3.3~17.7 | 1.2~15.6 | 2.1~17.4 |

> 사용자 표기는 1-based 다 — "2번 진동"은 ch1, "4번 느림"은 ch3, "ch5 느림"은 ch5.

### 크래킹을 "무반응 상한"으로 다시 잡았다

이벤트 검출(dP/dt 가 문턱을 넘는 순간)은 이미 진동·지연 중인 채널에서 편향된다.
대신 **지령을 0.3 초 이상 유지한 구간**만 모아 압력이 올랐는지로 묶었다.
"무반응 상한"(그 전류까지는 확실히 안 흐른다)은 표본이 충분해 믿을 수 있다:

| ch | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 무반응 상한 [mA] | 136 | 158 | 153 | 144 | 141 | 145 |
| 무반응 상한 [지령%] | 54 | 63 | 61 | 57 | 56 | 58 |
| 직전 설정 [%] | 54 | 61 | 59 | **54** | **53** | **55** |

ch3·ch4·ch5 가 실측보다 3%p 낮았다 — 그래서 느렸다. 전부 무반응 상한으로 올렸다.

> ch1 은 진동 때문에 지령을 0.3 초도 유지하지 않아 표본이 2 개뿐이다.
> **폐루프 로그로는 측정이 안 된다.** 램프 시험이 따로 필요하다.

### 시도했다가 끈 것 — 데드존 인식 적분

압력이 원하는 방향으로 움직이면 적분을 멈추고, 안 움직이면(데드존) 배속으로
통과하는 구조를 넣었다. 크래킹을 정확히 몰라도 느림·진동이 함께 처리되리라
기대했는데, 시뮬에서는 오히려 나빴다:

| 설정 | 정착 중앙 |
|---|---|
| 기본 (정지·부스트 없음) | **0.69 s** |
| 정지 켬 | 1.91 s |
| 정지 + 부스트 6 배 | 5.73 s |

시뮬은 모델과 플랜트가 가까워 데드존이 짧고, 그 조건에서는 과적분만 한다.
실기의 긴 데드존(3.2 초)에서만 이득이 있을 수 있다. **구현은 남기고 기본은 껐다**
(`integ_hold_rate_kpas: 0.0`, `integ_deadzone_boost: 1.0`). 실기 검증 후 켤 것.

---

## S-24. 1축 위치 제어 준비 — 150 mm 링크 · 2 kg (2026-08-29)

### 압력 제어 현황 (`20260829_154608`)

| ch | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| p-p [kPa] | 1.4~1.8 | **35.6** | 1.4~2.8 | **1.0~1.8** | **19~21** | 1.4~2.1 |
| 지연 중앙 [s] | 0.37 | 0.72 | 0.69 | 1.28 | 0.80 | 1.41 |

- **ch3 해결** (p-p 1.2~7.1 → 1.0~1.8), **ch5 대체로 해결**
- **ch1·ch4 는 크래킹을 과하게 올렸다** — 진동 중 "무반응"으로 잡힌 구간이 실제로는
  전환 중이었다. 폐루프 로그로는 더 못 좁힌다. `axis:=1` / `axis:=4` 램프 시험 필요.

**위치 제어는 ch0(axis0)으로 간다** — 가장 안정적이다.

### 지뢰 하나를 제거했다 — `default_volume_ml: 1.0`

`actuator_connected: true` 가 되면 `vol_ml_` 이 이 값으로 바뀐다. 그런데 이를
갱신할 `actuator/volumes_ml` 토픽을 **이 저장소의 어떤 노드도 발행하지 않는다**
(`Controller::on_volume` 은 외부 발행을 기다린다). 그대로 켰다면 요구 유량이
50 배 작아져 컨트롤러가 거의 아무것도 하지 않았을 것이다. 50.0 으로 고쳤다.
채널별로는 `channel_config.chN.volume_ml` 로 덮어쓴다.

### 2 kg 로드 반영

| 항목 | 이전 | 지금 | 이유 |
|---|---|---|---|
| `axis0.mass_kg` | 5.0 | **2.0** | 실기 부하 |
| `axis0.link_length_m` | 0.15 | 0.15 | 이미 맞음 |
| `TorquePID.tau_ff_gain` | 0.5 | **1.0** | 설정 주석에 "액추에이터를 붙이면 1.0 으로 되돌릴 것" 이라고 적혀 있었다. 이제 중력을 실제로 든다. |
| `TorquePID.integ_limit_nm` | 2.0 | **0.75** | τ_max 가 7.76 → 2.94 N·m 다. 2.0 이면 τ_max 의 68% 라 적분만으로 축이 흔들린다. 같은 비율(26%)로 맞췄다. |

### 보수적 기동 — 각도 목표 슬루

`PositionController.target_slew_deg_per_s` (기본 **15**, 0 이면 끔) 를 추가했다.
TCP 로 들어온 목표를 계단으로 주면 생성기가 즉시 큰 힘을 요구해 액추에이터가
세게 튄다. 목표를 램프시키면 압력 레퍼런스도 따라서 완만해진다.

시뮬 45° 스텝 (`axis:=0`, `actuator_connected:=true`):

| 슬루 [deg/s] | 오버슛 | 양압 ref 최대 |
|---|---|---|
| 0 (계단) | +5.68° | **185 (정격 포화)** |
| 8 | +5.24° | 133 |
| **15** | +3.76° | **130** |
| 25 | +3.76° | 125 |

계단 입력에서는 압력 레퍼런스가 정격 185 kPa 에 붙는다. 슬루를 걸면 130 에서
멈춘다 — 이것이 "보수적으로 움직인다" 의 실체다.

중력 FF 를 1.0 으로 되돌린 뒤: 정상상태 오차 −3.76° → **+0.67°**, 각도 p-p
10.13° → 7.99°.

### 아직 남은 것 — 위치 게인은 실기 튜닝이 필요하다

시뮬에서 각도가 p-p 8° 로 흔들린다. `TorquePID.axis0` 의 kp 0.0786 / ki 0.20 /
kd 0.04 는 **5 kg 기준으로 튜닝된 값**이고 2 kg 에서는 과하다. 다만 게인은 질량에
단순 비례하지 않아 추정으로 바꾸지 않았다. 실기에서 흔들리면 이 순서로 낮출 것:
1. `kd` 를 먼저 (0.04 → 0.02) — 속도 잡음에 가장 민감하다
2. 그래도 흔들리면 `kp` (0.0786 → 0.05)
3. 정상상태 오차가 남으면 `ki` 를 올리되 `integ_limit_nm` 을 함께 본다

그리고 슬루를 더 조이면(8 deg/s) 그 자체로 진동이 줄어든다.

---

## S-25. 모니터 각도 표 통합 + 엔코더 보정 일치 (2026-08-29)

### 각도가 12 개 나오던 문제

모니터에 각도를 보여 주는 절이 **두 개**였다:
- `Encoders` — `board/analog` 을 3 개씩 끊어 출력 (보드 17~25, 9 개)
- `Position Control` — `position_dbg` 를 축마다 출력

같은 각도가 두 곳에 찍혀 화면에 12 개가 됐다. **보드 순서 6 행 한 표**로 합쳤다:

```
|  Enc | Axis |  Angle(deg) | Target(deg) |   Err(deg) |
|  17  |  0   |       44.93 |       45.00 |      +0.07 |
|  18  |  1   |        0.00 |             |            |
...
|  22  |  5   |        0.03 |             |            |
```

목표각은 `control_mode 2` 면 `controller/pressure_ref_dbg` (축 블록의 [1]),
`mode 1` 이면 `position_dbg` 에서 온다. 안 도는 축은 목표·오차를 비운다.
위치 제어 세부(속도·pid·ff·마찰)는 mode 1 에서만 별도 한 줄로 남겼다.

### 엔코더 보정 — 두 경로가 어긋나 있었다

각도는 **CanBridge** 가 계산해 `board/analog` 로 낸다. 그 경로는 이미
`EncoderCalibration.boards.N.raw_0deg/raw_90deg` 를 읽어 `can_monitor.py` 의
`calib_from_raw_2pt()` 와 같은 식으로 offset/gain 을 만든다 — 값도 can_monitor 와
같은 raw 1200/2470 (board 17) 이었다.

그런데 **컨트롤러 쪽 `Sensor_calibration.boards.17~19` 는 일반 기본값**
(offset 1740.0, gain 0.0757) 그대로였다. 같은 raw 2 점에서 계산한 값으로 맞췄다:

| board | raw 0°/90° | offset | gain |
|---|---|---|---|
| 17 | 1200 / 2470 | 3827.8388 | −0.07254921 |
| 18 | 860 / 2000 | 4159.9512 | −0.08082237 |
| 19 | 2115 / 3155 | 2934.0659 | −0.08859375 |

검산: raw 1200 → 0.00°, raw 2470 → 90.00° (board 17).

> 실기에서 각도가 이상하면 **`EncoderCalibration.boards.N` 의 raw 2 점**을 먼저 볼 것.
> 그쪽이 실제로 각도를 만드는 경로다. `Sensor_calibration` 은 컨트롤러의 보조 계산이다.

---

## S-26. 0도가 158도로 읽히던 문제 — 엔코더 보정이 통째로 무시되고 있었다 (2026-08-29)

### 증상

링크가 0° 인데 모니터에 **158°**. `can_monitor.py` 는 같은 순간 0° 로 정확히 나온다.

### 원인 — 두 겹이었다

**(1) 브리지가 실측 보정을 못 읽고 일반 기본값으로 돌고 있었다.**

`EncoderCalibration.boards.17.raw_0deg` 를 yaml 에 **정수(`1200`)** 로 적어 두었다.
`declare_double_flexible()` 은 `declare_parameter<double>()` 이 타입 불일치로 던지는
경우를 처리하려 했지만, 이 rclcpp 버전에서는 그 뒤 `get_parameter()` 도 실패해
값이 통째로 무시된다. 그러면 기본값(offset 1740.0, gain **+0.0757**)으로 도는데,
실측값은 gain 이 **음수**(−0.0725)다 — 부호까지 반대다.

검산: raw 1200 에 기본값을 쓰면 정확히 **+158.05°** 가 나온다. 관측값과 일치한다.

`raw_0deg: 1200` → `1200.0` (실수)로 고쳤다. 이제 로그에 이렇게 뜬다:
```
엔코더 board 17: offset 3827.84 mV, gain -0.072549 deg/mV (실측)
```
검산: raw 1200 → 0.00°, raw 1835 → 45.00°, raw 2470 → 90.00°.

**(2) 브리지가 이미 경고하고 있었는데 아무도 못 봤다.**

```
[ERROR] 엔코더 캘리브레이션 없음: board 17 — 일반 기본값(...)으로 돈다.
        실측된 보드들은 gain 이 모두 **음수**인데 이 기본값은 **양수**다
        → 각도가 반대 방향으로 읽힐 수 있다.
```
이 ERROR 가 **며칠째 매 실행마다** 찍히고 있었다. `control.launch.py` 의
can_bridge 가 `output='log'` 라 터미널이 아니라 로그 파일로만 갔다.
`output='screen'` 으로 바꿨다.

### 교훈

- **yaml 의 수치는 실수로 적을 것.** `1200` 과 `1200.0` 이 다르게 동작한다.
  같은 함정이 `declare_double_flexible` 을 쓰는 모든 파라미터에 있다.
- **진단을 만들었으면 보이는 곳에 띄울 것.** 코드는 이 문제를 정확히 짚어내고
  있었는데 `output='log'` 하나 때문에 소용이 없었다.

---

## S-27. 값이 다 깨진 이유 — 시뮬레이터가 실기 브리지의 노드 이름을 뺏고 있었다 (2026-08-29)

### 증상

CAN 만 연결하고(펌프 off, 매크로 미연결) 제어기를 돌렸는데:

- 실제로는 대기압인데 **board 5 가 305 kPa**, **board 11 이 43 kPa** 까지 튄다
- **board 17 엔코더가 0° 와 105.02° 를 번갈아** 찍는다
- 안 도는 축의 채널은 정확히 **101.325 kPa** 고정
- 그런데 **`can_monitor.py` 는 내내 정상**이었다

### 원인

`virtual.launch.py` 의 시뮬레이터가 **16:06:51 에 떠서 20 분간 안 죽고 있었다.**

```
15324  virtual_powerpack --ros-args -r __node:=can_bridge -r __ns:=/pack2
```

`launch/virtual.launch.py:123` 이 `name='can_bridge'  # CanBridge 자리를 그대로 대체`
다. 시뮬레이터를 단독으로 쓸 때는 맞는 설계지만, 실기 브리지와 **동시에** 뜨면
둘이 같은 이름·같은 네임스페이스·같은 토픽이 된다. ROS 2 는 이걸 오류로 보지
않는다 — 두 퍼블리셔의 메시지가 조용히 **번갈아** 구독자에게 간다.

계측(8 초):

| | 시뮬 살아 있을 때 | 내린 뒤 |
|---|---|---|
| `board/analog` 메시지 수 | **7966** (두 놈) | 3981 (하나) |
| bd17 raw | 1190 ↔ **1527** | 1190 ± 3 |
| bd17 각도 | 0° ↔ **105.02°** | **−1.9 ~ +1.1°** |
| board 5 | 최대 **305 kPa** | 1077 mV → **101.6 kPa** |
| board 11 | 최소 **43 kPa** | 1068 mV → **101.4 kPa** |

`can_monitor.py` 가 멀쩡했던 것은 그놈이 ROS 토픽이 아니라 **CAN 을 직접** 읽기
때문이다. 그래서 "브리지가 잘못 계산한다" 로 오해하기 딱 좋았다.

**오염된 실험: 20260829_160652 / _160841 / _161411 / _161722** — 각 디렉터리에
`INVALID_시뮬레이터_혼입.txt` 를 넣었다. 직전 정상 데이터는 `_160058`.

### 고친 것

1. **양쪽 launch 파일이 서로를 막는다.** `control.launch.py` 는
   `virtual_powerpack` 이 떠 있으면, `virtual.launch.py` 는 `can_bridge_node` 가
   떠 있으면 기동을 **거부하고** 무엇을 죽여야 하는지 알려 준다.
   `pgrep -f` 는 이름만 언급한 셸 한 줄까지 잡으므로 argv[0] 의 basename 이
   정확히 일치하는 프로세스만 센다.

2. **브리지가 스스로 중복 퍼블리셔를 감시한다.** `diag_routine()` (5 초 주기) 이
   `count_publishers("board/sensors") > 1` 이면 ERROR 를 짖는다. 어떻게 띄웠든
   잡힌다 — launch 를 안 쓰고 손으로 띄운 경우까지.

3. **프레임을 한 번도 못 받은 엔코더 보드는 0° 로 낸다.** raw 0 을 그대로 역산하면
   (4125−0)/0.825 = 5000 mV 라 기본 보정에서 **246.78°** 가 나온다. 지금 board
   20·21 이 그 상태였다 (배선 없음). 그 값이 위치 제어로 흘러가면 오차 부호가
   통째로 뒤집힌다. `diag_routine` 이 이미 "CAN 수신 없음" 으로 따로 짖는다.

### 교훈

- **`ros2 node list` 에 같은 이름이 하나만 보인다고 노드가 하나인 게 아니다.**
  ROS 2 는 이름 충돌을 막지 않는다. 확인은 `ros2 topic info <토픽>` 의
  `Publisher count` 로 한다.
- **실기와 시뮬이 같은 노드 이름을 쓰는 설계는 그 자체가 지뢰다.** 편의(설정을
  안 바꾸고 갈아끼우기) 대가로 이 사고가 가능해진다. 상호 배제로 막았다.
- **`colcon build` 가 "Finished" 라고 해도 설치본이 갱신됐다는 뜻이 아니다.**
  이번 세션에서 두 번(설정 yaml, launch 파일) 설치본이 한 세대 뒤처졌다. 고친
  다음에는 `install/` 안의 파일을 직접 grep 해서 확인할 것.

---

## S-28. 캘리브레이션 단일 출처화 — 같은 값을 세 군데 손으로 복사하고 있었다 (2026-08-29)

board 17 엔코더를 재장착해 2 점을 다시 쟀다 (**raw 2114 @ 0°, raw 3392 @ 90°**,
이전 1200/2470). 그런데 이 값을 넣어야 할 곳이 셋이었다:

1. `powerpack_config.yaml`  `EncoderCalibration.boards."17"` — CanBridge 가 실제로 쓰는 것
2. `powerpack_config.yaml`  `Sensor_calibration.boards."17"` — Controller 의 별도 각도 경로
3. `scripts/can_monitor.py` `ENCODER_RAW_POINTS[17]` — CAN 을 직접 읽는 대시보드

셋이 따로 놀아서 오늘만 두 번 헤맸다. S-26 에서는 1 번이 안 먹어 팝업이 158° 를
찍는데 3 번은 0° 를 찍었고, 그래서 "브리지 계산이 틀렸다" 로 오해했다.

### 고친 것

`can_monitor.py` 가 **`powerpack_config.yaml` 을 읽어** `PRESSURE_CALIB` 과
`ENCODER_RAW_POINTS` 를 덮어쓴다. 하드코딩 표는 폴백으로 남겼다 (이 스크립트는
ROS 없이도 떠야 한다). 화면 맨 위에 어느 파일을 썼는지 찍는다:

```
 보정: /home/.../install/can_powerpack/share/can_powerpack/config/powerpack_config.yaml
```

**설치본을 먼저** 본다 — ROS 노드가 실제로 읽는 게 그것이라, 소스만 고치고 빌드를
안 한 상태에서도 "지금 돌고 있는 값" 을 보여 줘야 한다.

`can_test.py` 는 보드 구분 없이 일반 기본값(offset 1740, gain **+**0.0757) 하나로
모든 엔코더를 읽고 있었다 — 실측 보드는 gain 이 **음수**라 각도가 반대로 나온다.
`can_monitor` 에서 `ENCODER_CALIB` 을 빌려 쓰도록 바꿨다.

여전히 손으로 맞춰야 하는 것은 yaml 안의 1 번 ↔ 2 번뿐이다 (2 번은 1 번의 2 점에서
계산한 offset/gain 이다). 검산은 아래로 한다:

```
python3 - <<'EOF'
TO=3300.0/4095.0; o=lambda r:(4125.0-r*TO)/0.825
r0,r90 = 2114.0, 3392.0          # EncoderCalibration 의 2 점
print(f'offset={o(r0):.4f}  gain={90.0/(o(r90)-o(r0)):.8f}')   # → Sensor_calibration
EOF
```

### 현재 board 17

```
raw 2114 →  0.00°     raw 2753 → 45.00°     raw 3392 → 90.00°
offset 2935.0427 mV,  gain -0.07209507 deg/mV
```

---

## S-29. 양압 레퍼런스가 아예 안 나오던 이유 + 각도 계단 충격 (2026-08-29)

실기 `20260829_165306` (1 축, 2 kg · 150 mm, control_mode 2). 증상 두 가지:
70° 를 못 가고 60° 에서 오차 10° 로 주저앉는데 **양압 레퍼런스가 전 구간
대기압(101.33 kPa abs = 0 gauge)** 이었다. 그리고 각도 명령이 들어오는 순간
계단으로 튀며 충격이 갔다.

### (1) 레일 ↔ 챔버 순환 고리 — 양압이 스스로 못 깨어난다

두 상한이 서로를 참조하고 있었다:

- `decide_rail_setpoint`: 레일 SP ≤ **직전 챔버 레퍼런스 최대값** + `pos_headroom`(60)
- `build_slew_box`:      챔버 상한 ≤ **측정 레일압** − `chamber_pos_headroom`(15)

챔버 목표가 낮으면 → 레일 상한이 낮고 → 레일이 낮으면 → 챔버 천장이 낮고 →
챔버 목표가 계속 낮다. **한 번 바닥에 붙으면 스스로 못 빠져나온다.**

로그 수치가 정확히 그 상태다:

| | 값 | 뜻 |
|---|---|---|
| `p_line_pos` 평균 | 139.6 kPa abs = **38 gauge** | 레일이 안 올라감 |
| 챔버 천장 | 38 − 15 = **23 gauge** | |
| 필요한 양압 (τ 평균 2.04 N·m) | 81.6 N / 1963 mm² = **41.6 gauge** | **천장보다 높다** |
| `p_pos_ref` | 전 구간 **0 gauge** | 양압 포기 |
| `tau_ach` 최대 | **4.18 N·m** | |
| 음압 단독 한계 | 101.3 kPa × 1963 mm² × 25 mm = **4.97 N·m** | 딱 그 벽 |

즉 힘을 **전부 음압으로만** 내고 있었고, 달성 토크가 음압 단독 한계에 붙어 있었다.

**고침**: 레일 상한의 바닥에 "지금 요구되는 힘을 **양압만으로** 낼 때 필요한
압력"(`P_need = F/A`)을 깐다.

```
cap = max(직전 챔버 레퍼런스 최대, max_i F_ref[i]/Apos[i]) + pos_headroom
```

`pressure_ref_test` 1b 절로 검증 (Ø50 mm → A=1963.5 mm²):

| 수요 F | F/A | +헤드룸 60 | 고치기 전 |
|---|---|---|---|
| 0 N | 0.0 | **30.0** (하한) | 30.0 |
| 30 N | 15.3 | **75.3** | 60.0 |
| 75 N | 38.2 | **98.2** | 60.0 |
| 150 N | 76.4 | **136.4** | 60.0 |
| 300 N | 152.8 | **212.8** | 60.0 |

수요가 없으면 예전처럼 낮게 유지되므로, 이 상한을 넣은 원래 이유(레일이 필요량의
14 배로 떠 밸브가 초킹되던 것, S-1x) 는 그대로 막힌다.

### (2) 첫 명령에서 목표각이 계단으로 떨어진다

위치 루프는 명령 전까지 `angle_ref = 측정각` 으로 돌지만(오차 0 유지),
`target_angle_slewed_` 는 0 에 그대로 있었다. **첫 명령이 오는 순간** `angle_ref`
가 측정각에서 슬루 상태(0)로 계단 점프하고 거기서부터 램프가 시작됐다.

```
t=10.76  target 19.86   angle 19.86
t=10.80  target  0.45   angle 20.07     ← −19.4° 계단
t=10.84  target  1.05   angle 20.00     ← 여기서부터 15 deg/s 램프
```

**고침**: 명령 전에는 슬루 상태를 측정각에 얹어 둔다. 첫 명령에서 `angle_ref` 가
이어지고 거기서부터 램프한다.

### (3) 압력 레퍼런스에 슬루 제한이 없었다 (모드 2)

모드 1 에는 `ref_slew_kpa_per_s` 가 있는데 모드 2 에는 없어서 생성기 출력이 그대로
MPC 로 갔다. 40 ms 사이에 P⁻ 레퍼런스가 **101.3 → 51.3 → 101.0 kPa (≈1250 kPa/s)**,
P⁺ 가 101.5 → 122.1 → 105.4 로 튀었다. 챔버가 따라갈 수 없는 명령이라 밸브만
두들기고 액추에이터에는 충격으로 간다.

**고침**: `PressureRefGen.ref_slew_kpa_per_s: 150.0` 추가. 필요한 42 kPa 이동에
0.28 s 라 응답은 안 죽이고 떨림만 걸러진다. 0 이하면 끔.

### (4) 떨림의 진짜 출처 — D 항 잡음이 중력과 맞먹었다

`vel_filter_alpha: 0.3` 은 **시뮬레이터 기준 튜닝**이었다. 실기 엔코더의 고주파
잡음은 양자화(0.070°/LSB)가 아니라 그 3 배인 **1σ 0.224°** 다 (정지 구간 인접
샘플 차분으로 측정).

```
        alpha  τ[ms]   vel잡음    kd·vel    중력 2.94 N·m 대비
         0.30    6.7   ±66 dps   2.66 Nm      90%   ← 예전
         0.05   40.0   ±25 dps   1.01 Nm      34%
         0.03   66.7   ±20 dps   0.78 Nm      27%   ← 지금
```

D 항 잡음 하나가 중력 전체와 맞먹었다. 이게 `tau_ref` 를 최대 14.73 N·m 까지
튀게 하고(평균 2.04) 그대로 레퍼런스 떨림이 됐다. 지연 67 ms 는 목표 슬루
15 deg/s 에서 1° 라 무시할 만하다.

마찰 보상도 하드 `sign` 이라 오차가 0 을 지날 때마다 ±0.48 이 통째로 뒤집혀
**0.96 N·m 계단**(중력 최대의 33%)이 됐다. `friction_band_deg: 1.0` 안에서
선형으로 준다.

### 다음에 확인할 것

- 70° 도달 여부와 정착 시간. 양압이 실제로 쓰이는지는 로그의 `p_pos_ref` 가
  대기압을 벗어나는지로 본다.
- 그래도 느리면 `ref_slew_kpa_per_s` 를 올리고, 그래도 떨리면
  `vel_filter_alpha` 를 0.02 로 내린다.

---

## S-30. 하강이 계단이 되는 이유 / 45° 한계진동 / 양압이 느린 진짜 병목 (2026-08-29)

실기 `20260829_191758` (1 축, 2 kg · 150 mm). S-29 의 순환 고리 해제는 **먹혔다** —
90° 도달(최대 91.3°), `p_pos_ref` 가 최대 144.9 kPa 로 대기압을 벗어났다.
남은 세 가지를 판다.

### (1) 하강이 계단이 된다 — 붙잡기·놓기

이 시스템은 **한 방향 힘만 낸다** (`τ_ref = max(0, …)`). 하강은 오로지 중력이
시킨다. 그런데 목표는 `target_slew_deg_per_s` 15 로 계속 달아나므로 팔이 못
따라오고 오차가 −10°, −32° 까지 벌어진다. 그러면 τ_ref 가 0 으로 떨어져
**완전 방출 = 자유낙하**가 되고, 떨어지다 오차 부호가 뒤집히면 다시 붙잡는다.

```
   t   목표   각도   오차   τ_ref
 45.0  61.9  60.6   +1.3   4.00
 45.2  59.1  63.1   −4.0   1.35     ← 되튐
 47.6  23.4  33.7  −10.3   0.00     ← 자유낙하 시작
 48.0  17.7  32.3  −14.6   0.00
 48.2  14.8  29.7  −14.9   0.00     0.2 s 에 −11.7° 급강하
 48.6   8.9  25.9  −17.0   0.00
```

오차 대 τ 관계: **−1.3 → 4.00 | −5.2 → 1.41 | −10.3 → 0.00**.

**고침 — 추종 오차 제한 `target_follow_band_deg: 5.0`**. 슬루된 목표가 측정각에서
±5° 이상 벌어지지 못하게 묶는다. 목표가 팔을 **끌고 내려가므로** 하강 속도가
중력이 허용하는 만큼으로 저절로 맞춰지고, τ 가 1.4 N·m 이상 남아 방출까지 가지
않는다. 올라갈 때도 적분 와인드업과 도달 못 할 큰 수요를 막는다.
키우면 상승이 공격적, 줄이면 부드럽지만 느려진다. 0 이하면 끔.

### (2) 45° 부근 한계진동 — D 항이 지배하고 있었다

t=16~21 에서 **1.45 Hz, p-p 22.9°** 의 한계진동. τ_ref 가 **0.00 ↔ 4.51 N·m** 로
뱅뱅했다 (45° 중력은 2.08 N·m). t=21 이후 ±0.5° 로 잦아들긴 하는데 5 초가 걸린다.

그 진동에서 각 항의 크기:

| 항 | 크기 | 비고 |
|---|---|---|
| `kd·vel` | 0.04 × 64 dps = **2.55 N·m** | |
| `kp·err` | 0.0786 × 7° = **0.55 N·m** | D 가 **4.6 배** |
| 중력 | 2.08 N·m | D 하나가 중력을 지운다 |

D 는 원래 위상 앞섬으로 감쇠를 주는데, 1.45 Hz 에서는 속도 LPF(τ 67 ms)만으로도
**31°** 가 밀리고 압력 루프 지연이 더해져 앞섬이 이미 깎여 있다. 감쇠가 아니라
지연된 고이득 되먹임으로 작동해 진동을 키웠다. → **kd 0.04 → 0.02**.

(kd=0.04 는 시뮬레이터 오버슈트 튜닝에서 나온 값이다. S-29 의 `vel_filter_alpha`
와 같은 계보 — 시뮬에는 엔코더 잡음도 압력 루프 지연도 없다.)

### (3) 양압이 느린 진짜 병목 — 레일이 아니라 **탱크가 없다**

레퍼런스 생성기는 이제 제대로 요구하고 있다. 막는 것은 **공급**이다.

| | |
|---|---|
| 레퍼런스가 상한(`ub_pos`)에 붙어 있던 비율 | **36.9%** |
| 상한이 필요압보다 낮았던 비율 | **56.2%** |
| 고수요 구간(t 27~40) | `p_pos_ref` == `ub_pos` **매 샘플** |
| 그때 필요압 | 200~222 kPa abs |
| 그때 상한 | 102~132 kPa abs |
| `starve_pos` | ~0% — **유량이 아니라 천장이 막는다** |

천장은 `측정 레일압 − chamber_pos_headroom(15)` 이다. 레일 실측은
최소 97.7 / 평균 152.5 / 최대 318.1 kPa abs — **챔버가 빨기 시작하면 105 로
주저앉는다**. 놀 때는 300 까지 오르므로 펌프가 못 만드는 게 아니라 **받아 둘
곳이 없다**.

`p_macro_pos` 가 전 구간 **98.1 kPa (대기압)** 이다 — **매크로 탱크가 미연결이다.**
부스트 경로(v3macro)가 죽어 있어 모든 과도 유량을 작은 레일 하나가 감당한다.
`tank_low` 경고도 계속 떠 있었다.

→ **이건 하드웨어다. 탱크를 연결하고 충전해야 한다.** 제어로 더 짜낼 여지는
`chamber_pos_headroom` 을 줄이거나 `supply_filter_tau_s` 를 키워 순간 새그를
무시하는 정도인데, 둘 다 "레일이 못 주는 압력을 목표로 삼는" 예전 문제(S-1x)로
되돌아가는 방향이라 손대지 않았다.

### (4) 로그에 rail SP 를 남긴다 (schema 5)

`refgen_dbg` 말미 공용 6 개
(`rail_pos_sp`, `rail_neg_sp`, `tank`, `tank_low`, `boost g/s`, `eject g/s`)를
CSV 로 뺀다. 이게 없으면 "양압이 왜 느린가" 를 로그만으로 못 가른다 —
셋포인트가 낮은 것인지(제어), 셋포인트는 높은데 레일이 못 따라오는 것인지(공급)
구분이 안 된다. 말미는 축 수에 따라 위치가 달라지므로 **뒤에서** 읽는다.

### (5) 2 축 준비

`axis:=` 가 쉼표 목록을 받는다: **`axis:=0,1`** → `num_actuators=2`,
axis0=ch0/ch6/board17, axis1=ch1/ch7/board18. 브리지에도 넘어가므로 board 19~22
에 대한 헛경고가 안 뜬다.

`PositionController.axis1.mass_kg` 를 5.0 → **2.0** 으로 맞췄다 —
**axis0 과 같은 리그(150 mm · 2 kg)로 가정**한 것이다. 2 축 하중이 다르면 반드시
고칠 것. 중력 FF 는 목표 압력에 거의 선형으로 들어간다.

board 18 엔코더 2 점이 `raw 1120/2350` 으로 바뀌어 있었는데
`Sensor_calibration.boards.18` 은 옛 값(raw 860/2000 기준)이었다 —
`offset 3905.9829, gain -0.07490854` 로 동기화했다 (S-28 의 그 두 번째 사본).

실기 2 축 기동 확인:
```
엔코더 board 17: offset 2935.04 gain -0.072095 | board 18: offset 3905.98 gain -0.074909
[PosCtrl axis0] m=2.0kg L=0.150m | gid: pos=0 neg=6 enc=0
[PosCtrl axis1] m=2.0kg L=0.150m | gid: pos=1 neg=7 enc=1
PressureRefGen: N=2
```

---

## S-31. 매크로 연결 후 결과 / 레퍼런스가 목표까지 안 가던 버그 / 하강 계단 (2026-08-29)

### 매크로를 연결하니 양압 병목이 풀렸다 (S-30 (3) 확인)

실기 `20260829_193431` (2 축, 각 2 kg · 150 mm, 탱크 ~580 kPa):

| | S-30 (탱크 없음) | 지금 (탱크 580 kPa) |
|---|---|---|
| `p_pos_ref` | 101~145 (천장에 붙음) | **112~150** |
| `ub_pos` (천장) | 102~132 | **150~185** — 더 이상 안 막는다 |
| rail SP / 실측 | 148 / 106~168 | **168~250 / 127~245** |
| 90° 도달 | 60° 에서 주저앉음 | **ax0 89.9° / ax1 88.6°** |

두 축 모두 잘 따라간다. **진단이 맞았다 — 제어가 아니라 공급이었다.**

### 버그: 목표가 명령까지 안 간다

`80° → 0°` 를 주면 목표가 **9° 에서 멈춘다.** S-30 에서 넣은
`target_follow_band_deg` 가 **목표(`target_angle_slewed_`) 자체**를 측정각 ±5° 로
묶었기 때문이다. 팔이 마찰로 14° 에 서면 목표도 9° 에서 같이 얼어붙는다.

실기 `20260829_193558` 에서 `angle − target` 이 **정확히 +5.00** 에 붙어 있다:

```
 t     목표    각도    차
17.7   13.48   18.38  +4.90
22.7   10.35   15.35  +5.00
30.3    9.86   14.86  +5.00
32.8    9.48   14.44  +4.95     ← 목표가 0 에 못 간다
```

**고침**: 목표는 건드리지 않고 **제어기에 들어가는 오차만** 밴드로 자른다.
밴드의 목적(적분 와인드업·도달 못 할 큰 수요 차단)은 오차에서 자르면 충분하다.
검증(시뮬, 80°→0°): 팔은 10.07° 에 멈추지만 **목표는 0.00° 까지 간다**.

### 하강 계단의 정체 — stiction 이 아니라 **자유낙하**다

`20260829_193558` 하강(t 8~18), 0.1 s 창 속도:

```
평균 −6.5 dps, 범위 −63.8 ~ +14.1, 목표 슬루의 최대 4.3 배
하강 중인데 위로 되튀는(속도 > +2) 비율 15%
```

τ 수지를 보면 스틱이 아니다. 50° 에서 중력은 2.25 N·m 인데 명령 τ 는 1.0~1.4 뿐:

| 항 | 값 |
|---|---|
| `kp·err` (err = −5, 밴드 포화) | −0.39 |
| `ki·I` (한계 포화) | −0.75 |
| `τ_fric` | −0.48 |
| `τ_grav` | +2.25 |
| **합** | **0.63 ~ 1.4** |

부족분 ~1 N·m 가 팔(관성 ≈ m·L² = 0.045 kg·m²)을 **1500 deg/s²** 로 가속한다 —
0.1 s 면 150 dps 다. 그래서 미끄러지고, PID 가 뒤늦게 붙잡고(τ 1.2 → 3.5 N·m),
챔버는 그 catch 를 못 따라오고(목표 156 kPa 인데 실측 128), 되튄다.

**고침 — D 항 목표속도 피드포워드 `kd_vel_ff: 1.0`**

예전 `−kd·vel` 은 **명령한 움직임까지 억제**했다. 목표가 −15 deg/s 로 내려가라
하는데 팔이 그대로 내려가면 D 가 +0.3 N·m 로 붙잡는다. 못 내려가다 중력이 이겨
−45 로 미끄러지면 D 가 뒤늦게 +0.9 로 튀어 붙잡는다 — 계단의 정체다.

`−kd·(vel − 목표속도)` 로 바꾸면 **명령한 속도로 가는 동안 D 는 0** 이고 벗어난
만큼만 억제한다. 모드 1·2 양쪽에 넣었다. 0 이면 예전 동작.

목표 슬루도 **15 → 25 deg/s** (사용자 요청 — 기울기는 더 가팔라도 된다).
부드러움은 이제 슬루를 늦추는 게 아니라 속도 피드포워드가 맡는다.

### 다음에 볼 것

- 하강 속도의 진폭. 0.1 s 창 속도가 −63.8 ~ +14.1 이던 것이 목표 −25 근처로
  모이는지, "위로 되튀는 비율 15%" 가 줄어드는지가 지표다.
- 그래도 되튀면 `kd` 를 0.02 → 0.03~0.04 로 **올린다** (이제 D 가 명령한 움직임을
  안 막으므로 예전처럼 진동을 키우지 않는다).
- 여전히 목표에 못 서면 `friction_nm`(0.48) 실측을 다시 볼 것 — 14° 에서 서는
  것은 중력 0.71 N·m 를 마찰이 이긴다는 뜻이다.

---

## S-32. axis1 이 더 진동한 이유 — 레이트 리미터가 그 축에서만 포화하고 있었다 (2026-08-29)

실기 `20260829_194658` (2 축, 커밋 f90a92c). S-31 의 버그 수정은 **먹혔다** —
`80° → 0°` 에서 팔은 15° 에 서지만 **목표는 0.00° 까지 간다**.

### 두 축은 게인·질량·기하가 같은데 진폭이 다르다

목표 60° 유지 구간(t 20.5~30.5):

| | axis0 | axis1 |
|---|---|---|
| 각도 p-p | 18.5° (1.07 Hz) | **27.2°** (0.93 Hz) |
| 속도 표준편차 | 11.1 dps | **30.8 dps** |
| `P⁺` 실측 p-p | 21.3 kPa | **32.5 kPa** |

**엔코더 잡음은 거의 같다** (board 17 = 0.135° / board 18 = 0.154° 1σ) — 원인이 아니다.
**힘 수지도 양쪽 다 맞는다** (정지 구간에서 τ(압력) − τ(중력) = −0.31 / −0.23 N·m,
차이는 마찰) — 엔코더 스케일·기하 문제도 아니다.

### 다른 것은 하나뿐이었다 — 레퍼런스 레이트 리미터에 걸리는 비율

S-29 에서 넣은 `ref_slew_kpa_per_s: 150` 이 **정상 동작에서 이미 포화하고 있었다.**

```
                    제한의 90% 이상에 걸린 비율        각도 p-p
  전 구간   axis0 P⁺   6.2%      axis1 P⁺  18.0%
  진동 구간 axis0 P⁺   7.2%      axis1 P⁺  27.5%      18.5° vs 27.2°
```

되먹임 고리 안의 **포화된 레이트 리미터는 최대 90° 의 위상 지연**을 만든다 —
한계진동의 교과서적 원인이다. 리미터에 두 배 넘게 걸리는 축이 진폭도 1.5 배다.

**고침**: `ref_slew_kpa_per_s` **150 → 400**. 억제하려던 떨림은 1250 kPa/s 였고
정상 수요는 평균 30~55 / 최대 183 kPa/s 다 — 400 이면 정상 수요에 2 배 여유를
두면서 떨림은 여전히 3 배로 자른다.

### 남은 의심 — ch1 의 밸브 크래킹 (미해결)

채널별로 다른 파라미터는 `micro/C_k` **하나뿐**이다:

```
ch0 0.16472   ch1 0.18672   ch2 0.18172   ch3 0.17272   ch4 0.16972   ch5 0.17372
```

`k_shape = 284.5` 라 시그모이드가 5%→95% 로 열리는 폭이 `2·ln(19)/284.5 ≈ 0.0207`
이다. **ch1 과 ch0 의 차이 0.0220 은 딱 한 번의 전개 폭 전체다** — 즉 모델이
"완전히 닫힘" 이라고 보는 지점에서 실제로는 "완전히 열림" 일 수 있다.
C_k 가 실제보다 높으면 컨트롤러가 필요보다 많은 전류를 내 과개방한다 → 게인이
커진다 → axis1 이 더 흔들린다. 방향이 맞다.

다만 이건 **피팅된 물리값**이라 추측으로 고치지 않았다. 확정하려면 ch1 하나만
띄워 램프 시험을 해야 한다:

```
ros2 launch can_powerpack control.launch.py axis:=1
python3 scripts/valve_fit_from_log.py <최근 로그>
```

### 3 축 확장

`axis:=0,1,2` — `PositionController.axis2.mass_kg` 5.0 → 2.0 (axis0·1 과 같은 리그).
board 19 의 2 점은 사용자가 `raw 160 / 1444` 로 넣었다. 검산하면 기울기가
`(4843.7−3589.3)/90 = 13.9 mV/deg` 로 board 17 의 `1248/90 = 13.9` 와 같다 — 정상.

기동 확인:
```
엔코더 board 17 -0.072095 | board 18 -0.074909 | board 19 -0.071758  (모두 실측)
[PosCtrl axis0] gid: pos=0 neg=6 enc=0    axis1: pos=1 neg=7 enc=1    axis2: pos=2 neg=8 enc=2
PressureRefGen: N=3
```

### 엔코더 보정 사본을 없앴다 (두 번 사고 낸 그것)

`Sensor_calibration.boards."17"~"22"` 를 **지웠다.** 이건 처음부터 죽은 설정이었다 —
컨트롤러는 `board/sensors`(보드 1~16)만 받으므로 `snap_sensors[16..21]` 은 언제나
0 이고, 실제 각도는 CanBridge 가 `board/analog` 로 낸다. 그런데도 이 사본이
`EncoderCalibration` 과 어긋나 두 번 사고를 냈다 (S-28 board 17, S-30 board 18).

이제 **엔코더를 새로 달면 `EncoderCalibration` 의 `raw_0deg/raw_90deg` 한 곳만**
고치면 된다. `can_monitor.py` 는 S-28 이후 그 yaml 을 읽는다.
컨트롤러의 죽은 분기는 `filt_out_ = 0` 으로 바꿔 "이 슬롯엔 압력이 없다" 를
분명히 했다.

---

## S-33. axis1 만 진동하는 진짜 이유 — ch1 밸브 실효 이득이 5 배다 (2026-08-29)

실기 `20260829_195910` (3 축, 커밋 52bb0e4). S-32 의 레이트 리미터 완화는 **먹혔다** —
제한의 90% 이상에 걸린 비율이 axis1 에서 27.5% → **3.1%** 로 떨어졌다.
그런데 axis1 은 여전히 혼자 진동했다.

### 같은 조건에서 axis1 만 5 배 흔들린다

목표 60° 유지 (t 29~35), 세 축 모두 같은 게인·질량·기하:

| | axis0 | **axis1** | axis2 |
|---|---|---|---|
| 각도 p-p | 4.58° | **21.95°** | 3.15° |
| P⁺ 실측 p-p | 17.8 kPa | **31.3** | 18.8 |
| τ_ref p-p | 2.18 N·m | **4.14** | 1.51 |

**배제한 것들** — 전부 아니었다:
- 엔코더 잡음: 0.135 / 0.154 / — 1σ, 거의 같다
- 마찰: 정지 구간 τ(중력) − τ(압력) = 0.25 / 0.27 / 0.15 N·m — axis1 이 오히려 큰 쪽
- 챔버 상한 포화: 세 축 모두 **0.0%** (레퍼런스가 상한에 안 붙었다)
- 레일 리플: 공유 신호다. line⁺ 가 p-p 123 kPa @1.2 Hz 로 흔들리고 세 축 레퍼런스에
  30~43 kPa 로 돌아오는데, **axis0·2 는 그걸 보고도 4.6°/3.2° 밖에 안 움직인다.**
  공유 외란으로는 한 축만 진동하는 걸 설명 못 한다.

### 측정: 채널별 밸브 실효 이득

지령 15% 이상 · 반대 밸브 닫힘 · 차압 5 kPa 이상인 구간에서
`(dP/dt) / 지령% / 차압kPa` 의 중앙값 (n ≈ 1000~1600):

| | 양압 충전 | 양압 배기 | 음압 흡입 | 음압 대기유입 |
|---|---|---|---|---|
| axis0 | 0.0652 | 0.2327 | −0.0007 | 0.0511 |
| **axis1** | **0.3243 (4.97×)** | **1.9869 (8.5×)** | **0.6621** | **2.7073 (53×)** |
| axis2 | 0.0702 (1.08×) | 0.6671 (2.9×) | 0.0150 | 0.2319 |

**ch1 은 같은 지령·차압에서 ch0 보다 5 배 빠르게 압력이 변한다.** 양압·음압 양쪽 다.

`dP/dt = ṁ·R·T/V` 이므로 이득비가 곧 **유효 부피비**다. 기하 모델은 세 축을 모두
146 mL 로 보는데 axis1 은 그 **1/5** 로 반응한다 → MPC 가 필요량의 5 배 유량을
요구 → 과개방 → 진동. 이건 HANDOFF S-20 이 예고한 바로 그 고장 형태다.

### 고침 — 채널별 부피 **배율**

`channel_config.ch1.volume_scale: 0.201` (= 1/4.97), `ch7` 동일.

절대 오버라이드(`volume_ml`)로는 안 된다. 액추에이터가 붙어 있으면 부피가 각도의
함수라 매 틱 기하 모델이 덮어쓴다 — 시작할 때 넣은 값이 지워진다. 배율이면 각도
의존성은 그대로 두고 크기만 고친다. 기동 로그에서 확인:

```
[채널 부피 mL] ch0=50 ch1=30(x0.20) ch2=50 ... ch7=42(x0.20) ...
```

### **배관을 눈으로 확인할 것**

같은 리그 세 개인데 유효 부피가 5 배 차이나는 것은 제어로 설명할 일이 아니다.
axis1 의 튜브 길이·피팅·액추에이터가 다른 둘과 같은지 봐야 한다. 배율은 **증상을
맞춰 놓은 것**이지 원인을 고친 것이 아니다.

덤으로 나온 것: **axis0 의 음압 흡입 이득이 −0.0007 로 사실상 0 이다.**
그 채널은 흡입 밸브를 열어도 압력이 안 내려간다 — 막혔거나 안 연결됐다.
axis0 의 음압 배관도 같이 볼 것.

### 시도했으나 안 켠 것 — 레일 셋포인트 엔벨로프

레일 SP 가 τ 리플을 그대로 물고 있는 것은 사실이다 (τ 2.0~4.1 N·m p-p →
need = τ/(reel·A) = 41~84 kPa p-p, 실측 SP 리플 68.5 kPa @1.73 Hz).
`PressureRefGen.rail.sp_decay_tau_s` 로 "올림 즉시 / 내림 감쇠" 엔벨로프를 넣었는데,
실측 SP 신호에 통과시켜 보니 리플이 68.5 → 47 kPa (감쇠 31%) 밖에 안 줄었다 —
상승 스파이크를 그대로 따라가기 때문이다. 그리고 위에서 봤듯 레일 리플은 axis1
진동의 원인이 아니었다. **기본 0(끔)** 으로 두고 코드만 남겼다.

---

## S-34. **액추에이터 파손 사고** — CAN 송신 큐에 쌓인 묵은 지령이 쏟아졌다 (2026-08-29)

실기 `20260829_201659` (3 축). 팔 세 개가 스토퍼를 넘어 **129° / 136° / 276°** 까지
가서 액추에이터가 파손됐다. 정상 가동 범위는 0~105° 다.

### 사고 경위 — 로그가 그대로 보여 준다

**1. CAN 이 8.5 초 늦게 올라왔다.** 그동안 `board/sensors` 가 전부 raw 0 이었다.

**2. 컨트롤러는 그 0 을 진짜 압력으로 믿었다.**
`kpa(board1, 0) = (0 − 1112) × 0.25 + 101.325 = **−176.7 kPa**`.
로그의 `p_line_pos_kpa` 가 t=0~8.5 구간에서 정확히 −176.7 이다 (탱크 −165, 음압 127.1
— 전부 raw 0 의 환산값과 일치). 물리적으로 불가능한 값인데 아무도 안 막았다.

**3. 그래서 "세게 채워라" 를 계속 냈다.** 챔버가 대기압보다 278 kPa 낮다고 봤으니
당연하다. micro 30% + **macro 62%** (탱크 580 kPa → 챔버).

**4. `canWrite` 는 비동기인데 반환값을 안 봤다.** 버스가 죽어 있으니 프레임이 ACK 를
못 받고 드라이버 송신 큐에 **쌓이기만** 했다. 아무 신호도 없었다.

**5. CAN 이 살아나자 backlog 가 순서대로 쏟아졌다.** 전류가 증거다:

```
   t         macro 지령    macro 실측 전류        챔버 P⁺
 8.5~10.66     62%          0.3 mA (≈0%)         100 kPa   ← 아무것도 안 나갔다
10.74~11.46     0%        155 mA (=61.9%)     100→228 kPa  ← 2 초 전 지령이 실행됐다
```
155 mA ÷ 2.505 mA/% = **61.9%** — 정확히 2 초 전의 62% 다.
micro 도 73.5 mA = 29.3% 로 2 초 전 30% 와 일치한다.

**6. 과압 세이프티는 소프트웨어에서 제대로 걸렸다.** t≈10.98 에 래치되어
`micro 0 / 배기 4095 / macro 0` 을 냈다. **그런데 그 지령도 같은 큐 뒤에 서 있었다.**
배기 전류가 실제로 흐른 것은 t=11.54 — **0.8 초 늦었다.** 그 사이 탱크가
580 → 405 kPa 로 빠지며 챔버가 228 kPa 까지 올랐고 팔이 스토퍼를 넘었다.

### 왜 아무 방어도 안 걸렸나 — 구멍 네 개

| # | 구멍 | 상태 |
|---|---|---|
| 1 | `canWrite` 반환값을 버렸다 | 큐 넘침(`canERR_TXBUFOFL`)이 조용히 진행 |
| 2 | 송신 큐를 안 비웠다 | 늦게 도착한 setpoint 가 그대로 실행됨 |
| 3 | RX 워치독이 `if (last != 0)` 로 감싸여 있었다 | **한 번도 못 받은 경우**를 통째로 건너뜀 |
| 4 | raw 0 을 압력으로 환산했다 | −176.7 kPa 를 믿고 밸브를 열었다 |

`sensor_zeroed_{true}` 가 기본값이라 기동 0 점 보정도 안 돈다 — 거기에 방어를
넣어 봐야 실행되지 않는다 (실제로 처음에 그 자리에 넣었다가 안 걸리는 걸 확인했다).

### 고친 것

**A. 송신 큐를 매번 비우고 반환값을 본다** (`CanBridge::tx_send`)
```cpp
canIoCtl(hnd_, canIOCTL_FLUSH_TX_BUFFER, &dummy, sizeof(dummy));
tx_check(canWrite(...), 1);
```
이 스트림은 **주기적 setpoint** 다. 늦게 도착한 setpoint 는 쓸모없는 정도가 아니라
위험하다. 항상 최신 것 하나만 실어 보낸다. 송신 실패는 연속 횟수와 함께 ERROR.

**B. RX 워치독이 기동 구간을 덮는다** — 기준을 `node_start_ns_` 로 두어
"한 번도 안 왔다" 도 200 ms 뒤 안전 상태로 간다. 메시지도 구분해서 찍는다.

**C. 센서가 유효해지기 전에는 제어를 시작하지 않는다** (`Controller`)
`sensor_seen_[16]` 을 두고, 필요한 보드(라인 4 + 활성 채널) 가 하나라도 아직
프레임을 안 보냈으면 **제어 루프를 return 한다.** PWM 이 안 나가므로 브리지의
PWM 워치독이 안전 상태로 간다. 0 kPa 로 붙잡는 것도 안 된다 — 그것도 진공이라
똑같이 채우려 든다.

**D. raw 0 은 압력이 아니다** — 환산하지 않고 직전 값을 유지하며 ERROR.

검증 (지금 CAN 이 실제로 죽어 있어 그대로 재현된다):
```
제어 출력(RefGen) 횟수 : 32 → **0**
센서 대기: board 1, 2, 3, 4, 5, 6, 7, 11, 12, 13     ← axis:=0,1,2 에 필요한 보드 정확히
CAN 수신 워치독: 200 ms 동안 보드 프레임이 **한 번도** 없었다
```

### 지금 하드웨어 상태 — **보드가 하나도 응답하지 않는다**

```
CAN 수신 []
CAN 수신 없음: board 1 ~ 22 (전부)
```
파손 이후 보드 전원이 꺼졌거나 배선이 빠진 것으로 보인다. 재가동 전에 확인할 것.

### 남은 숙제

- **기계적 스토퍼가 소프트웨어보다 먼저 서야 한다.** 이번엔 소프트웨어 세이프티가
  제때 걸렸는데도(래치 자체는 정확했다) 전달이 늦어 부서졌다. 각도 105° 를
  물리적으로 막는 스토퍼가 있어야 이 계층 하나가 실패해도 팔이 안 부서진다.
- 압력 세이프티 한계 190 kPa 는 챔버가 228 kPa 까지 간 뒤 의미가 없다. 탱크
  580 kPa 가 직결되는 구조라면 **macro 밸브 앞단에 기계식 릴리프**를 두는 것이
  전기 제어보다 확실하다.

---

## S-35. **CAN 을 죽인 것은 내가 넣은 TX 플러시였다** (2026-08-29)

S-34 에서 "묵은 지령이 큐에 쌓였다가 쏟아진다" 를 막으려고 `tx_send()` 마다
`canIOCTL_FLUSH_TX_BUFFER` 로 송신 큐를 비우게 했다. **그게 버스를 죽였다.**
제어기를 켜면 CAN 이 죽고 `can_monitor.py`(수신 전용)로는 멀쩡한 것이 그 증상이다.

### A/B 계측 (브리지 단독, 같은 배선, 5 초 창)

| | 첫 창 | 다음 창 |
|---|---|---|
| FLUSH **있음** | **31 Hz** | **완전 사망 (`CAN 수신 []`)** |
| FLUSH **없음** | 265~372 Hz | 265~359 Hz |
| **새 대책** | **435~448 Hz** | 435~448 Hz (3 창 연속) |

`canWrite` 는 내내 `canOK` 를 반환했다 — **드라이버는 받아들이는데 버스가 죽는다.**
500 Hz 로 전송 중인 64 바이트 FD 프레임을 중간에 잘라내면 에러 프레임이 쏟아지고,
보드들의 송신까지 무너져 결국 아무것도 안 온다. `can_monitor.py` 는 수신만 하므로
자기가 원인이 아닌 이 붕괴를 못 보고, "모니터는 되는데 제어기는 안 된다" 로 보인다.

### 올바른 대책 — 큐를 비우지 말고 **애초에 안 쌓이게 한다**

backlog 는 **버스가 죽은 동안 계속 밀어 넣어서** 생긴다. 그러니 밀어 넣지 않으면 된다.

```cpp
if (rx_stale_) {            // 수신 워치독이 두절로 판정한 상태
  // 밸브 지령 송신을 멈춘다. 수신이 복구되면 자동 재개.
  return;                   // ← 버스는 전혀 건드리지 않는다
}
```

이러면 밀리는 양이 **워치독 시한(기본 200 ms)** 으로 묶인다 — 사고 때의 8.5 초가
아니라. 큐 잔량은 `canIOCTL_GET_TX_BUFFER_LEVEL` 로 **읽기만** 해서(버스에 영향
없음) 8 프레임 넘게 밀리면 ERROR 를 낸다. `canWrite` 반환값 확인은 그대로 둔다.

### 교훈

- **송신 큐를 강제로 비우는 것은 버스에 대한 간섭이다.** 진행 중인 프레임을
  잘라내면 에러 프레임이 된다. 큐를 관리하려면 "넣지 않는" 쪽으로 해야 한다.
- **`canWrite` 가 `canOK` 라는 것은 버스가 건강하다는 뜻이 아니다.** 드라이버가
  큐에 받아 줬다는 뜻뿐이다. 버스 건강은 **수신 주파수**로 본다.
- 안전장치를 넣을 때 그 장치가 만드는 새 위험을 같이 재야 한다. 이번에는 실기에서
  A/B 를 돌리기 전까지 몰랐다.

전체 스택(브리지 + 컨트롤러 + 로거) 확인: RX 435~448 Hz, 송신 실패 0, 송신 중단 0,
센서 게이트 오탐 0, 제어 정상 동작.

---

## S-36. 기동 목표 0° + 음압 하한이 대기압을 넘던 버그 (2026-08-29)

### 기동 시 전 축 0° 로 내려간다

예전에는 TCP 명령이 올 때까지 `angle_ref = 측정각` 이라 목표가 팔을 따라다니기만
했다 (`default_angle_deg: 0.0` 이 적용되지 않았다). 이제 슬루 상태를 **현재 각도에서
한 번만** 출발시키고 곧바로 0° 까지 램프한다 — 계단 없이 홈으로 내려간다.

```
기동 목표: 전 축 0.0° 로 내려간다 (25 deg/s). 현재 각도에서 출발: ax0=5.7° ax1=6.4° ax2=6.8°
θ=5.85→0.00°  θ=5.78→0.00°  ...
```

엔코더가 아직 안 들어왔으면(전부 0) 씨앗 심기를 다음 틱으로 미룬다 — 0 에서 출발했다가
진짜 각도가 들어오는 순간 계단이 되는 것을 막는다 (1 s 지나면 0 으로 확정).

### 버그: 음압 챔버 하한이 **대기압보다 높았다**

실기 `20260829_215726` — 90°/90°/90° 를 줬는데 `P⁺` 레퍼런스가 전 구간 대기압,
`P⁻` 가 **116.3 kPa** 고정이었다. 116.3 = 101.325 + 15 다.

```cpp
loN = std::max(loN, Pnp + p_.chamber_neg_headroom);   // ← 옛 식
```
음압레일이 배기되지 않은 상태(`Pnp` ≈ 0 gauge)면 `loN = 0 + 15 kPa = +15 kPa gauge`
= 116.3 kPa abs 가 된다. `hiN = min(0, …)` 이 그보다 낮으므로
`if (hiN < loN) hiN = loN` 에 걸려 박스가 **[+15, +15]** 로 붙박인다.

음압 챔버가 대기압보다 높으면 힘을 **빼는** 쪽으로 작용한다 (F = P⁺·A − P⁻·A).
쓸모없는 정도가 아니라 해롭다.

```cpp
loN = std::max(loN, std::min(0.0, Pnp + p_.chamber_neg_headroom));   // 고친 식
```
레일이 진짜 음압일 때만 이 하한이 살아난다 (−74.3 kPa 이면 하한 42.0 kPa abs, 그대로).
검증: `P⁻=116.3` → **`P⁻=101.3`**.

### 다만 그 실험이 안 된 **주된** 이유는 공급이 없어서다

```
p_line_pos  100.6 kPa   p_line_neg  101.3 kPa   tank  98.6 kPa   (전부 대기압)
```
컴프레서/펌프가 돌지 않았다. 공기가 없으면 챔버 경계가 대기압으로 닫히는 것이
정상 동작이다 — 레퍼런스 생성기는 못 만드는 압력을 목표로 삼지 않는다.
τ_ref 는 1.9 N·m 로 정확히 요구하고 있었고 레일 셋포인트도 205 kPa 로 올라가 있었다.

컨트롤러는 이미 정확히 짖고 있었다:
```
[공급 부족] 탱크 99 kPa — 운전 하한 551 kPa 미만이다. macro 부스트를 쓸 수 없다.
컴프레서/펌프를 확인할 것. 이 상태의 로그는 밸브 피팅에 쓰면 안 된다.
```
라인 밸브가 워치독에 잡혀 열려 있던 것도 아니다 (board1 v1 = 567/4095, PID 가
정상 변조 중). 워치독·안전상태·송신중단 발생 0.

---

## S-37. axis1 진동 — 원인은 채널별 **크래킹 점(C_k)** 이었다 (2026-08-29)

액추에이터를 떼고 끝을 막은 **고정부피 3 축 시험** (`20260829_220606`, 공급 정상:
line⁺ 169 / line⁻ 42 / tank 566 kPa). 이 구성이 결정적이다 — 세 채널의 물리 부피가
같아져서 채널 차이가 배관이 아니라 **밸브**에서 온다는 것이 드러난다.

### 레퍼런스는 같은데 실측만 다르다

| | P⁺ 목표 p-p | P⁺ 실측 p-p | 추종 오차 σ | micro 평균 | atm 평균 | 60% 초과 개방 |
|---|---|---|---|---|---|---|
| axis0 | 22.6 | 31.8 | **2.26 kPa** | 4.4% | 5.6% | **0.1%** |
| **axis1** | 22.5 | **62.7** (2.53 Hz) | **7.92** | 12.8% | 24.9% | **36.6%** |
| axis2 | 23.5 | 42.2 (1.21 Hz) | 4.77 | 8.9% | 15.6% | 13.1% |

레퍼런스는 사실상 동일한데 axis1 의 밸브만 **3~4 배 세게** 구동되고 36.6% 의 시간을
60% 넘게 열고 있다. 전형적인 과이득 내층이다.

### 소프트웨어에서 채널을 다르게 만드는 것은 딱 두 개였다

`channel_config` 의 내층 게인(k0/k1/k2, pos_ki_*)은 **12 채널 전부 동일**하다.
다른 것은 (a) 내가 넣은 `volume_scale`, (b) `valve_params.yaml` 의 `micro.C_k` 뿐.

**C_k 가 오차 순위와 정확히 일치한다:**

```
micro k_shape = 284.54 → 시그모이드 5%→95% 전개 폭 = 2·ln(19)/k = 0.0207

  채널      C_k      ch0 대비   전개폭 배수   오차 σ
  ch0    0.164721   +0.00000      0.00      2.26
  ch2    0.181722   +0.01700      0.82      4.77
  ch1    0.186722   +0.02200      1.06      7.92   ← 전개 폭 하나를 통째로 넘었다
```

`F_net = I + C_z·z + C_p·P_in − C_k` 다. **C_k 가 크면 모델은 "더 많은 전류를 줘야
열린다" 고 본다** → `valve_invert` 가 더 큰 지령을 낸다 → 실제 밸브는 이미 열려
있으므로 과공급 → 오버슈트 → 한계진동. 방향·순위·크기가 전부 맞는다.

이 값들은 **"무유량 상한"** 으로 쟀다 (S-1x) — 유량이 안 보이던 가장 높은 지령.
상한이므로 **체계적으로 과대평가**되고, 그 편향이 큰 채널일수록 더 흔들린다.

**고침**: 양압 6 채널의 `micro.C_k` 를 실측 최소값 `0.16472150` (ch0) 으로 통일했다.
모두 같은 방법으로 쟀으니 최소값이 가장 덜 부풀려진 값이고, 과소평가는 **덜 여는**
쪽(느려짐)으로 틀리므로 안전한 방향이다. 원래 값은 각 줄 주석에 남겼다.

### 내가 넣었던 `volume_scale` 은 되돌렸다

S-33 에서 ch1/ch7 에 `volume_scale: 0.201` 을 넣었다. **되돌린다.**

- 근거였던 이득 측정이 **진동 중에는 신뢰할 수 없다.** 같은 방법을 이번 로그에
  엄격한 조건으로 다시 돌리니 IQR 이 값보다 넓었다 (axis1 충전 중앙값 0.39,
  IQR [−0.08, 6.38]). dP/dt 가 밸브가 아니라 진동에 지배된다.
- 이번 시험은 끝을 막아 **세 채널의 물리 부피가 같다.** 그런데도 모델은 axis1 만
  27 mL, 나머지는 133/135 mL 였다 — 물리와 어긋나는 쪽으로 벌려 놓은 셈이다.
- 부피를 작게 잡으면 피드포워드 유량(`m_dot = Ṗ·V/RT`)이 그만큼 줄어 챔버가 느리게
  차고, 그 오차를 적분항(`pos_ki_micro 0.35`, `pos_ki_atm 0.60`)이 메우며 감아올린다
  → 지령 포화 → 오버슈트. 관측된 "평균 지령 3~4 배, 60% 초과 36.6%" 와 같은 모양이다.

교훈: **채널별로 하나씩만 바꿔야 한다.** C_k 와 volume 을 동시에 다르게 둔 탓에
어느 쪽이 원인인지 두 세션을 헤맸다.

### 덤 — 송신 큐 경고 임계값 오탐

S-35 에서 넣은 `canIOCTL_GET_TX_BUFFER_LEVEL > 8` 이 매 실행마다 짖었다. 500 Hz ×
2 프레임 = 1000 프레임/s 이므로 9 프레임은 **9 ms**, 정상 버퍼링이다. 사고 때
문제가 된 backlog 는 2 초(≈2000 프레임)였다. 임계를 **100 프레임(≈100 ms)** 으로
올렸다. 확인: 오탐 0, CAN 265~276 Hz 정상.
