# 이 브랜치 (`DMY_actuator_variance`) 에서 하는 일

**액추에이터 6개의 개체차를 없앤다.** 첫 단계는 개체차를 **재는** 것이다.

> 축마다 목표 압력을 컨트롤러에 주고, 압력을 추종시키고, 그때의 엔코더 각도를
> 로깅한다. **위치 피드백은 쓰지 않는다** — 개루프라야 개체차가 드러난다.

## 시작하기 전에 읽을 것 (순서대로)

1. **`src/can_powerpack/docs/개체차_보정_인수인계.md`** — 이 작업의 계획·현황·함정.
   **여기부터 읽는다.**
2. `src/can_powerpack/docs/축별_액추에이터_각도제어_절차.md` — 터미널 구성, 기동,
   측정 도구 사용법, 흔한 함정. 운용 절차는 전부 여기 있다.
3. `src/can_powerpack/docs/데드존측정_튜닝_절차.md` — 채널 압력 PID 의 전제.

## 지금 당장의 다음 작업

**3~6축 엔코더 2점 보정이 다른 장비에서 온 미검증 값이다.** 이걸 재측정하기
전에는 그 축들의 각도 데이터가 아무 의미가 없다 (1축은 재측정에서 13° 밀렸다).
인수인계 문서의 "★ 시작 전에 반드시" 절을 본다.

## 항상 지킬 것

- **시뮬레이션으로 검증하지 않는다.** 코드 수정만 하고 실기 확인은 사용자가 한다.
- **밸브 모델을 쓰지 않는다.** 채널 제어는 PID + 실측 데드존 표뿐이다.
- **`control_mode:=0`** 으로 런치한다. 기본값 2 로 띄우면 `PressureRefGen` 이 매 틱
  레퍼런스를 덮어써 외부 지령이 안 먹는다.
- **압력 제어 설정을 건드리지 않는다.** 채널 PID·데드존 표·`i_limit_pct`·레일
  게인은 현재 상태 유지 — 여기가 바뀌면 축간 비교의 기준이 흔들린다.
- **`angle_ctrl.py`(각도 피드백)는 이 브랜치에서 쓰지 않는다.**
- **90° 위는 정적으로 불안정하다.** 측정에 `--ang-max 88` 을 반드시 준다 (기본 125).
- **살아 있는 `/pack2` 네임스페이스에 노드를 더 띄우지 않는다.**
- 과압 소프트웨어 가드를 새로 걸지 않는다. 양압 레일은 270~280 kPa 에서 릴리프가
  열리고, 음압은 최대 진공까지 써도 안전하다. 펌프는 고정 회전수다.
- 진동은 절대 안 된다. 액추에이터가 깨진다. 느려도 되니 천천히 반응하게 한다.

## 빌드

```zsh
cd ~/claude_tutorial && source /opt/ros/humble/setup.zsh
colcon build --packages-select can_powerpack --cmake-args -DCMAKE_BUILD_TYPE=Release
```

**반드시 워크스페이스 루트에서** 한다 (`src/can_powerpack/` 안에 옛 build/install
트리가 남아 있다). yaml 만 고쳤으면 재빌드 없이 런치만 다시 띄우면 된다.
